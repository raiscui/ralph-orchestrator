# 任务计划：回退 Markdown 渲染器到 termimad（撤销 mdfrier）

## 目标

- 恢复项目原本的 Markdown 渲染链路：`termimad`
- 移除/停止使用 `mdfrier`（`mdfried` 风格）相关依赖与代码
- 保证 `cargo fmt --check`、`cargo clippy -- -D warnings`、`cargo test`、replay smoke tests 全部通过
- 不引入新的渲染退化（至少与回退前一致）

## 方案（至少二选一）

### 方案 A：彻底回退（最接近“原本”）

- 移除 Big Headers / 图片块等 `mdfried` 相关渲染特性
- Output 面板完全使用 `termimad` 的文本渲染结果

### 方案 B：最小回退（先恢复 termimad，保留其他 UI 改良）

- 仅把“Markdown→ANSI/Line”渲染器切回 `termimad`
- 其余与渲染器无关的 TUI 结构改良（例如前缀列/并行面板布局）保持不动
- 后续如需再“彻底回退”，再做第二步

> 我先按：方案 B 执行（影响面最小，能最快恢复 `termimad`）。
> 如果你希望改成方案 A，我再继续收敛。

## 阶段

- [x] 阶段1：确认回退范围与现状盘点
- [x] 阶段2：代码回退到 termimad
- [x] 阶段3：依赖清理（Cargo.toml/Cargo.lock）
- [x] 阶段4：编译与测试验证（含 smoke）
- [x] 阶段5：记录与交付（四文件 + 必要文档）

## 关键问题（默认先做最小回退）

1. 你说的“恢复原本”，是否包含撤回 Big Headers/图片块？
   - 默认假设：不包含（只回退渲染器）。
2. 输出面板左侧红色 `E` 是否也要一起回退？
   - 默认假设：先不动（单独处理）。

## 遇到错误

- [记录] `cargo fmt --check` 发现 import 顺序差异，已用 `cargo fmt` 修复并通过复检。

## 状态

**已完成**：
已把 Markdown 渲染器从 `mdfrier` 回退到 `termimad`，并完成全量验证与四文件记录。
下一步建议见本次交付说明（是否进一步撤回 Big Headers/图片块、以及是否回退许可证）。

## 日志

### 2026-01-30 12:38 +0800

- [变更] 用户决定取消 `mdfried/mdfrier` 渲染。
- [目标] 恢复项目原本的 `termimad` Markdown 渲染方式。
- [行动] 先按“最小回退”实施：恢复 `termimad` 渲染链路，并清理 `mdfrier` 依赖。

### 2026-01-30 12:47 +0800

- [完成] 代码渲染入口已回退到 `termimad`，并移除 `mdfrier` 依赖。
- [验证] 已通过 `cargo fmt --check`、`cargo clippy -- -D warnings`、`cargo test`、replay smoke tests。

### 2026-01-30 12:48 +0800

- [交付] 已补齐 `notes.md` / `WORKLOG.md` / `ERRORFIX.md` 记录，并完成本次回退闭环。

