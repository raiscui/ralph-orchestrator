# 笔记：OpenSpec 新建 change（/opsx:new）

## 待补充
- change 主题：
- change 名称（kebab-case）：
- schema/workflow：

## 过程记录
### 2026-01-29 23:47 +0800
- 等待用户描述 change 目标，用于生成 kebab-case 名称并确认。

### 2026-01-29 23:49 +0800
- 需求：更换 Markdown 渲染器，考虑采用 `mdfried` 项目中的 `mdfrier`（或参考 `mdfried` 实现）。
- 许可证：用户接受将本项目改为 GPL3（需在设计 artifact 中写清楚影响与风险）。

### 2026-01-29 23:51 +0800
- 已创建 change：`openspec/changes/tui-markdown-renderer-mdfried/`（schema：`spec-driven`）。
- artifacts 顺序（0/4）：
  - `proposal`（ready）
  - `design`（blocked by: proposal）
  - `specs`（blocked by: proposal）
  - `tasks`（blocked by: design, specs）
- 本仓库当前许可证为 MIT（见根目录 `LICENSE`），若要改为 GPL3，需要在 proposal/design 中明确：
  - 改许可的动机（例如依赖/参考的代码许可要求）
  - 对下游使用者与分发方式的影响
  - 是否涉及第三方贡献者代码的再许可问题（如果有多个贡献者，通常需要确认贡献许可/CLA）
