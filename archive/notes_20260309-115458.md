# 笔记(续档)

- 说明: 旧 `notes.md` 已超过 1000 行,已续档为 `archive/notes_2026-02-28_1706.md`。
- 新笔记从这里开始记录。

## 2026-02-28 17:17 +0800 | continuous-learning: 四文件摘要(因 task_plan 续档触发)

## 四文件摘要(用于决定是否提取 skill)

- 任务目标(task_plan):
  - 续档 `task_plan.md`(超过 1000 行)并保持根目录“低噪音、可检索”。
  - 回答你关于 openclaw 启发与 macOS Xcode license 的疑问,避免把环境问题误归因到 `cargo test`。
- 关键决定(task_plan):
  - 续档: `task_plan.md` -> `archive/task_plan_2026-02-28_1717.md`,新建 `task_plan.md`。
  - 归档: 在完成四文件摘要后,把本次检索覆盖到的历史版本移动到 `archive/`(减少根目录噪音)。
- 关键发现(notes):
  - openclaw 的启发点已经被提炼并落地为多项工程机制:
    - guardrails(可执行护栏进入 CI)
    - doctor/wizard(把常见失败变成可修复路径)
    - lanes + generation + draining(并发与退出收尾的确定性)
    - no-output watchdog(把“卡住无输出”变成一等失败原因)
    - context window guard(把上下文窗不足变成可执行预检)
  - macOS Xcode license 的根因是 Apple toolchain(xcrun/xcode-select)的选择,不是 cargo 自身依赖。
- 实际变更(WORKLOG):
  - 已在更早的实现中补齐:
    - `.envrc` 默认优先使用 Command Line Tools(`DEVELOPER_DIR=/Library/Developer/CommandLineTools`)来绕过 Xcode.app license。
    - `DEVELOPMENT.md` 增加 macOS troubleshooting 小节,避免后续误判。
- 错误与根因(ERRORFIX,如有):
  - 历史上 `cargo test` 失败(exit 69)的根因是 `xcode-select -p` 指向未接受 license 的 Xcode.app,导致 `xcrun` 拒绝执行。
- 可复用点候选(1-3 条):
  1. 续档规则 + 归档流程要“强制化”(否则根目录噪音会持续增长)。
  2. macOS toolchain 选择(`DEVELOPER_DIR`)应当作为“可执行默认”,而不是只写在聊天里。
  3. 把 openclaw 的“机制”而非“口号”迁移到 Rust(guardrails/lanes/doctor)是高 ROI 路线。
- 是否需要固化到 docs/specs: 否(已固化到 `.envrc`/`DEVELOPMENT.md`/`specs/*`/项目级 skills)。
- 是否提取/更新 skill: 否(已存在对应的 `self-learning.*` skills,且本次没有新增更通用的新模式)。

## 2026-02-28 18:31 +0800 | 研究: zeroclaw-labs/zeroclaw 值得借鉴点(映射到 ralph)

## 来源(本地 clone)

- /tmp/zeroclaw-20260228-1832

## 我认为最有启发的设计点(带原文/代码证据)

1. Trait 驱动 + 把扩展点写死成契约
   - README.md 直接把定位写成: "runtime operating system".
   - README.md 用一句话把卖点钉死: "Trait-driven architecture · secure-by-default runtime · provider/channel/tool swappable · pluggable everything".
   - AGENTS.md 直接列出 "Key extension points"(Provider/Channel/Tool/Memory/Observer/RuntimeAdapter/...).
   - 启发:
     - 扩展点不要藏在脑子里.
     - 用 trait + factory 注册点,把贡献路径固定下来.

2. 工具系统: Tool trait + JSON schema + runtime 能力门控 + SecurityPolicy 注入
   - src/tools/mod.rs 写得很明确:
     - tool 需要 name/description/JSON schema/execute.
     - "Security policy enforcement is injected via SecurityPolicy at construction time".
   - src/runtime/traits.rs 用 has_shell_access/has_filesystem_access/supports_long_running 来声明能力.
   - 启发:
     - "能不能用"由 runtime 能力声明决定.
     - "允不允许用"由 security policy 决定.
     - 这两层分开后,很多 if/else 会自然消失.

3. ApprovalManager: supervised 工具调用审批,带 session allowlist + audit log + 非 CLI 渠道 pending approvals
   - src/approval/mod.rs 顶部注释: "session-scoped 'Always' allowlists and audit logging".
   - needs_approval() 的优先级清晰:
     - autonomy_level(full/read_only) -> always_ask -> auto_approve -> session_allowlist -> default supervised.
   - 启发:
     - "工具审批"要有可审计证据(审计日志).
     - "Always" 这种临时放权,要显式建模成 session allowlist.
     - 非 CLI 渠道(telegram/slack)不能阻塞式 prompt,就要做 pending request + 显式确认.

4. Doctor: 结构化诊断结果 + 人类可读报告,并且能做错误分类
   - src/doctor/mod.rs: "Structured diagnostic result for programmatic consumption (web dashboard, API)."
   - check_config_semantics() 把配置问题分为 ok/warn/error,并给出可执行的 message.
   - model probe 里把常见 401/403/429 归类为 auth/access,而不是一律 error.
   - 启发:
     - doctor 不只是"检查",而是"把常见失败变成可修复路径".
     - 结构化结果(Vec<DiagResult>)比纯 stdout 更利于后续 TUI/CI/网页复用.

5. 安全: PromptGuard(快速特征匹配) + E-stop(一等公民,并且 fail-closed)
   - src/security/prompt_guard.rs: Aho-Corasick 多模式匹配 + regex 分类,输出 Safe/Suspicious/Blocked.
   - src/security/estop.rs: state file 解析失败会进入 fail-closed(kill_all=true),并落盘.
   - 启发:
     - "注入检测"要做到可配置(action+sensitivity),并且输出可解释的 patterns.
     - "紧急刹车"要先保证安全(失败即关闭),再谈易用.

6. Sandboxing: Sandbox trait + backend 实现,并且文档明确区分 proposal vs current
   - docs/sandboxing.md 头部明确标注: "Status: Proposal / Roadmap".
   - src/security/firejail.rs 以 wrap_command 方式做最小侵入的命令封装,并用单测锁死 flags.
   - 启发:
     - 先从"命令包装"切入,比一开始做复杂容器编排更容易落地.
     - proposal 文档必须显式标识,避免读者把未来设想当成现状.

7. 供应链与合规: cargo-deny 的 deny.toml
   - deny.toml 里把 unknown-registry/unknown-git 设为 deny.
   - licenses 用 allowlist,并且 ignore 条目都写 reason.
   - 启发:
     - 依赖治理要可审计.
     - ignore 不是"忽略",而是"带理由的延期偿还".

8. 文档信息架构(IA): docs hub + 决策树 + 按受众分类
   - docs/README.md 用表格回答 "I want to...".
   - docs/security/README.md 把文档分成 "Current-Behavior" 和 "Proposal/Roadmap".
   - 启发:
     - 文档是产品的一部分.
     - 对新手: 决策树.
     - 对维护者: runbook.
     - 对安全: 现状和设想分层.

## 对 ralph 的可迁移清单(初稿,按 ROI 排序)

- 低成本,高收益(建议优先考虑):
  - 在 docs 入口增加 "10 秒决策树" 和 "按受众" 导航,降低新用户学习成本.
  - 对现有 doctor 输出补一条 "结构化 JSON" 模式(若我们现在还没有),便于 TUI/CI 复用.
  - 为重要 gate/审批/外部输入增加更明确的审计落盘(谁触发,为什么,结果是什么).

- 中成本,高收益(需要确认边界,避免做成平台):
  - 把 "工具审批" 从实现细节提升为一等协议,并和 gate.request/gate.resolve 统一起来.
  - 引入轻量 PromptGuard,至少先覆盖最常见的系统覆盖/工具注入特征,并把命中证据写入日志.

- 不建议直接照搬(会违背 ralph 的 thin-coordination 目标):
  - 把 orchestrator 扩成 "runtime OS"(channel/memory/gateway/硬件外设全家桶).
  - 过早引入大量 sandbox backend,优先把"允许什么"和"审计证据"做扎实.

## 2026-02-28 20:46 +0800 | 设计: `ralph doctor` JSON 输出 schema(面向 code agent)

### 为什么要做

- 现状: `ralph doctor` 只有文本输出,CI/TUI/code agent 想做自动分流时,只能解析 stdout.
- 目标: 提供稳定的机器可读 schema,同时不破坏原文本输出.

### Schema(最小稳定字段)

- 顶层:
  - `schema_version`: u32(从 1 开始)
  - `verdict`: "pass" | "fail_errors" | "fail_strict"
  - `counts`: { `errors`: usize, `warnings`: usize }
  - `args`: { `fix`: bool, `strict`: bool, `format`: "text"|"json" }
  - `checks`: `DoctorCheck[]`

- DoctorCheck:
  - `id`: 稳定 check_id(用于分类)
  - `category`: 稳定类别(例如 config/hats/backend/workspace/context_window/events_marker/binary)
  - `status`: "ok" | "warn" | "err" | "skipped"
  - `message`: 保留原文本 message(包含 Fix/Skipped/Fixed 等,便于人读和回放证据)
  - `fix`: Option<String>(从 message 里解析 "Fix:" 后半段,便于程序直接展示/提取)

### check_id 初版约定(够用即可,后续可扩)

- config:
  - config.load
  - config.validate

- hats:
  - hats.validation.skipped
  - hats.solo_mode
  - hats.starting_event
  - hats.orphan_event
  - hats.dead_end

## 2026-03-05 17:05 +0800 | Root Cause(并行 TUI "看不到 reply")补充诊断

### 现象复盘(证据)

- `record summary` 显示语义层面确实发生过:
  - `reply.human.message: 1`
  - stdout tail 存在 `<event topic="reply.human.message" reply="...">...</event>`

### 关键推断(更像 UX 可见性问题,而不是“没回复”)

- 并行 TUI 的 Output 面板按 "selected instance" 展示.
- 用户常用的注入方式是外部命令:
  - `ralph emit human.message "..." --target-instance ralph#1`
- 这条路径不会像 TUI 内置 Chat 那样做“发送后自动切到目标实例”.
- 结果:
  - ralph#1 确实回复了.
  - 但 UI 仍停留在其他实例(例如 experiment_runner#1),Output 面板自然看不到 reply.
  - 用户体感就是“没回复 / TUI 显示不出来”.

### 结论

- termimad 吞 `<event ...>` 的问题已经有展示层改写 + 单测.
- 本轮更可能的根因是: reply 到达时缺少“可见性保证”(自动切换/自动跟随底部)。

## 2026-03-06 10:42 +0800 | Root Cause 更新: 这次不是"没切到来源实例",而是"逻辑行滚动低估了真实显示高度"

### 证据对齐

- 用户截图:
  - 左侧已选中 `ralph#1`
  - 底部状态已显示 `reply @ralph#1 (见 Output)`
  - Output 仍停在 prompt/transcript 尾部,没有看到 reply 正文
- `target/release/ralph record summary /Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`:
  - `Topics`: `human.message: 1`, `reply.human.message: 1`
  - `Stdout Tail`: `<event topic="reply.human.message" ...>我是 OpenAI 的 Codex...</event>`
- 录制文件尾部顺序:
  - 500 行附近还是 `------ END PROMPT ------`
  - 501 行才是 stdout reply
  - 502 行才是 `bus.publish(reply.human.message)`

### 关键结论

- 语义层没问题:
  - reply 已经输出到 stdout
  - 也已经被解析成 `bus.publish(reply.human.message)`
- 展示层也不是旧的 `<event>` 吞字问题:
  - Output 里看不到 reply 的真正原因是滚动模型不对
- 真正根因:
  - 并行 Output buffer 以前按"逻辑行"存储和滚动
  - 但右侧 Output 面板真正渲染时会对长行做 soft-wrap
  - 一旦前面有很多长 prompt 行,`row_count()` 会低估真实屏幕高度
  - 结果就是:
    - `following_bottom=true`
    - `scroll_offset` 也被算成"已经到底"
    - 但屏幕其实还停在前面那些被包裹出来的行里

### 本次修复思路

- 不再让 widget 的 soft-wrap 决定"底部是否可见"
- 改为在写入并行 Output buffer 前,先按当前 Output 宽度做 grapheme 级预换行

## 2026-03-08 12:45 +0800 | 整体代码 review: 最终结论与证据

### finding 1: `EventLoop` 的 completion 计数不是"连续确认"

- 现象:
  - 注释与日志都写的是"consecutive confirmation"。
  - 但代码只在"检测到 completion promise 且验证失败"时清零。
- 静态证据:
  - `crates/ralph-core/src/event_loop/mod.rs:897` 会递增 `self.state.completion_confirmations += 1`
  - `crates/ralph-core/src/event_loop/mod.rs:928`
  - `crates/ralph-core/src/event_loop/mod.rs:936`
  - 没有任何"本轮不是 completion 输出时归零"的分支
- 动态证据:
  - 用最小复演程序调用真实逻辑路径,输出为:
    - `r1=None c1=1`
    - `r2=None c2=1`
    - `r3=Some(CompletionPromise) c3=2`
  - 这说明第一轮 completion 后,中间一轮普通输出没有把计数复位,第三轮再次 completion 就直接终止。
- 结论:
  - 这里实现的是"累计两次确认",不是"连续两次确认"。

### finding 2: `ralph-e2e` 的 `analyze.complete` 解析对属性顺序不鲁棒

- 现象:
  - `parse_analysis_event()` 使用的 regex 只匹配 `<event topic="analyze.complete">...`
- 静态证据:
  - `crates/ralph-e2e/src/analyzer.rs:592`
  - 对照更稳的实现:
    - `crates/ralph-cli/src/autopilot.rs:1482`
- 动态证据:
  - 最小 regex 复演结果:
    - `topic_first -> True`
    - `id_before_topic -> False`
    - `multiline_topic_first -> True`
    - `multiline_id_before_topic -> False`
- 结论:
  - 只要模型把 `topic` 放到第二个属性,或者 opening tag 先写 `id=...` 再写 `topic=...`, analyzer 就会漏掉本该接受的 `analyze.complete` 事件。

### finding 3: `integration_agents` watch 测试仍有时序脆弱性

- 现象:
  - 这个测试通过 `sleep(300ms)` 后直接 `kill` 子进程,再断言 stdout 至少打印过一次。
- 静态证据:
  - `crates/ralph-cli/tests/integration_agents.rs:161`
  - `crates/ralph-cli/tests/integration_agents.rs:162`
- 结论:
  - 当前测试更像"赌 300ms 足够跑完首轮输出"。
  - 在 CI 抖动、进程调度变慢或首次启动成本上升时,它会是现成的 flaky 点。

### 口径回滚

- 早先怀疑过 `EventParser` 多行 opening tag 仍有主链路问题。
- 这条怀疑没有进入本轮最终 review 结论。
- 本轮对用户输出时,以以上 3 条已重新核实的问题为准。

## 2026-03-08 13:08 +0800 | 修复记录: review 建议落地后的实现要点

### 1. `EventLoop` completion 必须严格按"连续轮次"计算

- 调整点:
  - 在 `crates/ralph-core/src/event_loop/mod.rs` 中先算出 `completion_detected`
  - 只要当前轮没有形成有效 completion 检测,就把 `completion_confirmations` 复位为 0
- 回归测试:
  - `event_loop::tests::test_completion_promise_resets_after_non_completion_output`
- 结论:
  - 现在只有真正相邻的两次 completion 确认才会终止,中间夹普通输出会重新从 1 开始计数。

### 2. `ralph-e2e analyzer` 不再假设 `topic` 是第一个属性

- 调整点:
  - `parse_analysis_event()` 的 regex 改为:
    - `(?s)<event[^>]*\\btopic="analyze\\.complete"[^>]*>(.*?)</event>`
  - 同时改为取最后一次命中,避免旧分析事件覆盖新结果
- 回归测试:
  - `analyzer::tests::test_parse_analysis_event_accepts_topic_after_other_attributes`
- 结论:
  - `<event id="..." topic="analyze.complete">...` 和多行 opening tag 都能正常解析。

### 3. `integration_agents` watch 测试不再依赖固定睡眠

- 调整点:
  - stdout 改写到临时文件
  - 轮询文件内容直到看到 `Watching` / `writer#1` / `build.task` 或超时
  - 过程中如果子进程提前退出,立刻 panic 并带上已捕获 stdout
- 结论:
  - 这个测试现在依赖"看见证据后再结束",而不是赌 `300ms` 一定够。

### 验证摘要

- 精确测试全部通过。
- `cargo test` 全仓通过。
- `cargo test -p ralph-core smoke_runner` 通过。

## 2026-03-08 13:31 +0800 | 继续落地: 跨 crate 的 `<event ...>` 提取逻辑收敛

### 现象

- `crates/ralph-cli/src/autopilot.rs` 和 `crates/ralph-e2e/src/analyzer.rs` 都在解析 `<event topic="analyze.complete">...</event>`。
- 两边之前各自维护一套提取逻辑:
  - `autopilot` 是本地 regex helper
  - `analyzer` 是另一套本地 regex

### 风险

- 只要 opening tag 容错规则、属性顺序、"取第一个还是最后一个命中"中的任何一条被分别修改, 两边就会再次漂移。

### 本次实现

- 在 `crates/ralph-core/src/event_parser.rs` 新增:
  - `EventParser::extract_last_payload_for_topic(output, topic)`
- 设计选择:
  - 直接复用 `EventParser::parse()` 的 opening tag 识别与属性解析
  - 返回最后一次命中,避免旧事件覆盖新事件
- 调用方收敛:
  - `crates/ralph-cli/src/autopilot.rs` 删除本地 `extract_event_payload()` 并改用 core helper
  - `crates/ralph-e2e/src/analyzer.rs` 改用同一个 core helper

### 补充回归测试

- `event_parser::tests::test_extract_last_payload_for_topic_accepts_multiline_and_attribute_reordering`
- `event_parser::tests::test_extract_last_payload_for_topic_returns_none_when_topic_missing`
- `autopilot::tests::parse_agent_analysis_output_from_stdout_uses_last_matching_event`
- `autopilot::tests::parse_agent_analysis_output_from_stdout_errors_when_event_missing`

### 结论

- 这次不是单纯“把 analyzer 修到和 autopilot 一样”,而是把两边的共同语义真正沉到 core 里。
- 后面如果 `<event ...>` 提取规则还要继续扩,至少这两条链路会一起走。

## 2026-03-07 17:07 +0800 | 调查笔记: “第二条消息没响应”更接近 TUI 可见性问题

### 现象

- 用户给出的 record-session:
  - 只有 1 条 `human.message`
  - 只有 1 条 `reply.human.message`
  - termination 是 `MaxRuntime`,不是 `LOOP_COMPLETE`
- 用 headless `ralph emit` 做最小复现:
  - 连续两条 `human.message` 都能得到 `reply.human.message`
- 用 TUI 做最小复现:
  - 第 2 条消息能进入系统
  - 在 `ralph#1` 忙时,后续消息会被协调层改投给 `ralph#2`

### 静态证据

- `crates/ralph-cli/src/parallel_runner.rs`
  - `human.message` 会被转发给 TUI 观察者
- `crates/ralph-tui/src/state/parallel.rs`
  - `apply_event()` 目前只处理:
    - `reply.human.message`
    - `gate.*`
  - 对 external `human.message` 完全没有 UI 提示
- 结果:
  - 用户发送第二条后,在真正收到 reply 之前,看不到“已接收/已排队给哪个实例”
  - 如果此时消息被改投到 `ralph#2`,当前 UI 也不会立即切过去

### 候选结论

- 这次更像是“消息接收后的可见性断层”,不是调度层吞消息
- 修复方向应优先放在:
  - external `human.message` 的状态提示
  - target_instance 联动切换

## 2026-03-06 14:28 +0800 | Root Cause 校正: 这次截图对应的最终根因是 `app.rs` 宽度同步口径漂移,不是语义层缺 reply

### 再次核对录制文件后的事实

- 文件: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`
- `target/release/ralph record summary ...` 输出:
  - `reply.human.message: 1`
  - `Stdout Tail` 为:
    - `<event topic="reply.human.message" reply="E3creFfu6dgC">我是 Codex, 基于 GPT-5 ...</event>`
- `rg -n ... | tail` 再次看到关键尾部:
  - `500`: `------ END PROMPT ------`
  - `501`: stdout reply event
  - `502`: `bus.publish(reply.human.message)`
  - `503`: `_meta.termination`

### 为什么之前看起来像"没回复"

- reply 实际已经到 stdout,也已经被 parse 成 `reply.human.message`
- 但 TUI 右侧 Output 是否能滚到最底,依赖 `row_count/max_scroll`
- 当前代码里存在两套布局口径:
  - 真正渲染 Output pane: `instances + gap + output`
  - 旧的宽度同步: 漏掉 `gap`,等价于把 Output 当成更宽
- 这个 1 列误差在普通短内容下不明显
- 但用户这次录制里前面有很长的 prompt/transcript
  - pre-wrap 后总行数被低估几行
  - `following_bottom` 虽然为真,`max_scroll` 却偏小
  - 最终视口停在 `END PROMPT` 附近,reply 掉到屏幕外

### 为什么不回退之前那几处代码

- `state/parallel.rs` 与 `state/parallel/output.rs` 的可见性修复仍然成立:
  - reply 到来时自动选中来源实例
  - Rendered 模式 `<event ...>` payload 可见
  - buffer 预换行后 `row_count` 更贴近可见行
- 这次真正缺的不是撤回这些修复
- 而是把 `app.rs` 里"宽度同步"与"真实布局"统一成同一套 helper

### 本次最终修复

- `crates/ralph-tui/src/app.rs`
  - 新增 `split_parallel_pane_areas(content_area)`
  - 启动动画/宽度同步/真实渲染全部改为复用同一套布局
  - autoscroll 顺序调整为:
    1. 先根据真实 `output_area` 调 `set_output_render_width(...)`
    2. 再依据新 `row_count()` 计算 `max_scroll`

### 最终验证

- `cargo fmt --all`
- `cargo test -p ralph-tui --lib app::tests::split_parallel_pane_areas_accounts_for_gap_and_border_in_output_width -- --exact`
- `cargo test -p ralph-tui --lib state::parallel::tests::reply_remains_visible_when_previous_output_lines_wrap_heavily -- --exact`
- `cargo test -p ralph-tui`
- `cargo test`
- `cargo test -p ralph-core smoke_runner`
- `cargo build --release`

## 2026-03-06 14:41 +0800 | live run 补充证据: release 版真实在 `END PROMPT` 后输出 reply

### 运行

- 工作区: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-example`
- 命令:
  - `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-orchestrator/target/release/ralph run --record-session /tmp/ralph-live-check.jsonl`
  - `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-orchestrator/target/release/ralph emit human.message "你是什么模型" --target-instance ralph#1`

### 结果

- PTY 直接抓出来的 ANSI 过于嘈杂,不适合人工阅读
- 但 `record summary /tmp/ralph-live-check.jsonl` 与 `rg -n ...` 仍明确显示:
  - `500`: `------ END PROMPT ------`
  - `501`: `[codex-app-server] job canceled ...`
  - `502`: stdout reply `<event topic="reply.human.message" ...>我是 Ralph 协调代理实例 ...`
- 这再次说明:
  - 用户体感的"没回复"不是因为 stdout 根本没有东西
  - 关键问题仍是 TUI 视口/布局能否把 `END PROMPT` 后的最新 stdout 内容带进可见区
- 这样:
  - `row_count()/scroll_offset/visible_lines()` 和真实可见行数重新对齐
  - reply 到达后,自动到底就真的能落到最后一屏

- backend:
  - backend.skipped
  - backend.auto_detect
  - backend.custom.command_required
  - backend.custom.command_available
  - backend.custom.version_failed
  - backend.custom.command_not_runnable
  - backend.available

- context_window:
  - context_window.skipped_backend_failed
  - context_window.skipped_not_configured
  - context_window.size
  - context_window.prompt_fit
  - context_window.prompt_fit.skipped

- workspace/scratchpad:
  - workspace.scratchpad.skipped
  - workspace.scratchpad_dir
  - workspace.scratchpad_exists
  - workspace.scratchpad_writable
  - workspace.scratchpad_path_kind

- events marker:
  - events_marker.missing
  - events_marker.invalid
  - events_marker.events_file_writable

- binary freshness:
  - binary_freshness.skipped
  - binary_freshness.stale
  - binary_freshness.ok

### 与 spec 的关系

- `specs/ralph-doctor.spec.md` 目前把 `--json` 放在 "后续扩展".
- 本次实现会把它提升为正式能力,并补齐回归测试(保证 schema 稳定).

## 2026-03-01 17:38 +0800 | 设计探索: hats 用 `ralph emit` 通信,同时对 `turn_action` 做 fail-closed 边界

### 现状(代码证据)

- `ralph emit` 已存在,支持 `--target/--target-instance/--spawn-instance/--workspace-strategy/--session-strategy/--turn-action`:
  - `crates/ralph-cli/src/main.rs`(EmitArgs)
  - `crates/ralph-cli/src/main.rs`(emit_command 写 JSONL)
- 并行 Supervisor 会轮询 `.ralph/current-events` 指向的 JSONL,把外部事件映射为 `ralph_proto::Event` 并路由:
  - `crates/ralph-core/src/parallel/supervisor.rs`(外部事件读取与映射)
  - 映射时当前会把 `turn_action` 直接带入,没有权限校验.
- 并行 runner 在 spawn hat backend 进程时,已经注入了:
  - `RALPH_HAT_ID`
  - `RALPH_HAT_INSTANCE_ID`
  - `crates/ralph-cli/src/parallel_runner.rs`(Spawning headless job)

### 风险

- 只要 hat 能执行 shell,就能执行 `ralph emit ... --turn-action interrupt`,从而在运行时打断 ralph#1 的 in-flight turn.
- 这属于典型的"控制面信号被数据面误用"问题,和普通 topic 消息不是一个风险等级.

### 我建议的边界(满足: hats 可沟通,但 steer/interrupt 不乱飞)

- 数据面(允许所有 hats): topic + payload(+可选 target/target_instance,且仍受订阅拓扑 strict 校验约束).
- 控制面(默认拒绝,fail-closed): `turn_action=steer|interrupt`(以及通常需要 app_server 的那类 in-flight 控制).
- hats 如果确实需要"打断/插话",改为发 request:
  - `turn_action.request` 或 `control.request_interrupt` 之类 topic,交给 ralph#1 或 Supervisor 决策并执行真正的 steer/interrupt.

### 4.2 的"先能用"落地思路(不依赖 guard token)

- 在 `ralph emit` CLI 里:
  - 如果检测到环境变量 `RALPH_HAT_INSTANCE_ID` 存在(说明是在 hat job 环境里调用),
    就直接拒绝 `--turn-action steer|interrupt` 并输出可行动的错误信息.
  - 这样模型误触发时会当场失败,hat 能从 tool 输出里自我纠正.

### 4.1 的增强(后续)

- 对 `<event ...>` 协议引入 guard token(或干脆在并行模式禁用 `<event>` 解析),进一步降低"文本里举例导致误触发"。

## 2026-03-01 17:45 +0800 | 决策: hat-to-hat 采用 request/result(数据面),不使用 steer 回传结果

### 结论(你已确认)

- hats 之间沟通只走 data-plane: `ralph emit <topic> <payload>`。
- `turn_action=steer|interrupt` 仅用于 ExternalInput -> ralph#1 的 control-plane。

### 为什么“B 回传 A 的结论”不需要 steer

- 即使 A 正在 Running,Supervisor 仍会把 B 的结果事件路由到 A 的 instance。
- A instance 在 Running 时不会被“真 in-flight 注入”(因为没有 turn_action),而是把事件放进 pending 队列。
- 因此 A 会在下一次 job/turn 中消费该结果,实现稳定的“异步 await”。

### 推荐协议形态(最小字段,够用即可)

- A -> B: `subtask.request`(或更具体的 topic)
  - payload(JSON) 包含:
    - `request_id`: 稳定关联 id
    - `task`: B 要做的事(必须足够自包含,避免依赖 A 的上下文)
    - `reply_to`: `{ "topic": "subtask.result", "target_instance": "hatA#1" }`

- B -> A: `subtask.result`
  - payload(JSON) 包含:
    - `request_id`
    - `status`: `"ok"|"error"`
    - `final`: `true`(A 只在 final=true 时推进,避免中途消息污染)
    - `result` 或 `error`

## 2026-03-01 22:45 +0800 | OpenSpec: proposal 已完成(emit-control-plane-fail-closed)

- 产物:
  - `openspec/changes/emit-control-plane-fail-closed/proposal.md`
- proposal 锁定的边界:
  - data-plane: hats 间沟通只用普通 `ralph emit topic=... payload=...`。
  - control-plane: `turn_action=steer|interrupt` 仅允许 ExternalInput 对 `ralph#1` 使用。
- fail-closed 策略:
  - hat job 环境(`RALPH_HAT_INSTANCE_ID` 存在)禁止 `ralph emit --turn-action ...`。
  - 使用 `--turn-action` 时必须显式 `--target-instance ralph#1`,否则拒绝(避免误投递)。
- 状态:
  - `openspec status` 已解锁 `design/specs`(ready)。

## 2026-03-01 23:57 +0800 | OpenSpec: design 已完成(实现落点与风险收敛)

- 产物:
  - `openspec/changes/emit-control-plane-fail-closed/design.md`
- design 的关键决策(摘录):
  - CLI 侧快速自纠:
    - hat job 环境(检测 `RALPH_HAT_INSTANCE_ID`)硬拒绝 `ralph emit --turn-action steer|interrupt`。
  - Supervisor 侧最终裁判(防御纵深):
    - 任意外部事件只要携带 `turn_action=steer|interrupt`,就必须显式且仅能 `target_instance=ralph#1`,否则拒绝并告警.
  - TUI 侧本地预检:
    - `!steer/!interrupt` 仅允许作用于 `ralph#1`,避免“写入后被拒绝”的黑盒体验.
- 明确 trade-off:
  - 放弃对 worker hats 的 in-flight steer/interrupt,换取无人值守环境下的稳定性与可预期.
- 后续增强方向(未纳入 4.2):
  - guard token 或 source attribution,把“只有 ExternalInput 能 steer/interrupt”做成更强约束.

## 2026-03-02 00:31 +0800 | OpenSpec: specs + tasks 已完成(可进入 apply)

- delta specs(Modified Capabilities):
  - `openspec/changes/emit-control-plane-fail-closed/specs/parallel-hat-instances/spec.md`
  - `openspec/changes/emit-control-plane-fail-closed/specs/parallel-trigger-routing/spec.md`
- tasks:
  - `openspec/changes/emit-control-plane-fail-closed/tasks.md`
- 规格要点(再次强调边界,避免实现漂移):
  - data-plane: hats 之间/hat->ralph 只用普通 `topic/payload`。
  - control-plane: `turn_action=steer|interrupt` 必须显式 target 到 `ralph#1`,其余一律 fail-closed 拒绝。

## 2026-03-02 00:36 +0800 | 补充: hat-to-hat “不在中途 reply,只回最终结论”已写入 OpenSpec change

- 已补写到:
  - `openspec/changes/emit-control-plane-fail-closed/design.md` 新增 D5:
    - A 触发 B 的子任务后,B 仅在自身 job/turn 结束时回传一次最终结论(例如 `subtask.result`),不在中途回传半成品.
  - `openspec/changes/emit-control-plane-fail-closed/tasks.md` 新增 4.3:
    - 要求把该约定同步写入 `specs/parallel-event-channels.spec.md`(面向 code agent 的使用指南)。

## 2026-03-02 12:34 +0800 | 确认决策: `<event>` 暂不收口,但 external turn_action 拒绝需可见告警

- 你确认的范围:
  - 4.2 暂不处理 in-band `<event ...>` 产生的 `turn_action=steer|interrupt`。
  - 4.2 只收敛 out-of-band external JSONL 注入(`ralph emit`/TUI 写 JSONL)的 `turn_action`.
- 你确认的行为:
  - all_hat 文档示例允许改掉,避免 hats 学到“对 worker 做 steer”的用法.
  - Supervisor 拒绝 external control-plane 注入时,需要让 `ralph#1` 明确看到(不只写日志)。
- 已同步到 OpenSpec change:
  - `openspec/changes/emit-control-plane-fail-closed/design.md`: 明确 external-only 范围,并指定拒绝时复用 `routing.escalate` 给 `ralph#1` 发告警.
  - `openspec/changes/emit-control-plane-fail-closed/specs/parallel-hat-instances/spec.md`: requirement 文案改为 external-only.
  - `openspec/changes/emit-control-plane-fail-closed/specs/parallel-trigger-routing/spec.md`: rejection 场景要求 emit `routing.escalate`。
  - `openspec/changes/emit-control-plane-fail-closed/tasks.md`: 增加 2.4(告警)与 4.4(更新 `config/all_hat.md`)。

## 2026-03-02 16:22 +0800 | apply-change 实施笔记: emit-control-plane-fail-closed

### 目标与范围

- 按 OpenSpec change `emit-control-plane-fail-closed` 完成 17/17 任务。
- 本轮收敛范围是 external JSONL 注入路径(`ralph emit` + TUI 写 external events),不收口 in-band `<event ...>` 控制面。

### 关键实现点

1. CLI fail-closed(`crates/ralph-cli/src/main.rs`)
- 新增 `validate_emit_control_plane_args()`。
- 在 hat 环境(`RALPH_HAT_INSTANCE_ID` 存在)下,拒绝 `--turn-action steer|interrupt`。
- `--turn-action steer|interrupt` 强制 `--target-instance ralph#1`。
- 拒绝与 `--target`、`--spawn-instance` 同用。

2. Core 最终裁判(`crates/ralph-core/src/parallel/supervisor/routing.rs`)
- 新增 external turn_action 校验:
  - 仅 external(JSONL,无 source/source_instance)触发该校验。
  - `turn_action=steer|interrupt` 且非 `target_instance=ralph#1` 时拒绝。
  - 若带 `target` 或 `spawn_instance=true` 同样拒绝。
- 拒绝时记录结构化 warning,并复用 `routing.escalate` 告警到 `ralph#1`。

3. TUI 本地预检(`crates/ralph-tui/src/app.rs`)
- 新增 `validate_human_message_turn_action_target()`。
- `!steer/!interrupt` 若目标不是 `ralph#1`,TUI 直接报错并 `continue`,不会写 external JSONL。

4. 文档同步
- `specs/parallel-event-channels.spec.md`:
  - 新增 S5.1 external control-plane 边界。
  - 新增 S5.2 request/result 只回最终结论约定。
- `examples/parallel-experimental-dev-engine/README.md`:
  - 明确 steer/interrupt 仅允许 `ralph#1`。
- `config/all_hat.md`:
  - 去掉误导 worker steer 示例,改为 `ralph#1`。
  - 明确 hats 禁止 `--turn-action steer|interrupt`。

### 测试与验证

- `cargo fmt --check` 通过。
- `cargo test -p ralph-cli` 通过。
- `cargo test -p ralph-core` 通过。
- `cargo test -p ralph-core smoke_runner` 通过。
- `cargo test` 全量通过。

### 中途问题

- 编译错误: `Option<HatId>` 误用 `as_deref()`。
- 修复: 改为 `as_ref().map(HatId::as_str)`。


## 2026-03-02 16:40 +0800 | e2e 实跑验证(干净目录 + ralph-e2e)

### 验证目标

- 在干净目录中用当前修改后的 `ralph` 做黑盒验证,确认 external control-plane fail-closed 生效。
- 额外跑一条 `ralph-e2e` 官方场景作为补充证据。

### 干净目录黑盒验证

- 工作目录:
  - `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-example/e2e-clean-20260302-151150`
- 运行命令:
  - `cargo run --manifest-path .../ralph-orchestrator/Cargo.toml --bin ralph -- run -c ralph.yml --no-tui`

#### CLI fail-closed 证据

1) 非 `ralph#1` 目标被拒绝
- 命令: `... ralph emit ... --target-instance writer#1 --turn-action steer`
- 结果: 非零退出,错误包含 `requires --target-instance ralph#1`。

2) hat 环境使用 control-plane 被拒绝
- 命令: `RALPH_HAT_INSTANCE_ID=writer#1 ... ralph emit ... --target-instance ralph#1 --turn-action interrupt`
- 结果: 非零退出,错误包含 `reserved for ExternalInput`。

#### Supervisor 拒绝 + 告警证据

- 手工向 external events JSONL 追加 3 条非法 control-plane 事件:
  - `target_instance=writer#1`
  - `target=writer` + `turn_action`
  - `spawn_instance=true` + `turn_action`
- 观察到运行日志 warning:
  - `Rejected external control-plane turn_action event`(三次)
- 观察到事件日志:
  - 文件: `.ralph/events.jsonl`
  - 存在 3 条 `routing.escalate` 记录,payload 分别对应三种拒绝原因。

### ralph-e2e 场景验证

- 先执行 `cargo run -p ralph-e2e -- codex --list` 确认 codex 可用场景。
- 执行:
  - `cargo run -p ralph-e2e -- codex --filter parallel-emit-spawn-instance --skip-analysis --keep-workspace --verbose`
- 结果:
  - `✅ parallel-emit-spawn-instance (99.7s)`
  - Live report: `.e2e-tests/report-live.md`
  - Report: `.e2e-tests/report.md`

### 备注

- `--filter connect` 与 `--filter single-iter` 在 codex backend 下无匹配场景(非代码问题,是场景集合差异)。


## 2026-03-02 15:22 +0800 | e2e 结果补录: parallel-app-server-steer-live-reply-multi-turn

### 目标

- 执行你选择的后续项 `1`,验证 live steer 场景是否可稳定通过。

### 执行命令

- `cargo run -p ralph-e2e -- codex --filter parallel-app-server-steer-live-reply-multi-turn --keep-workspace --verbose`

### 结果

- 结果: PASS
- 耗时: 90.1s
- live 报告: `.e2e-tests/report-live.md`
- 汇总报告: `.e2e-tests/report.md`
- 场景工作区: `.e2e-tests/parallel-app-server-steer-live-reply-multi-turn/`

### 关键证据摘录

- `report-live.md`:
  - `Progress: 1/1 scenarios completed`
  - `Elapsed: 90.1s`
  - `Passed: 1 | Failed: 0`
- `report.md`:
  - `PASSED`
  - `Duration: 90.1s`
  - `parallel-app-server-steer-live-reply-multi-turn | 90.1s | Good`

## 2026-03-02 16:12 +0800 | 修复笔记: 默认 markdown 报告未刷新 report.json

### 现象

- 执行 `ralph-e2e` 默认参数(即 `--report markdown`)后,`report.md` 会更新。
- 但 `report.json` 可能保留历史内容,导致 md/json 结论不一致。

### 根因

- CLI 的 `ReportFormat::Markdown` 在 `main.rs` 中映射到库层 `LibReportFormat::Markdown`。
- `ReportWriter::write` 在该分支仅写 `report.md`,不会触碰 `report.json`。

### 修复策略

- 在 CLI 层把 `ReportFormat::Markdown` 的有效输出改为 `LibReportFormat::Both`。
- 保持 `--report json` 与 `--report both` 语义不变。

### 验证

1) 自动化测试
- `cargo test -p ralph-e2e` 通过。
- `cargo test` 通过。

2) 运行期验证
- 命令: `cargo run -p ralph-e2e -- codex --mock --filter parallel-emit-spawn-instance --report markdown --skip-analysis`
- 输出明确包含:
  - `Report written: .../.e2e-tests/report.md`
  - `Report written: .../.e2e-tests/report.json`
- 两文件 mtime 一致(同一秒),且 `report.json` 内容与本轮场景一致。

### 额外观察

- `--mock --filter connectivity` 在本地出现长时间无输出,本次为节省时间已中止并改用单场景 mock 验证。

## 2026-03-02 16:39 +0800 | live backend 复验: report.md/report.json 同轮一致

### 验证目标

- 在真实 backend 场景下复验默认 `--report markdown` 是否同步刷新 `report.json`。

### 执行命令

- `cargo run -p ralph-e2e -- codex --filter parallel-app-server-steer-live-reply-multi-turn --report markdown --keep-workspace --verbose`

### 结果

- 场景: `parallel-app-server-steer-live-reply-multi-turn`
- 结果: PASS
- 耗时: 90.1s

### 证据

1) 终端输出
- 同时出现两条写盘日志:
  - `Report written: .../.e2e-tests/report.md`
  - `Report written: .../.e2e-tests/report.json`

2) `report.md`
- `Generated: 2026-03-02T08:38:06Z`
- `Duration: 90.1s`
- `Passed: 1 / Failed: 0`
- 场景 id: `parallel-app-server-steer-live-reply-multi-turn`

3) `report.json`
- `timestamp: 2026-03-02T08:38:06.951837Z`
- `duration: 90.093945166`
- `passed: 1, failed: 0, total: 1`
- `scenario_ids: [parallel-app-server-steer-live-reply-multi-turn]`

4) 文件时间戳
- `report.md mtime: Mar 2 16:38:06 2026`
- `report.json mtime: Mar 2 16:38:06 2026`

### 结论

- live 路径下也已确认: 默认 markdown 运行会同步刷新 json,md/json 不再错位。

## 2026-03-03 10:18 +0800 | 现象复盘: record-session 看起来"没回复就结束"

### 来源(用户给的证据)

- record-session: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`
  - `bus.publish` 仅 1 条: topic=`human.message`(payload=`你是什么模型?`,target_instance=`ralph#1`)
  - `ux.terminal.write` 全部是 stderr(prompt transcript),没有 stdout
  - 文件内未看到 `reply.human.message`
- external events: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-example/.ralph/events-20260303-014507.jsonl`
  - 仅 1 条 `human.message`

### 本地复跑(用于定位根因)

- 场景: 在 `ralph-example` 目录,parallel + idle_start + no-tui
- 复跑结论:
  - `human.message` 触发 ralph#1 走 Codex app-server(`session_strategy=app_server` 默认值)。
  - reply 实际会在 turn 完成后才出现,不是立刻返回。
  - record-session 默认使用 `BufWriter`,运行中/被中断时,尾部记录可能还留在缓冲区:
    - 这会导致你从磁盘直接看 JSONL 时,出现"只看到 human.message,没看到 reply"的错觉。

### 初步结论

- 需要两类改良:
  1) record-session 开头增加 `_meta.session_start`(cwd/workspace_root/argv),便于定位"在哪个项目里跑的,跑了什么命令"。
  2) recorder 对关键记录做更积极的 flush(至少 bus.publish + stdout 的 terminal.write),提升中断/崩溃场景下的证据完整性。

## 2026-03-03 13:32 +0800 | 复盘: 为什么 E2E 抓不到 durability bug,以及如何兜底

### 现象

- "大 E2E 全绿",但 record-session JSONL 在 Ctrl+C 后看起来像"没回复就结束".

### 本质

- 这是 durability/observability contract 的问题,不是 topic 语义问题.
- run-to-completion 的 E2E 往往天然绕过 `BufWriter` 这类缓冲时序风险.

### 规避策略(A + B)

1) backpressure tests(更对症)
- unit: 针对 recorder flush 策略与 JSONL 行级可解析性.
- integration: 启动 `ralph run --idle-start --record-session ... --no-tui`,发送 SIGINT,断言 JSONL 仍逐行可解析且包含 termination(reason=Interrupted).

2) `ralph record watch`(更可观测)
- 面向增长中的 JSONL,容忍末尾半行 JSON.
- 默认人类可读输出: meta + termination + topic_counts + stdout tail.

### 已创建 OpenSpec change

- `openspec/changes/record-session-contract-and-watch/`

## 2026-03-03 13:42 +0800 | 架构复盘: 为什么 durability bug 会漏测,以及系统层面如何防漂移

### 我当前对项目整体架构的理解(高层)

- 设计哲学在仓库里写得很明确:
  - "The orchestrator is a thin coordination layer, not a platform."(README.md)
  - Tenet #2: "Backpressure Over Prescription"(README.md)
- 因此测试与可观测的定位应当是:
  - 语义问题用 e2e/replay/smoke 去卡.
  - 证据落盘问题用 contract tests 去卡,而不是继续堆大 e2e.

### 我观察到的"容易产生口径漂移"的边界

1) 多份 JSONL,语义不同,但长得像
- `.ralph/events*.jsonl`: 外部输入事件流(EventReader 读取).
- `--record-session` JSONL: cassette(Record/SessionRecorder/SessionPlayer).
- `.ralph/diagnostics/*.jsonl`: 诊断日志(agent-output/orchestration/trace/...).

2) 类型/命名重复
- `ralph_proto::Event`(内存中的 bus 事件) vs `event_reader::Event`(外部输入 jsonl) vs `event_logger::EventRecord`(调试日志).
- 这类重复不是一定要消灭,但需要在 docs/spec 里把"每个文件的 contract"写清楚,否则工具会各自演进.

### 新想法(未落地,但值得纳入设计)

- 把"journal contract"显式化,并作为一等 spec:
  - 每种 JSONL 的 source-of-truth,append-only 约束,flush 约束,容错策略(strict vs tolerant).
- `ralph record` 工具尽量复用 autopilot 的聚合口径,但不要把 autopilot 的业务断言混进去.
- watch 这类增量读取,建议共享"聚合器",而不是强行复用 strict parser(SessionPlayer).

## 2026-03-03 21:34 +0800 | 设计决策更新: record watch 的默认语义与中断契约

### 背景

- 之前的草案里,`record watch` 默认是“人类可读摘要 + 周期刷新”.
- 但用户明确偏好是:
  - watch 更常用来做 B: 像 `tail -f` 一样吐原始 record 行(低抽象).
  - Ctrl+C 的最终语义更像“尽量写完 termination 再退出”(证据完整优先).
  - watch 无参自动定位可以有,但 record-session 文件不一定在 `.ralph/`.

### 收敛后的默认语义

1) `ralph record watch [FILE]`:
   - 默认 raw follow: 输出新增的完整 JSONL 行,不做重排/不做 schema 解析.
   - 末尾 incomplete line(没有换行符)不输出,也不算 malformed.
   - 可通过管道交给 `jq`/`rg` 做过滤与诊断.

2) 自动定位:
   - `ralph run --record-session <FILE>` 时,在 workspace_root 写入 `.ralph/record-session.latest` 指针(内容为 `<FILE>` 的可解析路径).
   - `ralph record watch` 无参时,从该指针定位实际 record 文件(避免扫描磁盘猜路径).

3) Ctrl+C/SIGTERM:
   - contract tests 以“写 `_meta.termination(reason=Interrupted)` 并 flush”为硬门槛(可控中断必须尽量完成收尾).
   - 实现层面倾向“只杀子进程组,不 kill 自己所在进程组”,避免 termination 来不及落盘.

4) parallel iteration:
   - 口径定义为“协调者 ralph 的 job 完成次数”,用于 `_meta.termination.iterations`.

## 2026-03-04 01:31 +0800 | continuous-learning: WORKLOG 续档前复盘(record-session contract + watch)

## 四文件摘要(用于决定是否提取 skill)

- 任务目标(task_plan.md):
  - 把 record-session 当成“证据流 contract”,用更对症的 backpressure(专用 contract tests)兜住 Ctrl+C/SIGTERM 的落盘语义。
  - 同时提供 `ralph record summary/watch` 作为离线/在线排障入口,避免“只有 record 文件但看不懂/不确定它在不在增长”。
- 关键决定(task_plan.md):
  - Ctrl+C/SIGTERM: 语义优先“尽量写完 `_meta.termination(reason=Interrupted)` 并 flush 再退出”(证据完整优先)。
  - `ralph record watch`: 默认 raw follow(像 `tail -f`),只输出新增的完整 JSONL 行;末尾半行 JSON 不得提前输出。
  - 无参定位: 不扫描磁盘猜路径;由 `ralph run --record-session <FILE>` 写入 `.ralph/record-session.latest` 指针,watch 读取指针定位(因为 record 文件不一定在 `.ralph/`)。
  - parallel iteration: 定义为“协调者 ralph 的 job 完成次数”,写入 `_meta.termination.iterations` 便于排障。
- 关键发现(notes.md):
  - 这次用户踩坑的本质不是“语义(topic/路由)错了”,而是“证据落盘(durability/flush)的时序问题”。
  - run-to-completion 的 E2E 很容易绕过 Ctrl+C/SIGTERM + BufWriter flush 这类风险,所以 E2E 全绿也不等于“record 可审计”。
- 实际变更(WORKLOG.md):
  - 共享 strict parser/aggregator,`autopilot` 与 `record summary` 复用同一口径,降低漂移。
  - 新增 `ralph record summary/watch`,并实现 `.ralph/record-session.latest` 指针写入与无参 watch 自动定位。
  - 并行中断路径: 避免“kill 自己所在进程组”的自杀式策略;让每个 backend job 自成进程组,中断时 kill 子进程组,并让 orchestrator 有机会写 termination/flush。
  - 回归测试: SIGINT 集成测试 + watch partial-line 单测,把 contract 变成硬护栏。
  - 文档: 补齐 CLI reference 与 testing doc,解释“为什么 E2E 会漏掉 durability,以及 contract tests 怎么兜底”。
- 错误与根因(ERRORFIX.md,如有):
  - 典型根因链: “killpg 自杀式中断” + “BufWriter 未 flush” => `_meta.termination`/末尾 stdout 证据不可见或丢失。
- 可复用点候选(1-3 条):
  1) **把日志/录制当证据流时,优先用 contract tests 验证中断与落盘语义**,不要指望 run-to-completion E2E 自然覆盖。
  2) **watch 工具默认 raw follow,要明确 partial-line 语义**,否则会把“增长中的文件”误判为 malformed。
  3) **外部路径的自动定位用“显式指针文件”比扫描磁盘可靠**,尤其当证据文件不在固定目录时。
- 是否需要固化到 docs/specs: 是。
  - 已固化到:
    - `docs/guide/cli-reference.md`(新增 `ralph record summary/watch` 说明与示例)
    - `docs/advanced/testing.md`(增加 record 工具用法 + E2E vs contract tests 说明)
    - `specs/parallel-record-session.spec.md`(补齐“为什么 E2E 会漏掉 durability”与 contract tests 兜底口径)
- 是否提取/更新 skill: 否(本轮不新增)。
  - 理由:
    - “stdout-only 事件解析”与“kill 进程组”的关键坑点,仓库里已有对应的 `self-learning.*` skill 覆盖。
    - 本轮新增的关键知识已通过 spec + docs + contract tests 固化,更适合留在项目文档而不是再造一个重复 skill。

## 2026-03-05 12:31 +0800 | 并行 TUI "看不到 reply" 的证据与初步假设

### 证据(来自 record-session)

- 文件: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`
- `target/release/ralph record summary /Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl` 的关键输出(原文摘录):
  - `reply.human.message: 1`
  - `Stdout Tail`
    - `<event topic="reply.human.message" reply="oug_jDQUpAZu">我是 OpenAI 的 GPT-5.2。当前在 Codex CLI 里以 ralph#1(并行协调者)身份运行,主要负责派发任务事件并收敛结果,不直接写代码实现。</event>`

### 初步假设(更像交互层,不是语义层)

- durability/语义层: 该 run 确实产生了 `reply.human.message`(record-summary 已证明)。
- display/交互层: TUI 里不显示,更可能是以下原因之一:
  1) Chat 里显式 `@ralph#1` 发消息,但 Output 面板仍停留在其它实例,导致用户没看到 ralph#1 的输出。
  2) 仍存在某条渲染路径没有应用 `reply.human.message` 的展示层改写(或仍被 termimad 吞掉)。

## 2026-03-05 14:45 +0800 | continuous-learning: 并行 TUI 看不到 reply 的排障与修复摘要

## 四文件摘要(用于决定是否提取 skill)

- 任务目标(task_plan.md):
  - 让并行 TUI 下 `reply.human.message` 的“人类可见回复”稳定可见。
  - 把“有证据但看不到”的问题,从 durability/语义层与 display/展示层拆开处理。

- 关键决定(task_plan.md):
  - 以 record-session 为事实源,先钉死 `reply.human.message` 是否发生,再决定动哪条链路。
  - 展示层修复优先“改良胜过新增”: 不改事件语义,只在渲染/交互/退出路径做最小改良。

- 关键发现(notes.md):
  - `termimad` 在 Rendered 模式下会把 `<event ...>` 当作 HTML 片段处理,导致整段被吞,体感像“没回复”。
  - 即使 record-session 有 reply,若 TUI 在 Ctrl+C 后立刻退场,最后几帧也可能来不及渲染(典型 display 假阴性)。

- 实际变更(WORKLOG.md):
  - 展示层: Rendered 模式下对 `reply.human.message` 的 `<event ...>` 做 payload 提取改写(仅展示用)。
  - 交互层: Chat 显式 `@target` 发送后自动切到目标实例输出。
  - 兜底可见性: 收到 `reply.human.message` 时,底部 status 显示短预览。
  - 退出语义: 第一次 Ctrl+C 只 request_interrupt 并等待 terminated;第二次 Ctrl+C 强制退出。

- 错误与根因(ERRORFIX.md,如有):
  - 根因类型从“termimad 吞 HTML”扩展到“Ctrl+C 过早退出 UI loop”。

- 可复用点候选(1-3 条):
  1) “UI 空白/看不到回复”先分 durability vs display,用 record-session 快速判别。
  2) 对多渲染路径系统,展示层修复必须做 2x2 矩阵回归(TUI/Pretty x Rendered/Plain)。
  3) TUI 中断语义应等待 terminated/drain,否则会制造“证据有但人没看到”的假阴性。

- 是否需要固化到 docs/specs: 否。
  - 理由: UI 已在第一次 Ctrl+C 时给出明确 status 提示,且相关决策树已在根 `AGENTS.md` 固化。

- 是否提取/更新 skill: 否。
  - 理由: 这是项目内 TUI 具体实现细节,更适合留在代码与 ERRORFIX/WORKLOG 里,不再额外造一个重复 skill。

## 2026-03-05 21:11 +0800 | 并行 TUI "看不到 reply" 的展示层加固: `<event` opening tag 多行

### 关键观察

- TUI Output 面板里经常能看到 prompt transcript(多在 stderr),但 reply 的 `<event topic="reply.human.message" ...>` 仍可能被吞。
- `termimad` 在 Rendered Markdown 模式下会把形如 `<tag ...>` 的块当作 HTML 片段处理。
- 因此只要 reply 主要以 `<event ...>` 形式输出,就存在"整段不可见"的风险。

### 这次确认的边界条件

- 我们之前的展示层改写只匹配 `<event ` (event 后紧跟空格)。
- 但模型输出偶发会把 opening tag 格式化成多行,例如:

  <event
    topic="reply.human.message" reply="...">payload</event>

- 这种情况下并不包含 `<event ` 子串,导致改写不会触发,显示路径仍会被吞。

### 修复策略

- 不改变事件解析/路由语义,只在渲染前做展示层改写。
- 把匹配从 `<event ` 放宽为 `<event` + (任意空白 or `>`),并增加"防误判"(避免 `<eventual...>` 被当成事件)。

### 回归测试

- `ralph-adapters`: `markdown_rendered_mode_shows_reply_human_message_payload_with_multiline_opening_tag`
- `ralph-tui`: `parallel_output_rendered_shows_reply_human_message_payload_instead_of_event_wrapper`

## 2026-03-05 22:38 +0800 | UX 口径: Chat 区域=输入,Output 区域=对话正文(像 AI Chat)

### 用户期望

- Chat 窗口只作为输入框(类似 Slack/ChatGPT 的底部输入栏)。
- 回复正文应当出现在 Output 面板,而不是 Chat status 行。

### 我们的取舍

- Chat status 的职责收敛为"提示/结果/错误"(例如 sent ok,写文件失败,Ctrl+C 正在退出)。
- reply.human.message 的正文展示完全交给 Output 面板:
  - stdout 输出里仍是 `<event topic="reply.human.message">payload</event>`。
  - Rendered 渲染前会抽 payload 隐藏 wrapper,避免 termimad 吞 HTML。

### 实现调整

- `ParallelTuiState::apply_event(reply.human.message)` 不再写 payload preview 到 chat_status,只写来源提示。

## 2026-03-05 23:10 +0800 | 并行 TUI: 无 stdout/reply 的新证据与可疑点

### 证据

- 录制文件: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`
- `target/release/ralph record summary`:
  - Topics: `human.message: 2`
  - Stdout Tail: `<empty>`
  - Termination: Interrupted

### 关键观察

- 录制里出现的 `<event topic="reply.human.message" ...>` 全部来自 prompt transcript(示例文本),且在 stderr(stdout=false).
- 本次 run 没有任何 stdout=true 的输出,因此 EventParser 不可能产出 `reply.human.message`.

### 可疑根因(代码)

- `crates/ralph-cli/src/codex_app_server_session.rs`:
  - job 主循环里对所有 response `{id,result}`/`{id,error}` 直接 `continue` 忽略.
  - 若 `turn/start` 收到 error response,会导致“静默失败 + 无输出卡死”.
  - 需要把 error response:
    1) 输出到 Output(最好是 stdout 也可见,避免 `--hide-stderr` 下完全空白)
    2) 并对关键方法 fail-fast 结束 job

## 2026-03-05 23:58 +0800 | 复现验证(无 TUI): idle_start + emit human.message

- 运行(工作区: `ralph-example`):
  - `target/release/ralph run --no-tui --idle-start --record-session /tmp/ralph-talk-repro.jsonl`
  - 然后: `target/release/ralph emit human.message "你是什么模型" --target-instance ralph#1`
- 结果:
  - stdout 收到 `<event topic="reply.human.message" ...>...`.
  - `record summary /tmp/ralph-talk-repro.jsonl`:
    - `human.message: 1`
    - `reply.human.message: 1`
- 说明:
  - 真实后端在该路径下能正常产出 reply。
  - 若后续再出现无 stdout,现在会把 `{id,error}` / timeout / cancel 等关键原因以 stdout 行回显,不再“空白静默”.

## 2026-03-06 11:57 +0800 | 复核笔记: 本轮只确认到文件上下文误插入,未确认到需要回退的代码越界改动

### 审核范围

- 代码:
  - `crates/ralph-tui/src/app.rs`
  - `crates/ralph-tui/src/state/parallel.rs`
  - `crates/ralph-tui/src/state/parallel/output.rs`
  - `crates/ralph-adapters/src/stream_handler.rs`
  - `crates/ralph-cli/src/codex_app_server_session.rs`
- 文件上下文:
  - `task_plan.md`
  - `notes.md`
  - `WORKLOG.md`
  - `ERRORFIX.md`
  - `LATER_PLANS.md`

### 结论

- 唯一确认的误改:
  - `LATER_PLANS.md` 把 `2026-03-06 10:42 +0800` 新条目插进了 `2026-02-24 13:30 +0800` 旧条目的建议块中间。
  - 这属于文件上下文维护错误,不是业务代码错误。
- 代码层暂未确认需要回退:
  - `app.rs` 的 `@target` 自动切换有对应测试 `maybe_select_chat_target_instance_switches_selected_instance`。
  - `stream_handler.rs` 的 multiline `<event` 展示层修复有对应测试 `markdown_rendered_mode_shows_reply_human_message_payload_with_multiline_opening_tag`。
  - `parallel.rs` / `output.rs` 的 reply 可见性修复有对应测试 `reply_remains_visible_when_previous_output_lines_wrap_heavily`。
  - `codex_app_server_session.rs` 的 error response 可见性有对应测试 `app_server_turn_start_error_is_visible_and_fails_fast`。

### 新提醒

- 以后追加 `LATER_PLANS.md` 时,不能只看 diff 末尾,必须再次确认“文件真实尾部”没有未结束的旧条目,否则很容易把新 section 插进上一条的列表里。

## 2026-03-06 16:12 +0800 | app-server 多 turn prompt 缩减方案

- 现状:
  - `HatInstanceActor` 每个 job 都会重新 `build_prompt()`。
  - `CodexAppServerRuntime` 每个 job 都会把 `job.prompt` 作为新的 `turn/start input` 发给同一 session/thread。
- 根因:
  - “job” 与 “session” 分离了,但 `HatJob` 只有一份完整 prompt,session 层无法区分“首 turn 输入”和“后续 turn 输入”。
- 设计结论:
  - 把“完整输入”和“continuation 输入”同时放进 `HatJob`。
  - 由 `CodexAppServerSession` 按 thread 生命周期判断首 turn 还是后续 turn。
  - continuation 输入只保留短提示 + `Incoming Events` 区域,不再重复 prelude / hat instructions。
## 2026-03-07 16:34 +0800 | Gemini headless backend 稳定正文输出修复笔记

### 现象

- 自动化或并行 hat 调用 Gemini 时, 上层有时会看到"无返回内容"。
- 结合 `.agents` 侧 smoke 结果可知, Gemini CLI 并不是真的没回答。
- 真正不稳定的是"上层如何采集最终正文"这一步。

### 关键证据

- `gemini` CLI 在 headless 场景下, 小提示词也可能需要 20-30+ 秒才返回最终正文。
- 原始执行链里, stderr 会混入审批/MCP/运行日志。
- 如果 PTY 把 stderr 和 stdout 混在一起, 上层就很难可靠提取最终正文。
- `--output-format json` 的 `response` 字段更适合自动化消费。

### 根因判断

- 旧的 Gemini backend 仍走 `OutputFormat::Text`。
- 执行器和并行 runner 消费的是原始文本流, 而不是结构化最终结果。
- 一旦 stdout/stderr 混流, 或 CLI 在最终正文前输出额外日志, 上层就可能误判为空或解析失败。

### 设计决策

- 只改 headless/自动化路径。
- 不改 Gemini interactive/TUI 路径, 避免扩大行为面。
- 让 `CliBackend::gemini()` 默认走 `--approval-mode yolo --output-format json -p`。
- 在 `CliExecutor` 与并行 runner 里统一提取最终 `response`。
- 当 JSON 不合法时, 回退到原始 stdout/stderr 组合, 确保排障证据不丢。

### 涉及文件

- `crates/ralph-adapters/src/cli_backend.rs`
- `crates/ralph-adapters/src/cli_executor.rs`
- `crates/ralph-cli/src/loop_runner.rs`
- `crates/ralph-cli/src/parallel_runner.rs`
- `specs/cli-adapters.spec.md`
- `specs/e2e-testing/research/ralph-backends.md`
- `specs/event-loop/research/per-hat-backends.md`

### 验证结论

- `cargo fmt --all` 通过。
- `cargo test -p ralph-adapters` 通过。
- `cargo test -p ralph-core smoke_runner` 通过。
- 面向本次改动的精确测试已通过:
  - `cli_backend::tests::test_gemini_finalize_headless_output_extracts_response`
  - `cli_backend::tests::test_gemini_finalize_headless_output_handles_mixed_logs`
  - `cli_backend::tests::test_gemini_finalize_headless_output_falls_back_when_json_invalid`
  - `cli_executor::tests::test_execute_json_backend_extracts_response_from_stdout`
  - `parallel_runner::guardrail_tests::parallel_output_for_event_parsing_is_stdout_only`
- `cargo test -p ralph-cli` 仍有 1 个与本次改动无关的既存失败:
  - `tests/integration_agents.rs::test_agents_command_watch_prints_output_at_least_once`
- 已额外做 shell/Python 最小复现, `ralph agents --watch` 在 pipe 场景下确实会输出 `Watching` 与实例行, 因此该失败更像测试时序/夹具问题, 已记录到 `LATER_PLANS.md`。

## 2026-03-08 00:26 +0800 | 研究: 无 `PROMPT.md` 模式下主 `ralph#1` 的 `MaxRuntime` 语义

### 现象

- 用户要求:
  - 主 `ralph#1` 在无 `PROMPT.md` 的模式下,不应受 `MaxRuntime` 限制。

### 静态证据

- `crates/ralph-cli/src/parallel_runner.rs`
  - 并行模式在缺失/空 `PROMPT.md` 时,会进入 `idle_start`。
  - 交互式 TUI 可自动进入该模式,headless 则需显式 `--idle-start`。
- `crates/ralph-core/src/parallel/supervisor.rs`
  - 旧实现里:
    - `idle_start` 期间先暂停 `max_runtime`
    - 一旦看到任意实例 `StateChanged -> Running`,就重新开始计时
- `specs/parallel-idle-start.spec.md`
  - 旧规格也写成了“直到首次 Running 才开始计时”。

### 候选假设

- 主假设:
  - 当前实现把无 prompt 会话当作“先暂停,后恢复计时”,这比用户要的语义更弱。
- 备选解释:
  - 用户遇到的是串行 `event_loop` 路径,而不是并行 `Supervisor`。

### 证伪备选解释

- `loop_runner::resolve_prompt_content()` 在串行路径仍要求 prompt 可解析,不会进入“无 PROMPT.md 常驻待机”语义。
- 因此这次需求落点明确在并行路径,不是串行 `event_loop`。

### 设计决策

- 把修改边界限定在并行 `Supervisor`:
  - `idle_start` 会话整体禁用 Supervisor 级 `MaxRuntime`
  - 不改串行 `event_loop`
  - 不移除其他护栏:
    - `job_timeout_secs`
    - `max_iterations`
    - 人工中断
- 同步更新规格:
  - `specs/parallel-idle-start.spec.md`
  - 从“首次 Running 后恢复计时”改为“整场会话不受 Supervisor 级 `MaxRuntime` 限制”

### 动态验证

- 新增更强回归:
  - `supervisor_idle_start_disables_max_runtime_even_after_first_running`
- 该测试验证:
  - idle_start 等待首条消息前不超时
  - 首条 `human.message` 触发 `ralph#1` 进入 Running 后
  - 再等待超过 `max_runtime_seconds`,仍不会以 `MaxRuntime` 退出

## 2026-03-08 00:58 +0800 | 研究: idle-start E2E 如何稳定证明“首条消息后也无 MaxRuntime”

### 现象

- 现有 fake/live idle-start 场景都能证明:
  - 启动后等待超过 `max_runtime_seconds` 也不退出
  - 第一条 `human.message` 能触发首次 job
- 但它们还不能充分证明:
  - 第一条消息已经触发 `Running` 之后
  - 再等待一段超过 `max_runtime_seconds` 的时间
  - 会话依然不会以 `MaxRuntime` 收尾

### 候选方案比较

- 方案A: 让第一次 turn 故意跑很久
  - 问题:
    - fake 可做, live 不稳
    - live 会变成“赌模型延迟”, 容易漂
- 方案B: 两次外部消息
  - 第一次只返回 `warmup ack`, 不输出 `LOOP_COMPLETE`
  - 然后等待超过 `max_runtime_seconds`
  - 第二次再输出最终答案 + `LOOP_COMPLETE`

### 结论

- 采用方案B。
- 原因:
  - 它验证的是“会话级 runtime 护栏是否被关闭”, 而不是“某次 turn 是否恰好很慢”
  - fake/live 都能共用这套证据形状
  - 如果旧 bug 还在, 会话会在两次消息之间被 `MaxRuntime` 收掉
  - 这样第二次消息就无法产出最终 answer + `LOOP_COMPLETE`

### 计划落点

- fake 场景:
  - fake codex shim 按 `phase: warmup` / `phase: finish` 分两轮返回
- live 场景:
  - 通过 `ralph_prompt` 约束真实 codex:
    - `warmup` 轮只回 ack
    - `finish` 轮才回最终答案与 `LOOP_COMPLETE`
- 两个场景都要补:
  - 两次 emit 的 human-log
  - “总时长至少覆盖两段超时级等待”的断言

## 2026-03-08 12:52 +0800 | 研究结论: `EventParser` 的三条语义必须统一

### 现象

- `parse()`、`promise_in_event_tags()`、`strip_event_tags()` 三处都只认 `"<event "`。
- 这会带来两类动态问题:
  - 多行 opening tag 的 event 完全不被解析
  - 未闭合 event 的 payload 会漏到 completion 检测里

### 主假设

- 根因不是某一个调用点漏判, 而是三处 helper 共享了同一种过窄假设:
  - 把 `<event` 后必须紧跟空格当成唯一合法 opening tag 形态
  - 遇到未闭合 event 时, 仍把尾部文本当成“外部正文”

### 备选解释

- 可能只是展示层 `termimad` 的吞字问题, 核心解析层其实没错。

### 证伪备选解释

- 展示层在 `crates/ralph-adapters/src/stream_handler.rs` 已经兼容:
  - `<event\n  topic="...">`
  - `<event\ttopic="...">`
- 因此这次问题明确属于核心解析层与展示层语义不一致, 不是展示层单点 bug。

### 设计决策

- 新增统一 helper:
  - 识别真正的 `<event ...>` / `<event>` 起点
  - 允许 `<event` 后跟 `>` 或任意 ASCII 空白
  - 排除 `<eventual>` / `<event-handler>` 这类误命中
- `parse()`、`promise_in_event_tags()`、`strip_event_tags()` 全部复用这套识别逻辑。
- 对未闭合 event:
  - completion 检测按“剩余内容仍在 event 内”处理
  - `strip_event_tags()` 直接丢弃未闭合 event 之后的尾部, 不再把它还原成正文

### 动态验证

- `cargo test -p ralph-core --lib event_parser::tests`
  - `29 passed`
- `cargo test`
  - 全仓通过
- `cargo test -p ralph-core smoke_runner`
  - 通过

## 2026-03-08 13:16 +0800 | 跟进建议: 为什么要在 `EventLoop::process_output()` 再补一条回归

### 现象

- `EventParser` 单测已经锁住了:
  - 多行 opening tag 可解析
  - event payload 内的 `LOOP_COMPLETE` 不再被当成 completion
- 但这仍然只覆盖“解析层”。
- 真正在线上驱动 termination 的入口是:
  - `EventLoop::process_output()`
  - 它会先做 completion 检测, 再解析事件, 再发布到总线

### 主假设

- 如果以后有人改动 `process_output()` 的调用顺序或接线方式, 只靠 `EventParser` 单测还不够。
- 更稳的回归形状应该同时锁住两件事:
  - 不会误触发 completion
  - 事件也不会因此丢失

### 设计决策

- 在 `crates/ralph-core/src/event_loop/tests.rs` 新增真实入口回归:
  - 由 `ralph` 输出一个多行 `<event ...>` block
  - payload 明确包含 `LOOP_COMPLETE`
  - scratchpad 预置为“所有任务已完成”, 确保一旦误判 completion 就会暴露
- 断言同时覆盖:
  - `process_output()` 返回 `None`
  - `completion_confirmations == 0`
  - `build.task` 仍被发布到 builder 的 pending queue

### 动态验证

- `cargo test -p ralph-core --lib event_loop::tests::test_process_output_multiline_event_payload_promise_does_not_complete_and_still_routes_event -- --exact`
  - 通过
- `cargo test -p ralph-core --lib event_loop::tests`
  - `41 passed`
- `cargo test`
  - 通过
- `cargo test -p ralph-core smoke_runner`
  - 通过

## [2026-03-09 09:16:00] 笔记: example 覆盖型 E2E 全量执行结果

### 来源

- 命令: `cargo run -p ralph-e2e -- codex --filter example --keep-workspace --verbose`
- 正式报告: `.e2e-tests/report.md`, `.e2e-tests/report.json`
- 现场证据:
  - `.e2e-tests/parallel-trigger-routing-example/.ralph/events.jsonl`
  - `.e2e-tests/parallel-trigger-routing-example/.ralph/agents.json`
  - `.e2e-tests/parallel-experimental-dev-engine-example/.ralph/events.jsonl`
  - `.e2e-tests/parallel-experimental-dev-engine-example/.ralph/agents.json`

### 现象

- 全量 example 覆盖型 E2E 共跑了 2 个场景,总耗时 `572.2s`,进程退出码 `1`。
- `parallel-trigger-routing-example` 失败,正式断言显示:
  - `spec_logger` 聚合 job 次数应为 `3`,实际为 `7`
  - `LOOP_COMPLETE` 之后仍出现新的 job: `ralph#1=4`, `spec_reviewer#1=2`
- `parallel-experimental-dev-engine-example` 失败,但完整 topic 链已经出现:
  - `experiment.task -> experiment.result -> experiment.reviewed -> integration.task -> integration.applied -> experiment.complete`
  - 仍然出现了 `18` 次 `routing.escalate`
- 两个场景的 `events.jsonl` 中都看到了很多异常的占位/示例型事件:
  - `build.task` payload 为 `...`
  - `reply.human.message` payload 为 `我已收到,这里是我的回复...`
  - `human.message` payload 为 `...`
  - escalation 文本多次指向 `invalid target: hat "writer" is not subscribed to topic "build.task"`

### 假设

- 候选假设1: example 场景的 agent prompt 中混入了教程/示例文本,模型把示例 `<event>` 或示例 topic 当成真实运行时输出,从而制造额外 job 与 `routing.escalate`。
- 候选假设2: `LOOP_COMPLETE` 后仍有迟到事件或控制面补偿逻辑继续触发,导致 example 断言里的“收敛后无新 job”被打破。
- 候选假设3: `parallel-experimental-dev-engine-example` 的集成工作区没有完全隔离到 example workspace,导致 experiment integrator 在主仓库执行了 `git cherry-pick` 并推进了真实 HEAD。

### 已验证结论

- `parallel-trigger-routing-example` 的正式失败原因,以 `.e2e-tests/report.md` 为准:
  - `Hat job run counts (example)` 失败
  - `No new jobs after LOOP_COMPLETE (example)` 失败
- `parallel-experimental-dev-engine-example` 的正式失败原因,以 `.e2e-tests/report.md` 为准:
  - `No unexpected gate/routing escalation (example)` 失败
- 第二个场景不是“链路没跑通”,而是“链路跑通了但夹杂大量不该出现的 `routing.escalate`”。这是由 report 中 `Required topic chain observed (example)=✅` 与 `No unexpected gate/routing escalation (example)=❌` 同时成立确认的。
- 本轮 E2E 对主仓库产生了真实副作用:
  - 当前 `git log --oneline -n 1` 为 `2c4dd37 exp-002: e2e marker file`
  - 根目录存在 `e2e_marker.txt`,内容为 `exp-002`
  - 我没有回退这次副作用提交

### 后续排查入口

- 先看 `parallel-trigger-routing-example` 与 `parallel-experimental-dev-engine-example` 的 prompt / scenario 构造,确认为什么运行时会持续出现 `build.task -> writer` 这条不属于当前拓扑的错误路径。
- 再看 example 集成阶段的 workspace 选择逻辑,确认为什么 integrator 证据里写的是主仓库路径 `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-orchestrator`。

## 2026-03-09 11:36 +0800 | 笔记: idle `ralph#1` 收到内部事件后如何再起 job

### 现象

- 用户关心的是:
  - 当 idle 的 `ralph#1` 收到一条内部事件,例如 `experiment.reviewed` 且 `source_instance=experiment_auditor#1`,
    是哪条代码路径把它重新调度成下一次 job。
- 这个问题要拆成两段看:
  - supervisor 如何把事件投到 `ralph#1`
  - instance 如何把一次 `Deliver` 变成一次新的 job

### 静态证据

- 事件来源:
  - `HatInstanceActor::on_job_completed()` 会解析 `output_for_parsing`,并把每条 event 补上 `source/source_instance/id`
  - 位置:
    - `crates/ralph-core/src/parallel/instance.rs:1494`
    - `crates/ralph-core/src/parallel/instance.rs:1597`
- supervisor 收到实例完成事件后,会把解析出的 events 继续顺序路由:
  - `crates/ralph-core/src/parallel/supervisor.rs:449-458`
  - `crates/ralph-core/src/parallel/supervisor.rs:1065-1080`
- 路由分叉:
  - 命中 `TopicContract`:
    - `route_event -> deliver_fanout / deliver_queue / deliver_to_instance_id`
    - 关键位置:
      - `crates/ralph-core/src/parallel/supervisor/routing.rs:293-325`
      - `crates/ralph-core/src/parallel/supervisor/routing.rs:358-419`
      - `crates/ralph-core/src/parallel/supervisor/routing.rs:883-976`
  - 未命中 `TopicContract`:
    - `route_event -> route_event_via_triggers`
    - `route_event_via_triggers -> choose_ralph_instance_for_delivery`
    - 关键位置:
      - `crates/ralph-core/src/parallel/supervisor/routing.rs:331-333`
      - `crates/ralph-core/src/parallel/supervisor/routing.rs:424-509`
      - `crates/ralph-core/src/parallel/supervisor/routing.rs:1379-1408`
- 对内部 hat 事件,`may_redirect_to_secondary_ralph()` 返回 false:
  - 因为它要求 `event.source.is_none() && event.source_instance.is_none()`
  - 所以带来源的 `experiment.reviewed` 不会改投 `ralph#2`
  - 位置:
    - `crates/ralph-core/src/parallel/supervisor/routing.rs:1351-1377`
- 真正的“再起 job”不在 supervisor,而在 instance actor:
  - `HatInstanceCommand::Deliver(mut event)` 分支里
  - 先 `self.pending.push(*event)`
  - 再 `self.maybe_start_job().await?`
  - 位置:
    - `crates/ralph-core/src/parallel/instance.rs:331-385`
- `maybe_start_job()` 的早退门槛:
  - `shutdown_requested`
  - `running.is_some()`
  - `pending.is_empty()`
  - `pending_permission_gate.is_some()`
  - `job_semaphore.try_acquire_owned()` 失败
  - 位置:
    - `crates/ralph-core/src/parallel/instance.rs:607-725`
- `record_agents_last_input()` 只是观测快照,不负责起 job
  - 位置:
    - `crates/ralph-core/src/parallel/supervisor.rs:1105-1132`

### 动态证据

- 已运行并通过:
  - `cargo test -p ralph-core --lib parallel::supervisor::routing_tests::busy_ralph_internal_hat_event_keeps_primary_queue_and_does_not_spawn_secondary -- --exact`
  - `cargo test -p ralph-core --lib parallel::supervisor::routing_tests::idle_ralph_prefers_primary_instance -- --exact`
  - `cargo test -p ralph-core --lib parallel::supervisor::routing_tests::supervisor_pause_on_completion_promise_continues_consuming_external_events_in_tui_mode -- --exact`
- 这些测试分别支持:
  - 内部 hat 事件不会改投 `ralph#2`
  - idle 时优先还是 `ralph#1`
  - 路由到 `ralph#1` 后,实例侧会再次拉起下一次 job

### 结论

- 如果 `experiment.reviewed` 这类事件没有被配置的 `TopicContract` 覆盖,则默认链路是:
  - 上游实例 `on_job_completed`
  - supervisor `route_events_batch`
  - `route_event`
  - `route_event_via_triggers`
  - `choose_ralph_instance_for_delivery(false)` 选回 `ralph#1`
  - `deliver_fanout`
  - `HatInstanceCommand::Deliver`
  - `pending.push`
  - `maybe_start_job`
- 如果有匹配的 `TopicContract`,则中间会改成:
  - `route_event`
  - `deliver_fanout` 或 `deliver_queue`
  - `deliver_to_instance_id`
  - 后半段仍然是 `HatInstanceCommand::Deliver -> pending.push -> maybe_start_job`
- 因此:
  - `last_input` 更新只能证明 supervisor 选中了某个实例并准备投递
  - 不能证明新的 job 已经真正启动
