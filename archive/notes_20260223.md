# 笔记续档

- 旧文件超过1000行,已续档为 `notes_2026-02-11_204839.md`.

## 2026-02-11 21:12 +0800 | 并行 E2E 复验诊断结论

### 现场结论

- 命令: `bash scripts/run-parallel-hat-instances-codex.sh`
- 结果: `parallel-hat-instances` 与 `parallel-hat-instances-zh` 仍失败(各约 120s).
- 共性失败:
  - `routing.escalate` 缺失
  - `completion_seen=false`(未观察到 `LOOP_COMPLETE`)
  - 计数断言波动(不同 run 的 `writer#1/#2`、`collector#1/#2` 分配不同)

### 关键发现

- 该脚本此前只执行 `cargo build`(dev),但 harness 使用 `target/release/ralph` 运行。
- 这会导致“验证目标二进制”与“最新代码”可能不一致。
- 另外脚本原先只清 `events.jsonl/current-events`,未清整目录,存在 workspace 复用污染风险。

### 已落地修复

- `scripts/run-parallel-hat-instances-codex.sh`
  - 预构建改为 `cargo build --release -p ralph-cli --bin ralph`.
  - 运行前改为删除整个 `.e2e-tests/parallel-hat-instances` workspace 再重建,提升确定性.

### 仍待跟进

- 场景本身在真实 Codex 下存在行为漂移(尤其 `routing.escalate` 与 completion 触发),需要单独调整 scenario 约束或断言稳定性策略.

## 2026-02-11 22:48 +0800 | parallel-hat-instances 稳定性修复(通过)

### 根因归纳

- 原场景依赖模型行为较强:
  - `writer` 长输出 + `tester` 二次派发 + `collector` 条件分支,在真实 Codex 下容易发生时序漂移。
- 漂移后果:
  - 第二任务不稳定落到 `writer#2`.
  - `collector` 触发 `routing.escalate` 条件经常错过。
  - 最终 `LOOP_COMPLETE` 缺失并触发超时。

### 修复策略

- 把“可能漂移的实例选择”改为“确定性实例直达”:
  - `tester` 第二任务固定 `target_instance=writer#2`.
- 把“手工发 completion candidate”改为“运行时严格校验自动生成”:
  - `collector` 仅发 `build.task(target=ghost_hat)`,由 Supervisor 产出 `routing.escalate`.
- 断言从“固定实例精确计数”改为“语义稳定断言”:
  - 按 hat 汇总次数 + `writer#2` 出现校验.
- 适度提高场景 runtime 护栏:
  - `max_runtime_seconds: 180`.

### 验证结果

- `bash scripts/run-parallel-hat-instances-codex.sh`:
  - `parallel-hat-instances` ✅ (105.4s)
  - `parallel-hat-instances-zh` ✅ (121.6s)
- `cargo test -p ralph-e2e` 全通过。

## 2026-02-11 23:18 +0800 | all_hat 从运行时读取改为编译期内嵌

### 需求理解

- 用户要求 `config/all_hat.md` 作为编译时配置项,并在编译时注入到所有 hat prompt(包含 `ralph`).
- 语义重点: 运行时不再依赖 workspace 下的 `config/all_hat.md` 文件.

### 代码现状核查

- 注入链路覆盖已存在:
  - 串行: `EventLoop`.
  - 并行: `ParallelSupervisor` -> `HatInstanceActor` + dispatch decider.
- 但配置源是运行时读取:
  - `prompt_overlay::load_all_hat_prompt(&workspace_root)`.

### 改造结论

- `crates/ralph-core/src/prompt_overlay.rs`
  - 使用 `include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/../../config/all_hat.md"))` 编译期内嵌.
  - `load_all_hat_prompt()` 改为无参,只返回内嵌内容(去首尾空白后).
- 调用点统一切换到无参加载:
  - `crates/ralph-core/src/event_loop/mod.rs`
  - `crates/ralph-core/src/parallel/supervisor.rs`
- 测试同步:
  - 单元测试新增 `load_all_hat_prompt_reads_compiled_overlay`.
  - `routing_tests` 从“临时写 workspace config”改为“断言内嵌内容锚点”.
  - `event_loop/tests.rs` 新增 ralph + custom hat 双路径注入回归测试.
  - 集成测试 `event_loop_ralph` 更新为编译期断言并重命名:
    - `test_ralph_prompt_includes_all_hat_overlay_from_compiled_config`.

### 验证结果

- `cargo fmt && cargo fmt --check` ✅
- `cargo test -p ralph-core --test event_loop_ralph test_ralph_prompt_includes_all_hat_overlay_from_compiled_config` ✅
- `cargo test` ✅

### 风险与影响

- 影响: 修改 `config/all_hat.md` 后需要重新编译才能生效.
- 收益: prompt 注入语义稳定,不受运行目录/文件缺失影响.

## 2026-02-12 23:55 +0800 | 现状核对: parallel 下 ralph(MCP) vs hat(exec)

### 关键代码位置

- `crates/ralph-cli/src/parallel_runner.rs`
  - `CliHatJobExecutor::should_use_codex_mcp()` 当前硬编码只对 `ralph#1/ralph#2` 生效.
  - 其他 hat 一律走 `codex exec --full-auto` 一次性 invocation.
- `crates/ralph-cli/src/codex_mcp_session.rs`
  - `CodexMcpRuntime` 已经支持 "按 instance_id" 维护 MCP session.
  - `sessions: HashMap<String, Arc<Mutex<CodexMcpSession>>>` 的 key 就是 instance_id 字符串.
  - 也就是说,技术上不止 ralph,writer#1 等 hat instance 也能复用同一 thread.

### 结论(对后续设计的约束)

- "Hat 是否 persistent" 目前是 executor 层策略,不影响 ralph-core 的调度/路由语义.
- 因此如果要做混合模式(C),最小改动点就是:
  - 让 executor 根据配置决定哪些 instance_id/hat_id 走 CodexMcpRuntime.

## 2026-02-13 01:09 +0800 | 已落地: 动态会话策略(session_strategy) + sticky(只升级)

### 代码要点

- `crates/ralph-proto/src/event.rs`
  - 新增 `SessionStrategy`.
  - `Event` 新增 `session_strategy: Option<SessionStrategy>`.
- `crates/ralph-core/src/event_parser.rs`
  - 解析 `<event ... session_strategy="exec|mcp">`.
- `crates/ralph-core/src/parallel/instance.rs`
  - 合并规则: pending events 任意请求 mcp -> job.mcp.
  - sticky 规则: instance 一旦进入 mcp -> 永久保持 mcp.
- `crates/ralph-cli/src/parallel_runner.rs`
  - ralph#1/#2 固定走 MCP.
  - 其他 hat 按 job.session_strategy 决定是否走 MCP.

### 文档同步

- `specs/parallel-hat-instances.spec.md`: 增加 session override 说明与示例.
- `config/all_hat.md`: 增加 session_strategy 使用说明(给 ralph/hats).
- `docs/plans/2026-02-13-parallel-session-strategy-design.md`: 本次设计文档.

### 验证

- `cargo fmt` ✅
- `cargo test` ✅
- `cargo test -p ralph-core smoke_runner` ✅

---

## 2026-02-14 08:47 +0800 | Codex App Server: turn/steer 关键语义摘录(用于 Ralph 设计)

### 结论先行

- Codex App Server 原生支持 `turn/steer` 与 `turn/interrupt`.
- 这意味着它具备 "同一 in-flight turn 内追加输入" 的协议能力.
- 相比之下,`codex mcp-server` 更像是 "下一轮继续",不具备 turn 内追加语义.

### 协议形态(实现侧会踩坑的点)

- 传输: JSONL over stdio 是默认(也可 ws).
- 协议: JSON-RPC 2.0,但报文里不带 `"jsonrpc":"2.0"` 字段.
- 生命周期: `initialize` -> `initialized` -> `thread/start` -> `turn/start` -> (可选) `turn/steer` / `turn/interrupt`.

### `turn/steer` 的硬约束(决定我们怎么接入)

- 必须带 `expectedTurnId`.
  - 这要求 executor 在 `turn/start` 后缓存 "当前活跃 turnId".
- `turn/steer` 会在同一 turn 内追加输入,不会创建新的 turn.
- 如果 thread 没有 in-flight turn,`turn/steer` 会失败.
- `turn/steer` 不支持 turn overrides(例如 model/cwd/sandboxPolicy 等).

### 对 Ralph 的直接映射建议

- `Event.session_strategy` 扩展为: `exec|mcp|app_server`.
- 继续沿用 "显式信号 + sticky(只升级)".
  - 这样 replay 才能复现真实选择.
- 增加 turn 级动作字段(建议叫 `turn_action` 或类似概念):
  - `start`(默认): 新开 turn.
  - `steer`: 对 in-flight turn 做 `turn/steer`.
  - `interrupt`: 对 in-flight turn 做 `turn/interrupt`(或等价 cancel).

### 参考

- 官方协议文档: `https://developers.openai.com/codex/app-server`

---

## 2026-02-14 08:55 +0800 | Event 需要 "可回复" 的最小协议: id(nanoid) + reply

### 用户新增需求

- 每个 event 需要一个可引用的 nanoid(或等价的稳定 id).
- event 需要新增属性 `reply=""`,用于表达 "我在回复哪个 event".
- 目标是让其他 hat 在回复某条 event 时,可以把该 event 的 nanoid 带入 reply,实现协作链路关联.

### 现状核对(代码事实)

- `ralph-proto::Event` 已经有 `id: Option<String>`,注释是 "Optional stable identifier for this event.".
- 并行模式下,实例侧会在 event 没有 id 时补齐:
  - 当前实现是 `"{instance_id}:{seq}"`(稳定可回放).
- Supervisor 自己生成的 event 也会补齐 id:
  - 当前实现是 `"supervisor:{seq}"`.
- 但是,并行模式下 hat 的 prompt 里 "Incoming Events" 目前只注入了 `topic + payload`,没有注入 `id`.
  - 这会导致: hat 看不到 event.id,因此无法在输出里写 `reply="<id>"`.

### 设计要点(最小改动,改良胜过新增)

- 优先复用现有字段 `Event.id` 作为 "nanoid/事件主键",避免再新增 `Event.nanoid` 造成双主键.
- 新增 `Event.reply: Option<String>`(或 `reply_to`),用于存储被回复的事件 id.
  - `<event ... reply=\"<event_id>\">...</event>` 解析进该字段.
- Prompt 必须把 incoming events 的 `id` 暴露给 hat.
  - 这样 writer/reviewer 才能把它原样带到 reply 字段里.

### 未决项(需要你确认口径)

1. "nanoid" 是不是就是 `Event.id` 的生成格式? 还是你坚持新字段名必须叫 `nanoid`?
2. `reply` 是单值(只回复一个 event)还是允许多值(例如逗号分隔或 JSON 数组)?

### 已确认口径(2026-02-14)

- 每条 event 都必须有可引用 id.
  - 这里的 id 复用 `Event.id`(不新增 `nanoid` 字段).
  - 允许保持现有稳定生成格式(例如 `{instance_id}:{seq}`),只要保证 "总会有".
- `reply` 是单值.
  - 一条 event 最多回复一个 event.id.
  - 多父依赖需要拆成多条回复事件,或选择一个主回复对象(由 ralph 协调约定).

---

## 2026-02-15 10:55 +0800 | parallel TUI: `--record-session` 录制口径核对

### 结论

- 并行 runtime 不再忽略 `--record-session`,会生成可回放的 JSONL cassette.
- parallel + TUI 时,`_meta.loop_start.ux_mode` 会写入 `"parallel-tui"`;不开 TUI 则是 `"parallel-cli"`.
- 录制内容以 "回放/解析" 为目标:
  - `bus.publish`: 每条业务事件一条.
  - `ux.terminal.write`: stdout-only,并带 `instance_id`(例如 `writer#1`)用于分流回放.
- 不录制 TUI 逐帧画面.
  - spec 明确写了 non-goal: 不要求录制 `ux.tui.frame`.

### 关键代码位置

- `specs/parallel-record-session.spec.md`: 目标/非目标/验收口径.
- `crates/ralph-cli/src/parallel_runner.rs`: 并行模式下 record-session wiring,以及 stdout-only + `instance_id` 归因.
- `crates/ralph-cli/src/loop_runner.rs`: 串行模式下 stdout-only 迭代输出写入 cassette.
- `crates/ralph-core/src/session_recorder.rs`: Record JSONL 格式,以及 `ux.terminal.write`/`bus.publish` 的序列化结构.

### 易踩坑

- 如果你用 `| tee out.txt` 这类管道,stdout 不是 TTY,并行会自动降级为 log 模式(看起来像 "TUI 没启动").
- cassette 默认只录 stdout:
  - 后端把主要输出打到 stderr 时,会看起来 "录不到内容".
  - 这是为了避免 stderr 里的 `<event ...>` 示例块造成假事件污染(并行语义要求).

---

## 2026-02-15 12:26 +0800 | `ux.terminal.write`: 增加 `text` 字段,便于诊断阅读

- 背景: 目前 cassette 里 `data.bytes` 是 base64,肉眼排障需要额外解码,非常不便.
- 方案: 保留 `bytes`(base64,回放/保真) + 新增 `text`(UTF-8 lossy,诊断可读).
- 关键约束: 回放与事件解析永远以 `bytes` 为准,`text` 只用于人类阅读 JSONL.
- 兼容性: `text` 是 `Option<String>`,旧 cassette 不带该字段也能正常解析.

---

## 2026-02-15 14:01 +0800 | 并行: stderr 可观测/可录制,但事件解析保持 stdout-only

### 现象

- 你给的 `parallel-experimental-dev-engine.jsonl` 只有 `stdout=true` 的 `ux.terminal.write`.
- 并行模式下 `ralph#1` 默认走 codex app-server.
  - 旧实现会后台消费 app-server stderr,但只打 tracing warn,不进入并行输出流.
  - 结果就是: "灰色 stderr 思考/诊断" 看不到,录制 cassette 也缺证据.

### 修复要点

- app-server stderr:
  - 转成 `HatJobOutputChunk{stream=Stderr}` 流式输出,交给并行 Supervisor/TUI 统一展示(灰色).
  - 仍保持"事件解析 stdout-only": stderr 永远不进入 `HatJobResult.output`.
- cassette:
  - parallel `--record-session` 录制 stdout+stderr,用 `ux.terminal.write.data.stdout` 区分.
  - smoke tests/ReplayBackend 过滤 stdout=false,避免 stderr 里的 `<event ...>` 假事件污染解析.
- `--hide-stderr`:
  - 只影响显示,不影响 cassette 录制(更利于排障回放).

---

## 2026-02-16 17:32 +0800 | follow-up: 自检实验补齐 stderr + `.ralph/current-events` 注入文档化

### 关键发现

- example 的自检实验如果只打印 stdout,很难用“肉眼 + cassette(JSONL)”确认 stderr 链路是否真的贯通。
  - 解决办法是让自检实验主动打印少量 stderr,从而稳定触发 `ux.terminal.write(stdout=false)`.
- 这里还有一个很隐蔽但高频的坑:
  - 在 Markdown 的 list item 里写 here-doc 示例时,如果把 `PY` 终止符缩进了,用户复制执行会直接卡住或报错。
  - 因此示例里必须保证 here-doc 内容与终止符 `PY` 都是行首对齐(不能带空格缩进)。

### 本次落地(只做最小补齐)

- `examples/parallel-experimental-dev-engine/PROMPT.md`
  - exp-par-001/002: stdout+stderr 同时输出,并修正 here-doc 缩进.
- `examples/parallel-experimental-dev-engine/README.md`
  - 明确 `--no-tui` 不适合持续对话(完成后退出).
  - 解释 `.ralph/current-events` marker 的作用,并给出手工 JSONL 注入 steer/interrupt 的示例.
- `crates/ralph-cli/src/codex_app_server_session.rs`
  - 保留 stderr 的兜底日志,但把默认等级降为 debug,避免 warn 刷屏(主要观测面在 parallel 输出流).

### 外部目录同步注意点

- 外部目录 `/Users/cuiluming/local_doc/l_dev/my/rust/parallel-experimental-dev-engine/` 的 `PROMPT.md` 可能是用户正在使用的真实任务内容。
  - 为避免覆盖造成信息丢失,同步时先做了备份: `PROMPT_backup_2026-02-16_1731.md`。

---

## 2026-02-16 18:24 +0800 | `ralph emit` 参数化写入 session_strategy/turn_action/workspace_strategy

### 背景问题

- 并行模式下,headless 场景(例如 `--no-tui`)想做 steer/interrupt 时,
  需要在外部事件 JSONL 里写入 `session_strategy` 与 `turn_action`。
- 之前 `ralph emit` 只支持 `topic/payload/ts/target_instance`,
  导致用户不得不手工追加 JSONL,容易:
  - 写错字段名(例如大小写/下划线).
  - 写错文件(不小心写到旧 run 的 events.jsonl).

### 关键约束(对齐点)

- 外部事件 JSONL 的 schema 由 `ralph_core::event_reader::Event` 定义:
  - 字段名必须是:
    - `workspace_strategy`
    - `session_strategy`
    - `turn_action`
  - 取值必须是 snake_case(尤其是 `app_server` 这种带下划线的值)。
- 为了减少人为错误,CLI 层用 clap 的 `ValueEnum` 做值域约束:
  - `#[value(rename_all = "snake_case")]` 让 `AppServer` 对应 `app_server`。

### 验证方式(最小 backpressure)

- 用集成测试保证闭环:
  - `ralph emit` 写入一行 JSONL
  - 直接用 `serde_json::from_str::<ralph_core::Event>()` 解析
  - 断言可选字段的值严格一致

---

## 2026-02-16 18:24 +0800 | 文档同步: `config/all_hat.md` 补齐 app_server 语义

### 背景

- `config/all_hat.md` 之前只描述了 `session_strategy="mcp"` 的 sticky 规则.
- 当你已经引入 `session_strategy="app_server"`(Codex App Server)后,
  文档如果不更新,很容易造成误解:
  - 以为系统仍然只有 mcp.
  - 或以为 "mcp 的 sticky" 表示运行时一定在 mcp.

### 本次处理

- 直接更新 `config/all_hat.md` 的会话策略段落:
  - 增加 `session_strategy="app_server"` 的说明.
  - 明确强弱排序: `exec < mcp < app_server`.
  - 明确不要从强切回弱(避免上下文分裂).
  - 额外提醒: `mcp` 与 `app_server` 是两套常驻实现,升级也可能丢上下文,因此建议一开始就选定策略.

## 2026-02-16 19:46 +0800 | 文档抽取: all_hat 基本定义上提 + parallel example ralph_prompt 精简

### 需求动机

- 用户反馈: `examples/parallel-experimental-dev-engine/ralph.yml` 的描述太具体,不够鲁棒.
- 用户希望:
  - 用更原则性的方式描述"ralph 与其他 hat 的基本定义".
  - 明确在本项目里,`ralph` 与 hat 都是 agent.
  - 强调 agent 通过 event 通信,并可按需创建更多子智能体协作(逻辑模型不受人数限制).

### 本次落地策略(分层)

- 把"稳定的项目级定义/原则"上提到 `config/all_hat.md`:
  - 该文件会注入所有 hat prompt,且当前实现为编译期内嵌.
  - 因此它适合承载跨 example 复用的本体定义,不适合承载 example 专属 topic.
- 把 `examples/parallel-experimental-dev-engine/ralph.yml` 的 `event_loop.ralph_prompt` 精简为:
  - 仅保留该 example 的专属协议(实验派发,windowed dispatch,强 backpressure,集成与收敛).
  - 避免写死 PROMPT.md 的字段结构,改为 best-effort 解析.

### 关键约束(避免回归)

- `config/all_hat.md` 仍必须包含锚点字符串 `文件上下文位置特殊情况转移`.
  - 原因: `crates/ralph-core/src/prompt_overlay.rs` 的单测会断言 overlay 包含该字符串.
- all_hat 的"无限"表述必须说明清楚:
  - 逻辑模型上不受人数/工时约束.
  - 运行时仍受并行与时间护栏约束(例如 `instances`, `max_running_jobs`, `job_timeout_secs`, `max_runtime_seconds`).

### 验证

- `cargo test -p ralph-core prompt_overlay` ✅
- `cargo test -p ralph-core --test event_loop_ralph test_ralph_prompt_includes_all_hat_overlay_from_compiled_config` ✅

## 2026-02-16 20:08 +0800 | 修正: orphan event 是给 Ralph 的一种消息方式

### 纠正点

- 之前在 `config/all_hat.md` 写了"event 必须有明确接收者(有人订阅 topic)".
- 这不符合项目语义:
  - event 可以有意发布为"无订阅者 topic".
  - 这类 event 会被当作 orphan event,由 Ralph 处理(并行模式下通常是 `ralph#1`).
  - 因此它也是一种"给 Ralph 发信息"的方式.

### 同步调整

- `config/all_hat.md`: 通信模型段落改为:
  - event 不一定需要明确接收者.
  - 无订阅者 topic 作为 orphan event 交给 Ralph.
  - 同时补一句提醒: 若因拼写错误导致 orphan,通常是 bug.
- `examples/parallel-experimental-dev-engine/ralph.yml`: 把"不要发布无人接收 topic"改成更精确的约束:
  - 不要发布"没有订阅者且也不是写给 Ralph 的协调消息"的 topic.
  - 明确 `experiment.start` 属于这种不该发的 topic.

### 验证

- `cargo test -p ralph-core prompt_overlay` ✅

## 2026-02-16 22:16 +0800 | 实现笔记: autopilot record-session JSONL verdict

### 关键实现点

- 新增 `ralph autopilot run/analyze`:
  - `run` 用子进程运行真实 `ralph run --no-tui --record-session ...`.
  - `analyze` 只读 JSONL,生成 report 与稳定退出码.
- Git 语义:
  - `--repo-dir` 只要在 Git 仓库内即可.
  - 实际运行会归一化到 `git rev-parse --show-toplevel` 的 repo root,避免 `.ralph/*` 相对路径漂移.
- 主证据源:
  - 硬断言只依赖 record-session JSONL(结构化 `bus.publish` + `_meta.termination` + `ux.terminal.write`).
  - stdout/stderr 仅辅助落盘,不参与硬判定.

### 硬断言合同(默认集合)

- 必须出现 topic:
  - `experiment.task`
  - `experiment.result`(payload 必须含 `commit`)
  - `experiment.reviewed`(payload 必须含 `evidence_ok=true`,允许 JSON/YAML)
  - `integration.task`
  - `integration.applied`
  - `experiment.complete`
- 禁止出现 topic:
  - `gate.request`,`gate.resolve`,`gate.timeout`,`routing.escalate`
- 终止原因:
  - `_meta.termination.reason` 必须为 `CompletionPromise`

### agent 分析(可选)

- 先从 JSONL 抽取有预算的证据包,写入 `analysis_input.json`.
- 再以子进程 `ralph run --no-tui` 运行最小分析配置:
  - analyzer hat 输出 `<event topic="analyze.complete">{...json...}</event>`
  - 并输出 `ANALYSIS_COMPLETE` 结束.
- 解析 analyze.complete 的 JSON,写入 `analysis_output.json`.

### 退出码语义(稳定)

- 0: hard PASS 且 agent PASS(或 skip)
- 1: hard FAIL(含 JSONL 不可解析)
- 2: hard PASS 但 agent FAIL 或 quality_score=suboptimal
- 3: hard PASS 但 agent 分析运行/解析失败

### 报告结构

- `report.json`:
  - `exit_code` 为数值(0/1/2/3),并提供 `exit_code_semantic`.
  - `hard_verdict.assertions[*]` 固化为 `passed/expected/actual/evidence_refs`.
- `report.md`:
  - 以"失败时先看哪些证据"为中心,列出 out-dir 下的关键文件路径.

## 2026-02-18 10:21 +0800 | 修复笔记: autopilot agent analysis 在 custom backend 下默认失败

### 现象

- 执行 `ralph autopilot analyze` 时:
  - hard verdict PASS.
  - 进入 agent analysis 后失败,report.json 里出现:
    - "Custom backend requires a command - set 'cli.command' in config".

### 根因

- `run_agent_analysis()` 只从主配置读取 `cli.backend` 字符串.
- 当 backend=custom 时,生成的 `analysis_ralph.yml` 没有携带 `cli.command/cli.args`.
- 子进程 `ralph run --config analysis_ralph.yml` 在 config validate 阶段直接退出.

### 修复点

- agent analysis 的 analysis config 生成逻辑:
  - 默认继承主配置的完整 `cli`(custom 时包含 command/args/prompt_mode/prompt_flag).
  - `--analysis-backend` 仅覆盖 backend 字段.
- 为顺序模式(由 ralph 执行)注入严格 `event_loop.ralph_prompt`,强制输出 `analyze.complete` JSON.
- 子进程 exit code=2(护栏退出)但 stdout 已产出可解析 JSON 时,允许继续判定为成功.

### 结果验证

- out_dir: `/tmp/ralph-autopilot-out-20260218-0917`
- `report.json`: exit_code=0,hard_verdict PASS,agent verdict PASS(quality_score=optimal).

## 2026-02-18 10:21 +0800 | 四文件摘要(continuous-learning)

- 任务目标(task_plan.md): 跑通 `$parallel-engine-autopilot` 的"运行+录制+判定+报告"闭环,并输出稳定 verdict.
- 关键决定(task_plan.md): 用临时 git worktree 跑真实 workflow,产物落盘到 /tmp,避免污染主工作区.
- 关键发现(notes.md): agent analysis 在 custom backend 下默认失败,根因是 analysis_ralph.yml 丢失 cli.command/cli.args; 另外子进程可能以 exit code=2 护栏退出.
- 实际变更(WORKLOG.md): 修复 autopilot 的 agent analysis 默认路径(继承完整 cli + 严格 ralph_prompt + exit code=2 仍解析 stdout).
- 错误与根因(ERRORFIX.md): custom backend 缺少 cli.command 导致 config validate 失败.
- 可复用点候选:
  - 派生配置文件时,backend=custom 必须继承 command/args,否则 validate 会在子进程阶段炸.
  - 自动化判定以"结构化产物"为准,子进程 exit code=2 可作为 warning 处理(只要核心 JSON 已产出).
- 是否需要固化到 docs/specs: 否(已通过单测锁死 + ERRORFIX/WORKLOG 记录).
- 是否提取/更新 skill: 否(原因: 该坑已在代码里消除; 后续只需按 LATER_PLANS 考虑 stdout/stderr 落盘增强排障).

---

# 笔记: parallel runner 未并发的根因与改良方向(2026-02-18 12:12 +0800)

## 现象

- autopilot 本次 run 里,只有 `experiment_runner#1` 进入过 `running`.
  - 证据: `/tmp/ralph-autopilot-out-20260218-112859/stdout.txt` 里仅出现一次 `experiment_runner#1:state running`.

## 根因

- 本次运行的 top-level prompt(被 `event_loop.prompt_file` 指向的文件)只包含 1 个可执行实验条目(exp-001).
- `ralph#1` 在 `task.start` 时只派发了 1 条 `experiment.task`,因此 Supervisor 只会分配 1 个 runner job.

## 关键发现

- 仓库里其实已经有"并行自检"的 prompt 模板:
  - `examples/parallel-experimental-dev-engine/PROMPT.md` 内置了 `exp-par-001`/`exp-par-002` 两条并行 smoke,并包含 stdout/stderr 交错输出脚本.
- 但当前 example 配置 `examples/parallel-experimental-dev-engine/ralph.yml` 的 `event_loop.prompt_file` 仍是 `PROMPT.md`(仓库根).
  - 如果用户按 README 只传 `-c examples/.../ralph.yml` 而不额外 `-p ...`,实际读到的是仓库根 `PROMPT.md`.
  - 仓库根 `PROMPT.md` 目前是一个"重任务"(重建 docs + websearch),不适合作为并行 smoke.

## 改良方向

- 推荐: 让 example config 直接指向 example prompt,并在 prompt 里加入"必须首批同时派发 >=2 条 experiment.task"的硬约束.
  - 这样 autopilot/CI 一键就能复现并行证据,不需要人工 copy/传 -p.

## 2026-02-18 14:05 +0800 | 并行 autopilot 为什么没并发,以及如何用 PROMPT 稳定激发并行

### 现象

- 之前某次 autopilot run 只有 1 个 runner 进入 running.
- 这会导致“并行 runtime 是否真的并发跑起来”无法被有效验证.

### 根因(本质)

- 并行引擎的 runner 并发度,本质上由 `ralph#1` 在同一批窗口里派发的 `experiment.task` 数量决定.
- 如果 top-level prompt(被 `event_loop.prompt_file` 指向)只包含 1 个可执行实验条目,
  那么 `ralph#1` 很自然只会派发 1 条 `experiment.task`,从而只有 1 个 runner job.

### 修复/改良策略

1. 让 example config 的 `event_loop.prompt_file` 指向 example 自己的 PROMPT.
   - 避免误读仓库根的 `PROMPT.md`(通常不是并行 smoke,可能只有 1 条任务或任务太重).
2. 在 `examples/parallel-experimental-dev-engine/PROMPT.md` 里明确硬约束:
   - “第一批窗口必须一次性发布 2 条 `experiment.task`(exp-par-001 与 exp-par-002)”.
   - 这样 Supervisor 会立刻排队 2 个 runner job,并有机会并发执行.

### 证据(已实跑验证)

- out_dir: `/tmp/ralph-autopilot-out-parallel-20260218-133409`
- record-session: `/tmp/ralph-autopilot-out-parallel-20260218-133409/session.jsonl`
- report: `/tmp/ralph-autopilot-out-parallel-20260218-133409/report.json`(exit_code=0)

并发证据(来自 stdout 状态机日志):

- `/tmp/ralph-autopilot-out-parallel-20260218-133409/stdout.txt:75` 出现 `experiment_runner#1:state running`.
- `/tmp/ralph-autopilot-out-parallel-20260218-133409/stdout.txt:76` 出现 `experiment_runner#3:state running`.
- 两行相邻,说明至少在该时刻同时有 2 个 runner 处于 running.
- 解析该文件可得:
  - unique_runner_instances_seen=5(实例 #1..#5 都被创建过)
  - runner_instances_entered_running=3(#1,#3,#4 真正执行过 job)
  - max_concurrent_running=2

## 四文件摘要(continuous-learning,2026-02-18 14:10 +0800)

- 任务目标(task_plan.md):
  - 让 parallel autopilot 闭环稳定 PASS.
  - 让 parallel example 能稳定激发 >=2 runner 并发,并可用 record-session 作为证据.

- 关键决定(task_plan.md):
  - 选择方案A: 修复动态 `ralph#2` 的 coordinator 指令注入,不放松 autopilot 合同.
  - 把 example 的可变输入收敛到 `examples/parallel-experimental-dev-engine/PROMPT.md`,避免误读根 `PROMPT.md`.

- 关键发现(notes.md):
  - “并行度”主要由同一批窗口内派发的 `experiment.task` 数量决定.
  - 并行下 `ralph#2` 若缺失与 `ralph#1` 等价的 prompt,会更容易漂移并破坏 topic 协议.

- 实际变更(WORKLOG.md):
  - `crates/ralph-core/src/parallel/supervisor.rs`: coordinator 指令生成函数支持按 instance 注入.
  - `crates/ralph-core/src/parallel/supervisor/routing.rs`: 动态 `ralph#2` 也注入完整 coordinator instructions.
  - `crates/ralph-core/src/parallel/supervisor/routing_tests.rs`: 新增回归测试锁死 ralph#2 prompt.
  - `examples/parallel-experimental-dev-engine/*`: prompt_file 指向 example prompt,并增加“首批窗口并发派发”硬约束.
  - 实跑 autopilot: `/tmp/ralph-autopilot-out-parallel-20260218-133409` exit_code=0.

- 错误与根因(ERRORFIX.md):
  - integration.applied 缺失的根因之一是 `ralph#2` prompt 注入缺失导致 topic 漂移.

- 可复用点候选:
  1) 并行系统里“备用协调实例”(例如 `ralph#2`)必须继承与主协调实例一致的 prompt/护栏,否则行为漂移会被 hard verdict 放大成 CI 不稳定.
  2) 想验证并行,一定要把“并发派发”写进 prompt 的硬约束,否则只能得到串行执行.
  3) 并行度证据可以从 out_dir 的 `stdout.txt`/`session.jsonl` 提取并固化成指标.

- 是否需要固化到 docs/specs: 否.
  - 原因: 并行示例与 README 已同步更新,且关键回归已由单测锁死.

- 是否提取/更新 skill: 否.
  - 原因: 该坑已在代码与回归测试中被消除; 再写 skill 的边际收益不高.

- 归档处理(说明):
  - 本次检索覆盖了 `task_plan_2026-02-18_132109.md` 与 `notes_2026-02-11_204839.md` 等历史文件.
  - 由于 `notes.md` 内已硬引用 `notes_2026-02-11_204839.md` 的路径,且 notes.md 约束为“只追加”,因此不移动该历史文件,避免产生“引用路径失效”的新坑.

### 补充: autopilot 终端摘要直接显示并行度指标(2026-02-18 14:12 +0800)

- `scripts/summarize_report.py` 已增强:
  - 自动解析 `<out_dir>/stdout.txt` 的 `experiment_runner#*:state` 日志.
  - 输出 max_concurrent_running 等指标.
- 目的: 用户不需要再手工打开 stdout/session.jsonl 就能快速回答“并行到底跑了几个”.

## 2026-02-18 14:28 +0800 | 外部事件注入用于持续派发并行任务(ralph event/emit)

### 关键结论

- 并行模式下,Supervisor 会持续消费“外部事件文件”(JSONL):
  - 路径来自 `.ralph/current-events`.
  - 若 marker 缺失,回退 `.ralph/events.jsonl`.
- 人类或工具可以在另一个终端随时追加 event.
  - 这使得“随时/陆续/连续”注入并行任务成为协议内的一等路径.

### 改良点

- 新增 `ralph event` 子命令,作为 `ralph emit` 的 alias.
  - 目的: 降低记忆成本,让用法更符合直觉.
  - 并行示例与 all-hat prompt 里同步给出 event/emit 两种写法.

## 2026-02-18 15:06 +0800 | 修正: 外部事件注入命令统一使用 ralph emit

- 用户澄清: 外部事件注入应该用 `ralph emit`,不是 `ralph event`.
- 因此回退到更简单的合同:
  - 只保留 `ralph emit`(注入事件).
  - `ralph events`(查看历史)保持不变.
- 这样可以减少歧义,也避免为了“记忆成本”新增 alias 带来的维护面.

## 2026-02-18 17:30 +0800 | 复跑 parallel-engine-autopilot: 稳定性复核与新风险

### 本轮结论(可审计证据)

- autopilot run(PASS):
  - out_dir: `/tmp/ralph-autopilot-out-parallel-20260218-165618`
  - exit_code=0,hard_verdict.passed=true
  - 并行度(从 stdout 状态机推断):
    - unique_runner_instances_seen=5
    - runner_instances_entered_running=3
    - max_concurrent_running=2
- autopilot analyze(PASS,补 agent analysis,不重跑):
  - out_dir: `/tmp/ralph-autopilot-analyze-parallel-20260218-172636`
  - agent_verdict=pass,quality_score=good

### 与上一轮 PASS(153951)对比,暴露出的新风险

- 事件噪声变大:
  - integration.task / integration.applied / experiment.complete 的出现次数从 2 增加到 3(重复派发与重复收敛倾向).
  - agent analysis 的 risks/suggested_fixes 也相应偏向“幂等性/重复任务”风险.
- 真实触发点之一: zsh 里 `status` 是只读变量.
  - 观察到命令片段 `status=$?` 直接报错: `zsh:1: read-only variable: status`.
  - 这会让“测试实际通过,但命令退出码=1”的证据链变脆弱,容易误导 integrator.
  - 建议统一用 `exit_code=$?`/`code=$?` 这类变量名.

### 仍可继续改良的方向(可选)

- 把“重复 experiment.task 不应导致重复 commit”固化为更硬的幂等性要求:
  - runner: 已存在同 message commit 时必须复用,不得再产出新 commit.
  - autopilot: 可选 hard assertion(通过 flag 开启),要求每个 experiment_id 至多 1 个 commit.
- 如果你希望 autopilot 完全不污染当前工作区:
  - 增加“在 out_dir 内临时 clone 并运行”的隔离模式(默认关闭或可选),避免 stash/cherry-pick 影响用户未提交改动.

---

## 2026-02-18 18:13 +0800 | 再复跑 parallel-engine-autopilot + 对比结论 + report.md 改良

### 本轮实跑证据

- autopilot run(out_dir,hard verdict PASS,跳过 agent 分析):
  - `/tmp/ralph-autopilot-out-parallel-20260218-174101`
  - record-session: `/tmp/ralph-autopilot-out-parallel-20260218-174101/session.jsonl`
- autopilot analyze(离线补 agent analysis):
  - `/tmp/ralph-autopilot-analyze-parallel-20260218-180155`
  - agent_verdict=pass,quality_score=acceptable

### 与上一轮 PASS(out_dir: 165618)对比

- 并行度(两轮都稳定): max_concurrent_running=2
- topic_counts 噪声下降(更接近稳定闭环):
  - experiment.task/result/reviewed: 4 -> 3
  - integration.task: 3 -> 2
  - integration.applied: 3 -> 1
  - experiment.complete: 3 -> 2

### agent analysis 给出的风险与建议(180155)

- risks(摘要):
  - 看到 rmcp UnexpectedContentType(None)/Transport channel closed,存在工具启动噪声与不稳定风险.
  - 并发性证据偏“间接推断”,缺少 running_jobs>1 这类直接度量.
  - 集成/验收在工作树有大量未提交改动背景下进行,可能污染审计信号.
- suggested_fixes(摘要):
  - 把并发度指标做成更可审计的证据,并可选纳入 hard verdict.
  - 让 autopilot/smoke 尽量在干净 worktree 或隔离 workspace 内运行.

### 本轮顺手落地的低风险改良

- 改良点: autopilot 的 report.md 现在会直接展示 Topic Counts.
  - 这样打开 report.md 就能看到重复派发是否变多,不需要再手动打开 analysis_input.json.
- 验证 out_dir(快速生成,skip agent analysis):
  - `/tmp/ralph-autopilot-analyze-parallel-20260218-181249`

## 2026-02-18 19:15 +0800 | autopilot/tests 支持显式限并发(强隔离)

- 新增: `ralph autopilot run --child-parallel-max-running-jobs <N>`.
- 实现策略(强隔离): autopilot 在 out_dir 内写入派生配置 `child_ralph.yml`,并只把它传给子进程 `ralph run --config`.
  - 这保证了并发限制是 autopilot/tests 的 opt-in.
  - 用户在自己项目目录里直接执行 `ralph run` 时,不会被这些约束影响.
- 同步更新:
  - `scripts/run_autopilot.sh` 增加同名参数并透传.
  - `.codex/skills/parallel-engine-autopilot/SKILL.md` 补充说明该参数仅用于 autopilot child run.
  - `report.md` 若检测到 `child_ralph.yml` 存在,会额外展示该文件路径,便于审计.
- 回归验证:
  - `cargo fmt` ✅
  - `cargo test -p ralph-cli` ✅
  - `cargo test -p ralph-core smoke_runner` ✅

## 2026-02-18 21:10 +0800 | app_server 三种消息投递模式的协议落地(spawn_instance/turn/steer)

### 关键结论

- “三种消息模式”用最小新增实现:
  - new_instance: 用 `spawn_instance=true` 强制创建新实例(上下文隔离).
  - turn: 默认语义(不写 turn_action),消息进入实例 pending 队列,等空闲后以新 turn 处理.
  - steer: 用 `turn_action="steer"` 把输入注入 in-flight turn(仅 app_server running 时成立,否则自动降级为 turn).

- `spawn_instance` 的约束与降级策略:
  - 推荐必须同时写 `target="<hat_id>"`.
  - 与 `target_instance` 互斥.
  - 若用法不合法或 spawn 失败:
    - Supervisor 会 best-effort 发送一条 `routing.escalate` 给 ralph.
    - 同时降级为普通路由(避免丢消息).

### `ralph agents` 的实现要点

- 并行 Supervisor 在 `.ralph/agents.json` 维护一个小型快照:
  - 当前注册的 instance 列表.
  - state(created/running/idle/done/failed).
  - 是否 dynamic.
  - “最后一次收到的输入摘要”(topic + payload 单行预览,截断到 160 字符).

- `ralph agents` 子命令:
  - 默认读 `.ralph/agents.json` 并表格展示.
  - 支持 `--format json` 输出原始 JSON(便于脚本/调试).

### 典型写法(给 hat/ralph 的心智模型)

- new_instance:
  - `<event ... target="writer" spawn_instance="true" session_strategy="app_server">...</event>`

- turn:
  - `<event ... target_instance="writer#1">...</event>`

- steer:
  - `<event ... target_instance="writer#1" turn_action="steer" session_strategy="app_server">...</event>`

## 四文件摘要(continuous-learning)(2026-02-18 21:15 +0800)

- 任务目标(task_plan.md):
  - 支持 app_server 接收方的三种消息投递模式(new_instance/turn/steer)。
  - 新增 `ralph agents` 查看当前实例列表与“最后一次输入概述”。

- 关键决定(task_plan.md):
  - 选择“最小新增”方案: 只新增 `spawn_instance` 字段,复用既有 `turn_action`/`session_strategy`。

- 关键发现(notes.md):
  - turn/steer 的底层语义已存在(实例侧 steer 不满足条件会自动降级为排队)。
  - 缺口只在“显式开新实例”与“运行态可观测性快照”。

- 实际变更(WORKLOG.md):
  - 新增 `spawn_instance` 字段贯通: proto Event + `<event ...>` 解析 + 外部 JSONL 读取 + Supervisor 路由。
  - 并行 Supervisor 写 `.ralph/agents.json`,新增 `ralph agents` 命令读取展示。
  - 文档已落在 `config/all_hat.md`。

- 错误与根因(ERRORFIX.md,如有):
  - 本次主要是功能迭代,过程中出现的编译错误已就地修复(借用冲突 + 新字段导致 struct literal 缺字段)。

- 可复用点候选(1-3 条):
  1) “新增 per-event 路由提示字段”的落地模板: proto -> parser -> external jsonl -> routing -> tests。
  2) “并行运行态快照文件”作为跨进程可观测性接口: Supervisor 写,CLI 读。

- 是否需要固化到 docs/specs: 是(已完成)
  - 已固化到: `config/all_hat.md`。

- 是否提取/更新 skill: 否
  - 原因: 该能力已通过 all-hat 文档固化,且属于直接功能点,不需要额外 skill 扩散维护面。

## 2026-02-18 21:25 +0800 | UX: `ralph agents` 在子目录也能自动定位工作区

### 需求

- 你希望在仓库子目录执行 `ralph agents` 时,仍能读取到当前工作区的 `.ralph/agents.json`。

### 落地方式(方案A)

- 在 CLI 里实现 best-effort 向上遍历:
  - 从当前 cwd 开始,逐级向上查找最近的 `.ralph/agents.json`。
  - 找到就用;找不到回退到当前目录的默认路径,并提示用户。
  - 若显式传 `--file`,则不做自动定位。

### 回归测试

- 新增 `integration_agents` 覆盖:
  - 在临时目录根写入 `.ralph/agents.json`。
  - 在 `a/b/c` 子目录执行 `ralph agents`。
  - 断言输出包含 `writer#1` 与 `build.task`。

## 2026-02-18 22:28 +0800 | `ralph agents --watch` 设计要点

- 需求本质:
  - `ralph agents` 已能输出一次性快照,但观察并行调度时需要“持续刷新”的可观测性.

- 关键实现选择:
  - `--watch` 仅支持 table 输出:
    - JSON 在 watch 场景会变成“无限流”,对脚本不友好,也缺少稳定 framing.
  - 输出策略按 stdout 是否为 TTY 分流:
    - TTY: 用 ANSI 清屏并原地刷新,体验接近 `watch`.
    - 非 TTY: 不输出清屏控制序列,改为分隔符追加输出,避免污染日志/CI.
  - auto-detect 的细节:
    - watch 模式下,如果用户还没启动并行 run,`.ralph/agents.json` 可能暂不存在.
    - 因此在未指定 `--file` 时,每轮 refresh 都向上遍历父目录探测一次,文件生成后可自动发现.

## 2026-02-19 11:31 +0800 | direct emit 业务 task topic 的风险与并发心智模型

### 为什么 direct emit 容易出问题

- 你绕过了 `ralph#1` 的窗口/backpressure.
- 你需要自己承担:
  - 什么时候该发新任务.
  - 一次发多少.
  - 如何避免控制面(ralph/auditor/integrator)被饿死.

### “emit experiment.task 会不会起很多 CLI 实例”的准确答案

- 会并行,但不会无限并发.
- 每个 hat instance 一次只跑 1 个 job.
- 每个 job 启动前必须拿到全局 permit(由 `parallel.autoscale.max_running_jobs` 控制).
- 你 emit 很多条 task:
  - 超过 cap 的部分不会立刻启动更多 CLI.
  - 它们会进入 instance 的 pending 队列,等待 permit 释放再执行.

### 如何在 direct emit 下控制并行度

- 最推荐: 仍然发 `human.message -> ralph#1`,让它按窗口分批派发.
- 如果必须 direct emit:
  - 用 `--target-instance <hat#key>` 定向到单个 worker,强制串行.
  - 只在你明确需要“新上下文隔离”时才用 `spawn_instance`(否则会扩大并行与资源占用).

## 四文件摘要(continuous-learning)(2026-02-19 13:23 +0800)

- 任务目标(task_plan.md):
  - 近期核心: 修复 parallel autopilot 闭环,让并行示例稳定并发.
  - 近期核心: 增强外部事件注入与 agents 可观测性,并补齐 direct emit 决策清单.
  - 本轮: 回读四文件 + 历史版本,提取复用点,并归档历史版本降低根目录噪音.

- 关键决定(task_plan.md):
  - 不放松 autopilot topic 合同,优先修复根因(ralph#2 指令缺失导致 topic 漂移).
  - `ralph agents` 未指定 `--file` 时向上遍历定位快照.
  - `ralph agents --watch` 区分 stdout 是否为 TTY,避免 ANSI 清屏污染日志/CI.
  - 文档上默认不推荐 direct emit,优先 `human.message -> ralph#1` 让 coordinator 控窗口/backpressure.

- 关键发现(notes.md):
  - 并发是否发生,本质取决于首批窗口派发 `experiment.task` 的数量.
  - 想把“并发”变成可回归证据,需要把“并发派发”写进 PROMPT 的硬约束.
  - 派生子进程配置时,custom backend 必须继承完整 `cli`(command/args/prompt_mode/prompt_flag),否则 validate 会直接失败.
  - 并行里备用协调实例(例如 ralph#2)缺少 coordinator prompt/护栏,会导致 topic 漂移,最终被 hard verdict 放大成 FAIL.
  - watch 输出必须区分 TTY 与非 TTY,否则 ANSI 控制序列会污染日志与快照对比.

- 实际变更(WORKLOG.md):
  - autopilot: agent analysis 默认继承完整 `cli`,并对 exit_code=2 做“结构化产物优先”的判定.
  - parallel: 动态 ralph#2 也注入完整 coordinator instructions + `event_loop.ralph_prompt`,并用单测锁死.
  - example: `event_loop.prompt_file` 指向 example 的 `PROMPT.md`,并增加“首批窗口并发派发”硬约束.
  - CLI: `ralph agents` 支持向上定位快照; `--watch` 支持定时刷新与 TTY/非 TTY 输出策略.
  - docs: `config/all_hat.md` 补齐 direct emit 决策清单与并行度心智模型.

- 错误与根因(ERRORFIX.md):
  - "Custom backend requires a command": 派生 analysis config 丢失 `cli.command/cli.args`.
  - "required_topic 缺失": ralph#2 缺少 prompt/instructions 导致发布了 `integration.done` 而非 `integration.applied`.

- 可复用点候选(1-3 条):
  1) "衍生子进程配置"必须完整继承 backend 的可执行参数,不要只拷贝 `cli.backend`.
  2) 并行系统里,任何“动态创建的协调实例”必须与主协调实例共享同一套 prompt/护栏,否则协议漂移会直接放大为 CI 不稳定.
  3) CLI 读取工作区产物时,优先实现“向上遍历定位 + 显式路径 override”,watch 场景再加 TTY/非 TTY 分流.

- 是否需要固化到 docs/specs: 否.
  - 理由: 关键行为已由回归测试锁死,面向用户的决策边界已写入 `config/all_hat.md`.

- 是否提取/更新 skill: 待定(先做去重检索).
  - 候选方向A: "派生子进程 config 继承完整 cli"的防回归心智模型.
  - 候选方向B: "备用协调实例必须继承 coordinator prompt"的并行系统稳定性原则.

- 归档计划(本轮会执行):
  - 将本次检索覆盖的历史版本文件移动到 `archive/`,并在 `notes.md`/`WORKLOG.md` 追加一条路径更正说明,避免读者按旧路径找不到文件.

## 2026-02-19 13:24 +0800 | 更正: 历史四文件版本已归档到 archive/

- 本轮已归档(降低根目录噪音):
  - `notes_2026-02-11_204839.md` -> `archive/notes_2026-02-11_204839.md`
  - `task_plan_2026-02-18_132109.md` -> `archive/task_plan_2026-02-18_132109.md`
  - `WORKLOG_2026-02-18_210146.md` -> `archive/WORKLOG_2026-02-18_210146.md`

- 说明:
  - `notes.md` 里存在对这些文件名的历史引用.
  - 我没有改写旧段落(遵循"只追加"规则),而是在这里追加路径更正,保证可追溯.

## 2026-02-19 16:16 +0800 | autopilot 复跑结果(硬断言 PASS)

- out_dir:
  - `/tmp/ralph-autopilot-out-parallel-20260219-155244`
- 核心结论:
  - exit_code: `0`(Pass)
  - hard_verdict.passed: `true`
  - termination_reason: `CompletionPromise`
  - required topics 齐全(包含 `integration.applied`)
  - banned topics 未命中(包含 `routing.escalate`/`gate.*`)

- topic_counts(用于观察重复派发,不作为 verdict 门槛):
  - `experiment.task`: 4
  - `experiment.result`: 4(且都包含 `commit`)
  - `experiment.reviewed`: 4(且 `evidence_ok=true`)
  - `integration.task`: 2
  - `integration.applied`: 1
  - `experiment.complete`: 2

- 并行度证据(从 stdout 状态机推断,runner 维度):
  - unique_runner_instances_seen: 5
  - runner_instances_entered_running: 3
  - max_concurrent_running: 2

- 额外观察:
  - 本轮用“单提交快照临时 repo”会丢失 git 历史,导致 integration.task 里的 `commit_lookup_cmd` 在临时 repo 中必然 miss(已记录在 `task_plan.md`).
  - 若后续要更贴近真实集成与可审计,建议改为“保留历史的临时 clone + 覆盖工作树(含未提交改动)”。

## 四文件摘要(continuous-learning)(2026-02-19 16:18 +0800)

- 任务目标(task_plan.md):
  - 在包含未提交改进的代码上复跑 `$parallel-engine-autopilot`,验证闭环 topic 合同与并行度证据稳定.

- 关键决定(task_plan.md):
  - 选择“临时 repo 隔离跑 autopilot”以避免污染当前仓库.

- 关键发现(notes.md):
  - autopilot 本轮 hard verdict PASS,并且 termination_reason=CompletionPromise.
  - “单提交快照临时 repo”会丢失 git 历史,使 `commit_lookup_cmd` 在临时 repo 中必然 miss,从而增加 integrator fallback 与耗时.

- 实际变更(WORKLOG.md):
  - 仅做验证与记录: 运行 autopilot,产物落盘到 `/tmp/...`,并回写四文件.
  - 未修改 Rust 代码与测试.

- 错误与根因(ERRORFIX.md,如有):
  - 本轮无新增错误修复项.

- 可复用点候选(1-3 条):
  1) 在“未提交改动”场景下跑 autopilot 的隔离方法: 优先用“保留历史的临时 clone + 覆盖工作树”,避免 commit_lookup 漂移.

- 是否需要固化到 docs/specs: 否
  - 理由: 这是执行策略/工作流层面的约定,更适合写入 skill/AGENTS.

- 是否提取/更新 skill: 是
  - 目标: 更新 `.codex/skills/parallel-engine-autopilot/SKILL.md`,补齐“如何在未提交改动上隔离运行且保留历史”的推荐做法.

## 2026-02-19 16:26 +0800 | 落地: `scripts/run_autopilot.sh --isolate`

### 我为什么要做这个升级

- 之前的“rsync + git init 单提交快照”会丢失 git 历史.
- integration 阶段的 `commit_lookup_cmd` 经常依赖历史(例如 grep 某条 commit message).
- 一旦历史丢失,就会引入不必要的 fallback 与漂移,拉长 autopilot 时间,也降低可审计性.

### 现在脚本的行为(关键点)

- `--isolate`:
  - `git clone --local` 保留完整历史.
  - `rsync --delete` 覆盖工作树(包含 untracked/未提交改动),并排除 `.git/target/.ralph/.e2e-tests` 等运行态目录.
  - 默认创建 1 个 snapshot commit,让临时 repo worktree 干净(可用 `--no-snapshot-commit` 关闭).
  - isolate 模式默认 out_dir: `/tmp/ralph-autopilot-out-<timestamp>`(避免清理临时 repo 时误删证据).
  - 清理策略: PASS 默认清理临时 repo; FAIL 默认保留;可用 `--keep-temp-repo` 强制保留.

### 典型用法

- `bash scripts/run_autopilot.sh --repo-dir <repo> --config <config> --isolate --skip-agent-analysis`
## 2026-02-19 19:40 +0800 | verify: isolate PASS + E2E PASS(详见 WORKLOG.md)
- out_dir: `/tmp/ralph-autopilot-out-20260219-191459`(report.md 含 Topic Counts + Parallelism)
- E2E mock: `cargo run -q -p ralph-e2e -- codex --mock --filter parallel-starting-event-inference` ✅

## 2026-02-23 18:27 +0800 | E2E: `ralph emit --spawn-instance` 动态实例闭环

- 新增 Tier 8 并行 E2E 场景:
  - `parallel-emit-spawn-instance`
  - 验证点:
    - `ralph#1` 在运行中执行 `ralph emit spawn.task ... --target worker --spawn-instance`
    - Supervisor 创建动态实例 `worker#2`(证据来自 `.ralph/agents.json`)
    - `worker#2` 回送 `spawn.done` 到 `ralph#1`，最终 `LOOP_COMPLETE`
    - 并保留人类可读日志(复制到 `.e2e-tests/artifacts/...`)

- mock-mode 关键机制(为了让回放也能“真的执行 emit”):
  - `mock-cli` 支持从 terminal writes 解析 `[E2E_CMD] ...` 行,并按 allowlist 执行.
  - 默认 allowlist 追加 `ralph emit`，用于该场景.

- 踩坑与修复:
  - 现象: mock-mode 下场景卡到 `max_runtime_seconds`，`worker#2` 未出现.
  - 根因: 本机环境变量 `RALPH_MOCK_ALLOW` 存在但为空字符串,导致覆盖 CLI allowlist,最终 allowlist 为空.
  - 修复: `crates/ralph-e2e/src/main.rs` 将空字符串视为“未提供”,不再覆盖 CLI allowlist.

- 额外改良:
  - allowlist 命令执行时,对 program=`ralph` 做一次本地 binary 解析(`target/release/ralph` 优先),避免 PATH 缺失导致命令执行失败.
