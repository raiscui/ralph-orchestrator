# 任务计划：OpenSpec Apply 收尾（tui-markdown-renderer-mdfried）

## 目标
把 OpenSpec change `tui-markdown-renderer-mdfried` 的剩余任务全部做完：

- tasks.md 的 `4.x`（回归测试/fixtures）与 `5.x`（fmt/clippy/test）全部勾选完成
- `openspec status --change "tui-markdown-renderer-mdfried"` 显示 tasks 全部 done（apply-ready 且可进入 archive）
- 代码变更通过全量验证：`cargo fmt --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo test`（包含 replay smoke tests）

## 方案（至少二选一）

### 方案 A：严格对齐 specs（推荐）
- 以 OpenSpec specs 为最终判据：补齐/确认 `--plain`、ANSI 优先级、安全降级等单测。
- 全量跑 fmt/clippy/test，若 replay smoke tests 因输出布局变化失败，则更新 fixtures（以 specs 语义为准，不做“像素级”对齐）。

### 方案 B：先通过测试，后面再收敛（不推荐）
- 如果 smoke fixtures 变化太大，先放宽断言或跳过部分比对以快速通过。
- 风险：会削弱“回放测试”的护栏，未来更难定位 TUI/输出回归。

> 本次选择：方案 A（因为 change 的核心就是“输出渲染语义”，必须用测试把语义钉死）。

## 阶段
- [x] 阶段1：确认 OpenSpec 状态与未完成项
- [ ] 阶段2：补齐/确认 tasks 4.x 的回归测试
- [ ] 阶段3：运行 fmt/clippy/test + smoke，并修复失败点
- [ ] 阶段4：勾选 tasks + `openspec status` 复核 + 记录到 WORKLOG

## 关键问题
1. `stream_handler` 里是否已经覆盖了 tasks 4.1/4.2/4.3？如果覆盖不足，最小新增哪些测试才算“对齐 specs”？
2. replay smoke tests 是否会因 Markdown 渲染器变化导致 fixtures 变化？如果变化，应该调整 fixtures 还是调整断言口径？
3. clippy 是否会因新渲染路径引入可修复的 lint（例如多余 clone / 不必要分支）？

## 做出的决定
- [决定] 以 `TuiStreamHandler`/`render_text_to_lines` 的行为作为回归测试主锚点。
  - [理由] 这是 Markdown/ANSI 分流的“最小可测单元”，能直接覆盖 Rendered/Plain/ANSI 优先级语义。

## 遇到错误
- 暂无（若 fmt/clippy/test 发现 error，会在此记录摘要，并把细节追加到 ERRORFIX.md）

## 状态
**目前在阶段2**：
我已经确认了 OpenSpec change 的 artifacts 都已完成（apply-ready）。
接下来我会把 specs 里最关键的三条语义，用单测钉死：
`--plain`、不完整 fenced code block 的安全降级、ANSI 优先级（含 ANSI 时不做 Markdown 二次渲染）。
完成后再进入 fmt/clippy/test + smoke 的全量验证阶段。

## 日志
### 2026-01-30 01:08 +0800
- [启动] 继续收尾 OpenSpec change：`tui-markdown-renderer-mdfried`（补齐 tasks 4.x/5.x 并跑全量验证）。

### 2026-01-30 01:12 +0800
- [阶段1] 已执行 `openspec status --change "tui-markdown-renderer-mdfried" --json` 与 `openspec instructions apply --change ... --json`，确认剩余任务为 tasks 4.1~5.2（共 6 项）。
- [阶段2] 已新增/验证单测（`crates/ralph-adapters/src/stream_handler.rs`）：
  - `markdown_unclosed_fenced_code_block_safely_degrades`
  - `ansi_output_skips_markdown_parsing_and_preserves_styles`
  - 并复核 `markdown_plain_mode_keeps_control_symbols_visible` 仍满足 `--plain` 语义。
