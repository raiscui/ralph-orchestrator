# 任务计划(续档)

- 说明: 旧 `task_plan.md` 已超过 1000 行,已续档为 `archive/task_plan_2026-02-28_1717.md`。
- 新任务从这里开始记录。

# 任务计划: 总结 openclaw 启发 + 解释 macOS Xcode license(2026-02-28 17:17 +0800)

## 目标

- 用更短、更可执行的方式,把 `openclaw/openclaw` 对本仓库的启发总结出来。
- 回答你问的: 为什么 `cargo test` 有时会提示需要接受 Xcode license。
- 把本次答复要点追加到 `notes.md`/`WORKLOG.md`(便于后续检索)。

## 阶段

- [x] 阶段1: 从既有研究笔记中提炼要点(openclaw 可迁移机制 + 已落地/待落地)
- [x] 阶段2: 解释 Xcode license 的根因与规避路径(不把环境问题误归因到 cargo)
- [x] 阶段3: 回写四文件(仅追加到文件尾部)

## 状态

**已完成**:

- 已完成 openclaw 启发总结(聚焦可迁移机制: guardrails/doctor/lanes/watchdog/context-window)。
- 已解释 macOS 下 Xcode license 报错的根因与规避路径(Apple toolchain/xcrun/xcode-select vs cargo)。
- 已回写与归档:
  - `notes.md` 已追加 continuous-learning 的四文件摘要。
  - `WORKLOG.md` 已追加本轮记录。
  - 历史版本已归档:
    - `archive/task_plan_2026-02-28_1717.md`
    - `archive/notes_2026-02-28_1706.md`

# 任务计划: 阅读 zeroclaw 源码,提炼可迁移启发(2026-02-28 18:31 +0800)

## 目标

- 阅读 https://github.com/zeroclaw-labs/zeroclaw 的核心代码与文档.
- 提炼对 ralph-orchestrator 有启发的设计点,并给出可落地的改良建议(优先改良,避免无意义新增).

## 两种路线(供选择)

1. 最佳方案(更花时间): clone 全仓,尽可能跑测试/示例,画架构图,逐模块点评,并列出可直接抄作业的 patch/task 清单.
2. 先能用(更快交付): 重点读 README/架构文档 + 核心模块入口,先总结 10-20 条可迁移模式,再按需要深挖.

## 我将采用

- 先走路线2,尽快产出结论.
- 若发现特别值得深挖的模块,再按路线1 对局部加深(只深挖最有价值的点).

## 阶段

- [x] 阶段1: 获取仓库与快速画像(语言/结构/入口/测试)
- [x] 阶段2: 深读关键模块 + 摘录原文证据
- [x] 阶段3: 映射到 ralph 的可迁移清单(可做/不做/原因)
- [x] 阶段4: 回写 notes/WORKLOG/LATER_PLANS,并答复

## 状态

**已完成**:

- 已完成源码阅读与机制提炼(approval/doctor/security/tools/providers/docs/supply-chain).
- 已把要点追加到三文件:
  - `notes.md`
  - `WORKLOG.md`
  - `LATER_PLANS.md`

# 任务计划: 强化 `ralph doctor` 的结构化输出(JSON)与错误分类(2026-02-28 20:46 +0800)

## 目标

- 为 `ralph doctor` 增加机器可读输出,让 code agent/CI/TUI 不必解析 stdout 文本.
- 输出里要包含稳定的分类字段(例如 check_id/category/status),用于自动判定与自动分流.
- 保持现有文本输出不变,避免破坏既有使用与测试.

## 两种路线(供选择)

1. 最佳方案(更工程化): `--format json` 输出稳定 schema,并把每条 check 都赋予稳定 id + category,同时保留原文本 message 作为人类可读.
2. 先能用(更小改动): 仅提供 `--json` 原样输出 warnings/errors 列表(不保证 schema 稳定),后续再补全 id/category.

## 我将采用

- 采用路线1.
- 原因: 你明确说只对 code agent 负责,那就优先让输出"可被程序消费"且长期稳定.

## 阶段

- [x] 阶段1: 读 spec + 盘点现状(doctor 现有检查项与输出)
- [x] 阶段2: 设计 JSON schema(最小稳定字段) + 约定 check_id/category
- [x] 阶段3: 实现 `--format json` + reporter 统一记录 checks
- [x] 阶段4: 补回归测试 + 跑 `cargo fmt`/`cargo test`
- [x] 阶段5: 回写 notes/WORKLOG/LATER_PLANS,并交付

## 状态

**已完成**:

- 已实现 `ralph doctor --format json/--json`(schema v1,含 check_id/category/status/message/fix).
- 已补齐回归测试,并跑完 `cargo fmt`/`cargo test` 验证.

# 任务计划: 设计 fail-closed 的外部事件注入边界(ralph emit / turn_action)(2026-03-01 17:38 +0800)

## 目标

- 允许 hats 在运行过程中用 `ralph emit` 点对点沟通,不要求输出 `<event ...>`.
- 同时把 `turn_action=steer|interrupt` 等控制面信号做成 fail-closed,避免模型误触发造成运行时被打断/被劫持.
- 输出一份"边界规则 + 落地点"清单,后续可以直接按清单实现和写回归测试.

## 两种路线(供选择)

1. 最佳方案(更安全,投入更大): 为 `ralph emit` 增加"来源归因"(source/source_instance)并在 Supervisor 侧做权限校验; 对 `<event ...>` 协议引入 guard token.
2. 先能用(更快落地): 先在 `ralph emit` CLI 层基于环境变量(`RALPH_HAT_ID`/`RALPH_HAT_INSTANCE_ID`)做硬拒绝,禁止 hat 发送 steer/interrupt; Supervisor 侧先做最小防御(发现 steer/interrupt 且疑似来自 hat 就拒绝并回送错误).

## 我将采用

- 先走路线2 交付 4.2(快速止血,高 ROI).
- 路线1 作为 4.1/4.3 的增强(需要明确 threat model,避免过度设计).

## 阶段

- [x] 阶段1: 盘点现状(ralph emit/外部事件读取/turn_action 路径)
- [x] 阶段2: 明确边界规则(谁能 steer,谁只能 request)
- [x] 阶段3: 设计 fail-closed 的反馈协议(错误如何回送给 hat)
- [x] 阶段4: 开 OpenSpec/change,再进入实现

## 状态

**目前在阶段4**:

- 已定位关键代码点:
  - `crates/ralph-cli/src/main.rs`(emit args/emit_command)
  - `crates/ralph-core/src/parallel/supervisor.rs`(外部事件->Event)
  - `crates/ralph-cli/src/parallel_runner.rs`(给 hat 进程注入 RALPH_HAT_ID/INSTANCE_ID)
  - `crates/ralph-core/src/event_parser.rs`(`<event>` 协议解析)
- 已确认边界:
  - hats 之间沟通只走 data-plane(`ralph emit topic=...`).
  - `turn_action=steer|interrupt` 仅用于 ExternalInput -> ralph#1(control-plane).
  - hat-to-hat 采用 request/result,由 B 在 job 结束时回传最终结论,不使用 steer.
- 已创建 OpenSpec change:
  - `openspec/changes/emit-control-plane-fail-closed/`
- 已完成 OpenSpec proposal:
  - `openspec/changes/emit-control-plane-fail-closed/proposal.md`
- 下一步: `openspec` 继续写 `specs`(delta spec),再写 `tasks`(实现清单).

## 2026-03-01 22:40 +0800 | 继续: 为 change 写 `proposal.md`(先锁定边界与反馈语义)

- 我正在做什么:
  - 读取 `openspec status/instructions` 获取 proposal 模板与输出路径。
  - 起草并落盘 `openspec/changes/emit-control-plane-fail-closed/proposal.md`。
- 为什么现在做:
  - change 已创建,但没有任何 artifact,当前仍处于“口头结论”.
  - 先把 data-plane vs control-plane 的边界规则写进 proposal,后续 design/specs 才能对齐并可测试。
- 计划产出(本轮最小闭环):
  - `proposal.md` 明确:
    - 哪些字段属于 control-plane(必须 fail-closed)。
    - 哪些属于 data-plane(允许 hats 互通)。
    - fail-closed 的错误反馈应如何让 hat 自行纠正(面向 4.2)。

## 2026-03-01 22:45 +0800 | 完成: proposal.md 已落盘,解锁 design/specs

- 已落盘:
  - `openspec/changes/emit-control-plane-fail-closed/proposal.md`
- `openspec status` 结果:
  - `proposal` = done
  - `design/specs` = ready
  - `tasks` = blocked(等待 design+specs)

## 2026-03-01 23:54 +0800 | 继续: 为 change 写 `design.md`(收敛实现决策与风险)

- 我正在做什么:
  - 读取 `openspec instructions design` 与 proposal,起草并落盘 `openspec/changes/emit-control-plane-fail-closed/design.md`。
- 为什么这样做:
  - proposal 只回答 why/what,但本 change 属于安全边界收敛,需要把“怎么做”写清楚:
    - 哪些校验在 CLI 做,哪些在 Supervisor 做(防御纵深)。
    - 哪些行为必须拒绝而不是降级(真正 fail-closed)。
    - 对 TUI/manual JSONL 注入的影响与兼容策略。
- 计划产出:
  - design.md 至少包含:
    - 关键决策与备选方案对比(为什么不用 guard token/为什么先做 4.2)。
    - 失败场景与 mitigation(误触发、误投递、无回应排查)。
    - 测试策略(哪些路径必须有回归测试)。

## 2026-03-01 23:57 +0800 | 完成: design.md 已落盘,下一步写 specs

- 已落盘:
  - `openspec/changes/emit-control-plane-fail-closed/design.md`
- `openspec status` 结果:
  - `proposal` = done
  - `design` = done
  - `specs` = ready
  - `tasks` = blocked(等待 specs)

## 2026-03-02 00:28 +0800 | 快进: $openspec-ff-change 生成 specs + tasks(到 apply-ready)

- 我正在做什么:
  - 按 proposal 的 Modified Capabilities,一次性生成 delta specs:
    - `specs/parallel-hat-instances/spec.md`
    - `specs/parallel-trigger-routing/spec.md`
  - 生成 `tasks.md`,把 4.2 落地拆成可执行清单(含测试与文档同步)。
- 为什么现在做:
  - 你明确要 fast-forward,目标是尽快进入实现阶段.
  - applyRequires=tasks,所以把 specs+tasks 补齐后就可以直接 `/opsx:apply`。

## 2026-03-02 00:31 +0800 | 完成: specs + tasks 已齐全(change apply-ready)

- 已新增 delta specs:
  - `openspec/changes/emit-control-plane-fail-closed/specs/parallel-hat-instances/spec.md`
  - `openspec/changes/emit-control-plane-fail-closed/specs/parallel-trigger-routing/spec.md`
- 已新增 tasks:
  - `openspec/changes/emit-control-plane-fail-closed/tasks.md`
- `openspec status` 结果:
  - `proposal/design/specs/tasks` 均为 done
  - `isComplete=true`

## 2026-03-02 00:36 +0800 | 补充: 把 hat-to-hat “只回最终结论,不在中途 reply”写入 change

- 我正在做什么:
  - 更新 `openspec/changes/emit-control-plane-fail-closed/design.md`,增加 D5 决策,明确 request/result 协作约束.
  - 更新 `openspec/changes/emit-control-plane-fail-closed/tasks.md`,把该约束加入 docs 同步任务(4.3).
- 为什么这样做:
  - 这是你明确提出并认可的高 ROI 行为约束,但之前只记录在 `notes.md/task_plan.md`,没有进入 OpenSpec change 的“可实现契约”.
  - 把它写进 design/tasks,实现阶段才不会忘,也避免后续人员误用“中途 reply”导致上游漂移.

## 2026-03-02 12:34 +0800 | 决策落盘: external-only + rejection escalate + 修正 all_hat 示范

- 你给出的反馈与决策:
  - `<event ...>` 这条 in-band 路暂不收口(4.2 只做 external JSONL 注入的 fail-closed).
  - all_hat 示例可以改,避免 hats 学到“对 worker steer”的用法.
  - Supervisor 拒绝 external control-plane 注入时,要让 `ralph#1` 明确看到(不只写日志).
- 我做的同步:
  - 已更新 OpenSpec change 文档与 specs/tasks,明确:
    - external-only 的适用范围.
    - 拒绝时复用 `routing.escalate` 告警到 `ralph#1`.
    - tasks 增加 `config/all_hat.md` 同步任务.

# 任务计划: OpenSpec apply `emit-control-plane-fail-closed` 实现与验收(2026-03-02 15:07 +0800)

## 目标

- 按 `openspec-apply-change` 完整实现 `emit-control-plane-fail-closed` 的待办任务。
- 实现 external control-plane fail-closed 边界,并补齐回归测试与文档同步。
- 确保最终可通过项目要求的测试验证,且 tasks 勾选状态与实际实现一致。

## 执行模式(4-D)

- 本次判定为复杂任务,采用"详细模式"执行。
- 我将按: 解构 -> 诊断 -> 开发 -> 交付 的顺序推进,中途持续同步状态。

## 两种路线

1. 最佳方案(不惜代价): 严格按 tasks 全量实现 + 全量回归(`cargo test`) + 相关子测试定向复核,并同步文档/四文件。
2. 先能用方案: 仅实现核心 fail-closed 逻辑并做最小测试,其余任务后补。

## 我将采用

- 采用路线1。
- 原因: 当前是控制面安全边界任务,若不一次闭环,容易留下高风险回归点。

## 阶段

- [x] 阶段1: 读取 OpenSpec apply 上下文并确认剩余任务
- [x] 阶段2: 按任务逐项实现代码改动(含测试)
- [x] 阶段3: 运行验证并修复编译/测试问题
- [x] 阶段4: 更新 tasks 勾选与四文件收尾,输出交付说明

## 关键问题

1. external turn_action 的 fail-closed 是否同时覆盖 CLI/TUI/Supervisor 三层。
2. 拒绝路径是否能向 `ralph#1` 发出可见告警(`routing.escalate`)。
3. 文档示例是否已移除误导性 worker steer 用法。

## 做出的决定

- [决定] 先读取 `openspec status` 与 `openspec instructions apply` 的动态指令,避免脱离当前任务列表实现。

## 状态

**已完成**:

- 已完成 OpenSpec 任务实现与勾选(17/17)。
- 已完成代码层落地:
  - CLI: `emit` 的 control-plane fail-closed 参数校验与测试。
  - Core: external turn_action 拒绝校验 + warning + `routing.escalate` 告警 + 路由回归测试。
  - TUI: `!steer/!interrupt` 限定为 `ralph#1` 并在本地预检拒绝。
- 已完成文档同步:
  - `specs/parallel-event-channels.spec.md`
  - `examples/parallel-experimental-dev-engine/README.md`
  - `config/all_hat.md`
- 已完成验证:
  - `cargo fmt --check`
  - `cargo test -p ralph-cli`
  - `cargo test -p ralph-core`
  - `cargo test -p ralph-core smoke_runner`
  - `cargo test`(全量)

# 任务计划: 在干净目录 `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-example` 进行 e2e 验证(2026-03-02 16:31 +0800)

## 目标

- 在干净目录环境下实际运行修改后的 `ralph`,验证本次 fail-closed 改动在真实运行链路可用。
- 至少完成一轮可审计的 e2e 执行证据(命令、结果、关键日志)。

## 两种路线

1. 最佳方案: 在 `ralph-example` 里启动并行 run,再用外部 `ralph emit` 注入合法/非法 turn_action 做黑盒验证,最后补跑 `ralph-e2e` 场景。
2. 先能用方案: 仅跑 `ralph-e2e` 现有场景,不做目录级黑盒注入验证。

## 我将采用

- 采用路线1,优先做目录级黑盒验证,再补一条 e2e 场景命令执行。

## 阶段

- [x] 阶段1: 读取 `ralph-example` 配置并确定可执行命令
- [x] 阶段2: 在干净目录启动 run 并注入 external events 验证 fail-closed
- [x] 阶段3: 运行一条 `ralph-e2e` 命令并记录结果
- [x] 阶段4: 回写四文件并交付结论

## 状态

**已完成**:

- 已完成干净目录黑盒验证:
  - 目录: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-example/e2e-clean-20260302-151150`
  - CLI 拒绝路径(非 `ralph#1`/hat 环境)均按预期 fail-closed。
  - 手工 JSONL 注入非法 control-plane 后,Supervisor 产生 warning 并写入 `routing.escalate` 到 `ralph#1`。
- 已完成 `ralph-e2e` 场景:
  - `parallel-emit-spawn-instance` 通过(99.7s)。
- 已回写 `notes.md`/`WORKLOG.md`。
- 下一步: 输出交付结论与后续建议。

# 任务计划: 执行 live steer 场景 e2e (`parallel-app-server-steer-live-reply-multi-turn`)(2026-03-02 16:48 +0800)

## 目标

- 按你选择的后续项,实际跑通(或拿到失败证据) live steer 场景:
  - `parallel-app-server-steer-live-reply-multi-turn`
- 输出可审计证据: 命令、耗时、报告路径、关键断言结果。

## 阶段

- [ ] 阶段1: 启动场景并持续观察运行状态
- [ ] 阶段2: 收集 report/workspace 证据并判定 PASS/FAIL
- [ ] 阶段3: 回写四文件并交付结果

## 状态

**目前在阶段1**:

- 正在启动 `ralph-e2e` live steer 场景执行。

## 2026-03-02 15:22 +0800 | 状态更新: live steer 场景 e2e 已完成

- 已执行命令:
  - `cargo run -p ralph-e2e -- codex --filter parallel-app-server-steer-live-reply-multi-turn --keep-workspace --verbose`
- 执行结果:
  - PASS,总耗时 90.1s。
- 证据路径:
  - `.e2e-tests/report-live.md`
  - `.e2e-tests/report.md`
  - `.e2e-tests/parallel-app-server-steer-live-reply-multi-turn/`

### live steer 场景阶段勾选(收尾)

- [x] 阶段1: 启动场景并持续观察运行状态
- [x] 阶段2: 收集 report/workspace 证据并判定 PASS/FAIL
- [x] 阶段3: 回写四文件并交付结果

## 状态

**已完成**:

- 你选择的后续项 `1` 已执行完成并通过。
- 当前已进入交付阶段,输出最终结论与证据。

# 任务计划: 修复 ralph-e2e 报告落盘一致性(report.md 与 report.json)(2026-03-02 15:25 +0800)

## 目标

- 解决 `--report markdown`(默认)执行后 `report.md` 已更新但 `report.json` 仍是历史内容的问题。
- 让默认路径下 `report.md` 与 `report.json` 始终来自同一轮测试结果,减少误判。

## 两种路线

1. 最佳方案: 在 CLI 层保持 `--report` 兼容语义,但默认 markdown 同步刷新 json 快照,并补测试与文档说明。
2. 先能用方案: 仅在 README 备注 `report.json` 可能过期,不改代码。

## 我将采用

- 采用路线1。
- 原因: 这个问题本质是可观测性一致性缺陷,应从代码层根修,而不是让用户记忆例外规则。

## 阶段

- [ ] 阶段1: 定位 `--report` 到写文件的调用链并确认最小改动点
- [ ] 阶段2: 实现默认 markdown 同步刷新 json 的逻辑 + 补充/更新测试
- [ ] 阶段3: 更新文档并运行测试验证
- [ ] 阶段4: 回写四文件并交付结果

## 状态

**目前在阶段1**:

- 已完成代码定位,准备开始实现改动。

## 2026-03-02 15:29 +0800 | 状态更新: 已完成实现,进入验证阶段

- 已完成阶段1: 调用链定位(`main.rs` -> `ReportWriter::write`)。
- 已完成阶段2: 默认 markdown 改为同步刷新 json,并补充 `main.rs` 单测。
- 已开始阶段3: 运行测试与命令验证。

## 2026-03-02 16:11 +0800 | 状态更新: 报告一致性修复完成

### 阶段勾选(收尾)

- [x] 阶段1: 定位 `--report` 到写文件的调用链并确认最小改动点
- [x] 阶段2: 实现默认 markdown 同步刷新 json 的逻辑 + 补充/更新测试
- [x] 阶段3: 更新文档并运行测试验证
- [x] 阶段4: 回写四文件并交付结果

## 状态

**已完成**:

- 默认 `--report markdown` 已改为同步刷新 `report.json`。
- 已补单测并通过 `cargo test -p ralph-e2e` 与 `cargo test` 全量验证。
- 已用 mock 单场景命令验证运行期会同时写出 `report.md` 和 `report.json`。

# 任务计划: live backend 复验 report.md/report.json 同步一致性(2026-03-02 16:36 +0800)

## 目标

- 在真实 codex backend 的 live 场景下,再次验证默认 `--report markdown` 会同步刷新 `report.json`。
- 给出可审计证据: 场景结果、两份报告内容与时间戳一致性。

## 两种路线

1. 最佳方案: 复跑 `parallel-app-server-steer-live-reply-multi-turn`,并检查 stdout 报告写盘日志 + md/json 内容字段 + mtime。
2. 先能用方案: 仅复跑一个 mock 场景验证。

## 我将采用

- 采用路线1。
- 原因: 你明确要求 live 路径复验,必须用真实 backend 证据闭环。

## 阶段

- [ ] 阶段1: 运行 live 场景并拿到 PASS/FAIL 结果
- [ ] 阶段2: 检查 report.md/report.json 是否同轮更新
- [ ] 阶段3: 回写四文件并交付

## 状态

**目前在阶段1**:

- 准备启动 live 场景执行命令。

## 2026-03-02 16:39 +0800 | 状态更新: live backend 复验完成

- 执行命令:
  - `cargo run -p ralph-e2e -- codex --filter parallel-app-server-steer-live-reply-multi-turn --report markdown --keep-workspace --verbose`
- 执行结果:
  - PASS,耗时 90.1s。
  - 终端输出同时出现:
    - `Report written: .../.e2e-tests/report.md`
    - `Report written: .../.e2e-tests/report.json`

### 阶段勾选(收尾)

- [x] 阶段1: 运行 live 场景并拿到 PASS/FAIL 结果
- [x] 阶段2: 检查 report.md/report.json 是否同轮更新
- [x] 阶段3: 回写四文件并交付

## 状态

**已完成**:

- live backend 路径已确认: 默认 `--report markdown` 会同步刷新 `report.json`。
- `report.md` 与 `report.json` 内容和时间戳一致(同一轮)。

## 2026-03-02 16:42 +0800 | 提交打包: report 同步修复

- 目标:
  - 仅提交本次 `report.md/report.json` 同步修复相关代码与文档。
- 策略:
  - 只暂存 `crates/ralph-e2e/src/main.rs` 与 `crates/ralph-e2e/README.md`。
  - 避免把工作区里其它并行改动混入当前 commit。

## 2026-03-02 16:43 +0800 | 提交完成

- 已提交:
  - `7c3de3b fix(e2e): keep report.json in sync with default markdown report`
- 提交范围:
  - `crates/ralph-e2e/src/main.rs`
  - `crates/ralph-e2e/README.md`
- 说明:
  - 工作区中其他既有改动保持未提交,避免混入本次 report 修复提交。

## 2026-03-02 16:46 +0800 | 提交打包: emit-control-plane-fail-closed 主改动

- 目标:
  - 将剩余 `emit-control-plane-fail-closed` 的代码与文档改动整理为独立 commit。
- 策略:
  - 仅暂存功能相关文件(rlaph-cli/core/tui + specs/examples/config + openspec tasks)。
  - 保持四文件日志和其它不相关改动不入本次提交。

## 2026-03-02 16:47 +0800 | 提交完成: emit-control-plane-fail-closed 主改动

- 已提交:
  - `be5cde8 feat(parallel): fail-close external control-plane turn actions`
- 提交文件:
  - `crates/ralph-cli/src/main.rs`
  - `crates/ralph-core/src/parallel/supervisor/routing.rs`
  - `crates/ralph-core/src/parallel/supervisor/routing_tests.rs`
  - `crates/ralph-tui/src/app.rs`
  - `specs/parallel-event-channels.spec.md`
  - `examples/parallel-experimental-dev-engine/README.md`
  - `config/all_hat.md`
  - `openspec/changes/emit-control-plane-fail-closed/tasks.md`
- 验证:
  - `cargo test` 全量通过后提交。
- 说明:
  - 四文件日志类改动保持未提交,避免污染功能提交边界。

# 任务计划: 强化 `--record-session` 元信息 + 提升中断时落盘可靠性(2026-03-03 10:15 +0800)

## 目标

- `ralph run --record-session <FILE>` 的 JSONL 在开头包含基本元信息:
  - 当前工作目录(cwd)
  - workspace_root(按 config 解析后的值)
  - 运行命令(argv),以及便于人类阅读的一行字符串
- 并行模式下,即使 Ctrl+C/SIGHUP 等中断,也尽量保证 cassette 已把关键证据刷盘:
  - `bus.publish`(外部输入与业务事件)
  - `ux.terminal.write` 的 stdout(用于事件解析的输出,例如 `<event ...>`)
- 修复你反馈的现象:"只问了一个问题,record 里只有 `human.message`,看起来没有回复就结束了".

## 两种路线(供选择)

1. 最佳方案(更工程化,改动更大):
   - 为并行 Supervisor 增加可控的 graceful shutdown:
     - 中断时先 cancel/shutdown 各实例,再 drain 一小段时间,最后退出并 flush cassette.
   - 优点: 退出码与收尾更可控,理论上更不容易留孤儿进程.
   - 代价: 需要改动 ralph-core 的并行收尾协议,并补更多回归测试.

2. 先能用(更快交付,低侵入):
   - 在 `SessionRecorder` 内对"关键记录"做立即 flush(写完就刷盘),降低中断丢尾部证据的概率.
   - 新增 `_meta.session_start` 记录 cwd/workspace_root/argv.
   - 优点: 改动集中在 recorder,对上层逻辑入侵小,可立刻提高可审计性.
   - 代价: record-session 写盘频率增加(但仅在显式开启 `--record-session` 时发生).

## 我将采用

- 采用路线2.
- 原因:
  - 你的诉求是"record 要有基本信息"与"不要出现看起来没回复就结束".
  - flush + meta 记录可以直接提升证据完整性,且不会引入新的回放格式或解析成本.

## 阶段

- [ ] 阶段1: 复现与证据收集(读取用户 record + 本地复跑)
- [ ] 阶段2: 设计 meta schema + flush 策略(只 flush 关键记录)
- [ ] 阶段3: 实现 `SessionRecorder` 改动 + 串行/并行 wiring
- [ ] 阶段4: 回归测试 + `cargo test` + smoke tests
- [ ] 阶段5: 回写 notes/WORKLOG/ERRORFIX/LATER_PLANS(如有)并交付

## 状态

**目前在阶段1**:

- 已读取你的 record: `/Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`。
- 观察到:
  - 仅出现 1 条 `bus.publish`(topic=`human.message`),未出现 `reply.human.message`。
  - `ux.terminal.write` 里全是 stderr(提示 prompt transcript),没有 stdout。
- 已本地复跑并确认一个关键点:
  - reply 有时会在稍后才出现(需要等待 turn 完成)。
  - 但 record-session 默认使用 `BufWriter`,在进程未退出或被强杀时,尾部记录可能还在缓冲区,从磁盘看起来像"没有回复"。
- 下一步: 进入阶段2,把 meta 记录与 flush 策略定下来,再落地代码与测试。

## 2026-03-03 10:28 +0800 | 阶段更新: 已实现 + 已验证通过

### 已完成的实现

- 新增 `_meta.session_start` 记录(用于记录 cwd/workspace_root/argv/pid)。
- `SessionRecorder` 对关键记录做立即 flush:
  - `_meta.*`
  - `bus.publish`
  - `ux.terminal.write` 的 stdout
- 串行/并行 runner 在启用 `--record-session` 时,都会写入 `_meta.session_start`。
- spec 同步更新: `specs/parallel-record-session.spec.md`。

### 验证

- 手工复跑(并行 idle_start):
  - 在进程未退出时读取 JSONL,已经能看到:
    - `_meta.session_start`
    - `bus.publish: reply.human.message`
    - `ux.terminal.write(stdout=true)` 的 `<event topic="reply.human.message" ...>`
- 自动化:
  - `cargo fmt` ✅
  - `cargo test -p ralph-core` ✅
  - `cargo test -p ralph-cli` ✅
  - `cargo test` ✅

### 阶段勾选(收尾)

- [x] 阶段1: 复现与证据收集(读取用户 record + 本地复跑)
- [x] 阶段2: 设计 meta schema + flush 策略(只 flush 关键记录)
- [x] 阶段3: 实现 `SessionRecorder` 改动 + 串行/并行 wiring
- [x] 阶段4: 回归测试 + `cargo test` + smoke tests
- [x] 阶段5: 回写 notes/WORKLOG/ERRORFIX/LATER_PLANS(如有)并交付

## 状态

**已完成**:

- 你的两个诉求均已闭环:
  - record-session 开头能看到“项目路径 + 运行命令”等基本信息。
  - 中断/强杀导致的“尾部 reply 丢失”概率显著下降,离线排障更可靠。

# 任务计划: record-session contract tests + `ralph record watch`(2026-03-03 13:30 +0800)

## 目标

- 把 record-session 当作"证据流 contract",用更对症的测试兜底,避免再出现"E2E 全绿但 record 不可靠".
- 新增一个轻量工具 `ralph record watch`,用于在运行中或刚中断时,快速扫一眼当前证据状态.

## 两种路线(已讨论并选择)

1) 最佳方案: contract tests + watch 工具一起做(A + B).
2) 先能用: 只补测试(A),把 watch 延后。

## 我将采用

- 采用路线 1(A + B).
- 约束选择:
  - 只覆盖 Ctrl+C/SIGINT 与 SIGTERM 这类"可控中断".
  - 不承诺 SIGKILL/断电级别的强保证(那需要更重的 fsync 契约).
- watch 输出:
  - 默认人类可读,不默认 JSON.

## 阶段

- [x] 阶段1: OpenSpec change + delta spec(固化需求与契约)
- [x] 阶段2: 共享 record-session 解析模块(复用 autopilot 现有口径)
- [x] 阶段3: `ralph record summary/watch` CLI
- [x] 阶段4: backpressure tests(unit + SIGINT integration)
- [x] 阶段5: 文档与示例,并回收 LATER_PLANS

## 状态

**已完成**:

- OpenSpec change tasks 已完成(14/14): `record-session-contract-and-watch`。
- 已补齐 `ralph record summary/watch` 的 docs/examples,并解释“为什么 E2E 可能漏掉 durability,以及 contract tests 如何兜底”。
- 已跑 `cargo fmt` + `cargo test` 验证通过。
- change 已进入可归档状态(下一步可执行 `openspec archive`).

## 2026-03-03 21:34 +0800 | 决策更新: watch 默认 raw follow + Ctrl+C 优先写 termination

- 我正在做什么:
  - 在不落地实现的前提下,先把新的偏好与边界写回 OpenSpec change artifacts,避免后续实现被旧文档带偏.
- 为什么要这样做:
  - watch/contract tests 属于“证据流契约”,一旦文档口径漂移,实现与测试很容易各自演进,最终又回到“E2E 全绿但用户踩坑”.
- 新决策(覆盖旧记录,以本条为准):
  1) Ctrl+C/SIGTERM:
     - 语义更偏“尽量写完 `_meta.termination` 并 flush 再退出”(证据完整优先).
  2) `ralph record watch` 默认输出:
     - 不倾向人类可读摘要.
     - 默认像 `tail -f` 一样 raw 输出新增的完整 JSONL 行(低抽象).
  3) watch 无参定位:
     - 允许 `ralph record watch` 不传 FILE.
     - 但 record-session 不一定在 `.ralph/`,因此采用 `.ralph/record-session.latest` 作为“路径指针证据”.
  4) parallel iteration:
     - 定义为“协调者 ralph 的 job 完成次数”,用于 `_meta.termination.iterations` 的口径收敛.

## 2026-03-04 01:05 +0800 | 开始落地: apply OpenSpec change `record-session-contract-and-watch`

- 我正在做什么:
  - 按 `openspec-apply-change` 流程,开始实现 change `record-session-contract-and-watch` 的 tasks 清单.
  - 目标是把“record-session 证据流契约 + watch 工具”真正变成可回归的硬护栏,不再依赖“用户线上踩坑”.
- 为什么现在做:
  - 当前 recorder 已做关键 flush 与 `_meta.session_start`,但 Ctrl+C 下仍可能因“自杀式 killpg”导致 termination 落盘不稳定.
  - 同时 `record watch` 尚不存在,无法在运行中确认 JSONL 是否在增长.
- 本轮计划产出(实现闭环):
  1) `ralph record watch [FILE]`: 默认 raw follow,容忍末尾 incomplete line,支持无参自动定位.
  2) `ralph record summary <FILE>`: strict parse + 人类摘要(复用共享聚合口径).
  3) shared strict parser/aggregator: 供 autopilot 与 summary 复用,减少漂移.
  4) contract tests:
     - SIGINT/SIGTERM 下 termination 落盘与 JSONL 行级可解析性.
     - `.ralph/record-session.latest` 指针写入与无参 watch 定位.

## 2026-03-04 01:10 +0800 | 收尾阶段: 补齐 docs/examples + 复验,然后关闭 change tasks

- 我正在做什么:
  - 继续按 `openspec-apply-change` 的剩余 tasks,把 6.1/6.2 做完.
  - 重点是把 `ralph record summary/watch` 写进用户可见的文档,并把“为什么 E2E 会漏掉 durability”解释清楚.
- 为什么要这样做:
  - 目前代码与测试基本闭环了,但如果 docs 没更新,用户仍会只看到 `--record-session` 一个 flag,不知道如何快速验证/排障.
  - 同时 6.2 这段说明能让团队形成共识: 这类问题要靠 contract tests 卡住,不是靠堆更多 E2E.
- 本阶段计划:
  1) 更新 `docs/guide/cli-reference.md`: 增加 `ralph record summary/watch` 命令与示例(含 `.ralph/record-session.latest` 说明).
  2) 更新 `docs/advanced/testing.md`: 在“录制 fixture/调试 cassette”相关段落补一个 record 工具的用法,并补一段 E2E vs contract tests 的解释.
  3) 检查 `specs/parallel-record-session.spec.md` 的 6.2 说明是否已到位(若缺失则补齐).
  4) 跑 `cargo fmt` + `cargo test`(至少覆盖 `ralph-core/ralph-cli`),确认没有回归.
  5) 在 OpenSpec `tasks.md` 勾选 6.1/6.2 完成,进入可归档状态.

## 2026-03-04 01:34 +0800 | 归档: record-session-contract-and-watch(同步 OpenSpec 主规格)

- 我正在做什么:
  - 归档 OpenSpec change `record-session-contract-and-watch`。
  - 归档将通过 `openspec archive` 完成,而不是手工移动目录。
- 为什么要这样做:
  - 本仓库的 OpenSpec 归档流程自带 validate,并且会把 delta specs 合并到 `openspec/specs/` 主规格中。
  - 这能避免“change 归档了,但主规格没更新”的漂移。
- 将要执行的命令:
  - `openspec archive -y record-session-contract-and-watch`

## 2026-03-04 01:35 +0800 | 归档完成: record-session-contract-and-watch

- 我做了什么:
  - 已执行 `openspec archive -y record-session-contract-and-watch` 完成归档与校验。
- 归档结果:
  - change 已归档到:
    - `openspec/changes/archive/2026-03-04-record-session-contract-and-watch/`
  - delta specs 已同步到 OpenSpec 主规格:
    - `openspec/specs/record-session-contract-and-watch/spec.md`(新建,新增 6 条 requirements)
- 校验:
  - `openspec archive` 输出显示 validate 通过。
  - `openspec list --json` 已不再包含该 change(归档已生效)。

## 2026-03-04 11:18 +0800 | 批量归档: 清理所有 status=complete 的 OpenSpec changes

- 我正在做什么:
  - 按你的确认,把剩余 `status=complete` 的 OpenSpec changes 批量归档。
- 为什么要这样做:
  - 已完成的 change 长期留在 `openspec/changes/` 会增加噪音,也容易造成“主规格没同步”的错觉。
  - `openspec archive` 自带 validate,并会把 delta specs 合并到 `openspec/specs/`,更不容易漂移。
- 已归档的 changes(均为 `openspec archive -y <name>`):
  - `tui-mouse-chat-and-clean-exit`
  - `tui-markdown-rendering`
  - `tui-markdown-renderer-mdfried`
  - `tui-exabind-style`
  - `tui-codeblock-syntax-highlighting`
  - `parallel-hat-solution-eval-example`
  - `codex-app-server-steer`
  - `autopilot-record-session-jsonl-verdict`
  - `emit-control-plane-fail-closed`
- 结果快照:
  - `openspec list --json` 目前只剩:
    - `event-id-and-reply`(no-tasks)
    - `tui-mdfried-viewer`(in-progress)

## 2026-03-04 13:02 +0800 | 修复: 并行 TUI 下 `reply.human.message` 看起来“没显示回答”

- 我正在做什么:
  - 针对你反馈的现象(问 ralph#1 "你是什么模型"但 UI 看起来没有回答),补一条“展示层”修复。
  - 目标是不改变事件解析/路由语义,只让人类在默认 Rendered 输出里能看到回复内容。
- 为什么要这样做:
  - 你的 record-session 里其实已经有 `reply.human.message` 的回复证据。
  - 但在默认 `MarkdownRenderMode::Rendered` 下,`termimad` 可能把 `<event ...>` 当作 HTML 片段处理,
    导致 `<event topic="reply.human.message">...</event>` 整段被吞掉,从而出现“问了但屏幕没显示”的错觉。
- 修复策略(最小侵入):
  - 仅在“展示层渲染”路径上改写:
    - 把 `<event topic="reply.human.message" ...>payload</event>` 改写成只显示 `payload`。
  - `--plain`(Plain 模式)保持原样,用于排障与复制粘贴。
- 回归测试:
  - 新增单测锁定 Rendered 模式下 reply payload 可见,Plain 模式下保留 `<event ...>` 原文。
- 验证:
  - `cargo test -p ralph-adapters` ✅
  - `cargo test -p ralph-tui` ✅

## 2026-03-04 13:20 +0800 | 复验: 用 record-summary 还原事实 + 更新 release 二进制

- 我正在做什么:
  - 用你给的 `ralph-talk-example.jsonl` 复验“到底有没有 reply”,并确保默认 `ralph`(release)二进制已包含展示层修复。
- 为什么要这样做:
  - 这类问题很容易卡在“事实不确定”(到底没回复,还是显示吞掉了)。
  - 另外你平时跑的是 `target/release/ralph`,如果只跑 debug tests,但 release 没重编译,体感上会像“修了但没生效”。
- 复验结果(证据):
  - `ralph record summary /Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`
    - `reply.human.message: 1`
    - stdout tail 可见 `<event topic="reply.human.message" ...>...</event>`
- 构建与验证:
  - `cargo build --release` ✅
  - `cargo test` ✅

## 2026-03-04 13:35 +0800 | 继续收敛: 同一修复必须覆盖 TUI 与非 TUI(Pretty) 两条渲染路径

- 我正在做什么:
  - 针对你说的“我之前就反馈过,你说改好了但现在还有”的情况,把这类问题的防回归手段收敛成硬门槛.
  - 具体到本 bug: 我会检查并补齐 "TUI Rendered" 与 "非 TUI Pretty Rendered" 两条输出路径的行为一致性。
- 为什么要这样做:
  - 当前 `ralph` 有不止一条“展示层渲染”路径:
    - 并行/串行 TUI: 走 `render_text_to_lines(..., Rendered)`。
    - `--no-tui` 或非 TTY 的 Pretty 输出: 走 `render_markdown_with_codeblocks_to_ansi(...)`。
  - 这意味着: 即使我在 TUI 路径修复了 `<event ...>` 被吞的问题,Pretty 路径仍可能继续吞.
  - 只要存在这类“双渲染器分叉”,就很容易出现:
    - agent 侧认为“修了”.
    - 人工在另一条路径复测,仍然失败.
- 我将怎么避免再次发生:
  1) 复用同一个“展示层改写”逻辑,不要在两条路径各写一份.
  2) 加回归测试同时覆盖:
     - TUI Rendered: payload 可见.
     - Pretty Rendered: payload 可见.
     - Plain: `<event ...>` 原文可见(排障契约).
  3) 跑 `cargo test` 作为 backpressure gate,避免“感觉修了”。

## 2026-03-04 13:50 +0800 | 完成: TUI + Pretty 两条路径统一,并用回归测试卡住

- 我做了什么:
  - 将 `reply.human.message` 的展示层改写逻辑复用到 Pretty(stdout) 渲染入口,避免路径分叉.
  - 新增回归测试同时覆盖:
    - TUI Rendered/Plain
    - Pretty Rendered/Plain
- 验证:
  - `cargo test -p ralph-adapters` ✅
  - `cargo test` ✅

## 2026-03-04 14:20 +0800 | 新任务: 把“实时证据流”流程写进 AGENTS.md + 强化 `ralph record watch` 的自动化能力

## 目标

- 把这次总结出来的“实时证据流验证闭环”(record watch/summary + 2x2 展示矩阵)写入 `AGENTS.md`，让后续智能体不再靠记忆与猜。
- 让 `ralph record watch` 更适合编程智能体自动化使用:
  - 能 “等到某个证据出现就退出”(until).
  - 能 “超时失败并给出明确退出码”(timeout/exit code).
  - 能 “安静运行只用退出码做判断”(quiet).
- 让 record-session 的 `_meta.session_start` 更自描述:
  - 增加 `current_exe` 与 `version`，降低“到底跑的是哪个 ralph”歧义。

## 阶段

- [ ] 阶段1: 设计 CLI 语义与退出码(对齐 agent automation)
- [ ] 阶段2: 实现 record watch 新参数 + 回归测试(含超时 exit code)
- [ ] 阶段3: 扩展 `_meta.session_start`(current_exe/version) + 更新 record summary 输出
- [ ] 阶段4: 文档与规范固化(AGENTS.md + docs) + `cargo fmt`/`cargo test` 验证

## 状态

**目前在阶段1**:

- 正在把需求从“口头流程”转换成可执行 contract(参数/输出/退出码/回归测试)。

## 2026-03-04 15:10 +0800 | 执行: 固化"实时证据流"流程到 AGENTS.md + 强化 record watch 自动化 + 补齐 session_start 自描述

- 我正在做什么:
  - 把"如何避免我以为修好了但人工复测仍失败"的流程,写进仓库根 `AGENTS.md`.
  - 继续强化 `ralph record watch` 让它更适合编程智能体自动化:
    - 用退出码表达 verdict(0=命中证据,2=超时),并补齐集成测试.
    - 在 docs 里把 until/timeout/quiet 写清楚,让团队能复用.
  - 扩展 record-session 的 `_meta.session_start`:
    - 增加 `current_exe` 与 `version`,降低"跑的是哪个 ralph"的歧义.

- 为什么要这样做:
  - 这类 bug 的本质是"证据存在但你没看到"或"证据没落盘但 E2E 没覆盖".
  - 仅靠 E2E 很容易漏掉 Ctrl+C/SIGTERM + flush + 展示分叉(TUI/Pretty)这种组合.
  - 把流程写进 `AGENTS.md` + 把工具做成可脚本化探针,才能把"感觉修了"变成"证据证明修了".

- 本阶段计划(目标是一次闭环):
  1) 代码: `_meta.session_start` 增加 `current_exe` + `version`(runner 写入 + summary 展示 + strict parse 兼容).
  2) 代码: `record watch` 补齐 agent 自动化集成测试(命中退出 0 / 超时退出 2).
  3) 文档: 更新 `docs/guide/cli-reference.md` + `docs/advanced/testing.md`,把 until/timeout/quiet 与退出码写清楚.
  4) 规范: 更新根 `AGENTS.md`,加入"durability vs display"决策树 + 2x2 展示矩阵 + 推荐命令.
  5) 验证: `cargo fmt` + `cargo test`.

- 阶段状态(更新版):
  - [x] 阶段1: 设计 CLI 语义与退出码(对齐 agent automation)
  - [ ] 阶段2: 实现 record watch 新参数 + 回归测试(含超时 exit code)
  - [ ] 阶段3: 扩展 `_meta.session_start`(current_exe/version) + 更新 record summary 输出
  - [ ] 阶段4: 文档与规范固化(AGENTS.md + docs) + `cargo fmt`/`cargo test` 验证

- 当前状态:
  - **正在进入阶段2**: 先把 until/timeout 的端到端测试补齐,避免"单测绿但 CLI 行为不对".

## 2026-03-04 15:25 +0800 | 完成: record watch 自动化探针闭环 + session_start 自描述 + AGENTS.md 固化

- 我做了什么:
  - `ralph record watch` 增加并验证了 agent 自动化参数:
    - `--until-event` / `--until-topic` / `--timeout-secs` / `--quiet`
    - 超时退出码固定为 2,便于脚本判定.
  - record-session `_meta.session_start` 补齐自描述字段:
    - `current_exe`(实际二进制路径)
    - `version`(ralph 版本)
  - 更新文档与规范:
    - `docs/guide/cli-reference.md`
    - `docs/advanced/testing.md`
    - 根 `AGENTS.md` 增加"durability vs display"决策树与 2x2 展示矩阵 backpressure.

- 验证:
  - `cargo fmt` ✅
  - `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo test -p ralph-core -p ralph-cli` ✅
  - `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo test` ✅

- 阶段状态(最终):
  - [x] 阶段1: 设计 CLI 语义与退出码(对齐 agent automation)
  - [x] 阶段2: 实现 record watch 新参数 + 回归测试(含超时 exit code)
  - [x] 阶段3: 扩展 `_meta.session_start`(current_exe/version) + 更新 record summary 输出
  - [x] 阶段4: 文档与规范固化(AGENTS.md + docs) + `cargo fmt`/`cargo test` 验证

- 当前状态:
  - **本任务已闭环**: 该类"我以为修好了但人工复测仍失败"的问题,现在有明确的证据链与硬门槛(测试+工具+规范).

## 2026-03-05 12:31 +0800 | 继续排查: 并行 TUI 仍然“看不到回复消息”(reply.human.message)

- 我正在做什么:
  - 继续跟进你最新反馈的现象:"仍然不显示回复消息"。
  - 你提供了新的证据文件:`/Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl`。

- 为什么要这样做:
  - 这类问题常见有 2 种本质:
    1) durability/语义层: 实际没有产生 `reply.human.message`。
    2) display/展示层: 有 `reply.human.message`,但 UI 没把它展示出来(或展示在你没看到的位置)。
  - 先用 record-session 把事实钉死,再改展示层/交互层,避免盲修。

- 已确认的事实(来自 record-session):
  - `target/release/ralph record summary /Users/cuiluming/local_doc/l_dev/my/rust/ralph-talk-example.jsonl` 显示:
    - `reply.human.message: 1`
    - `Stdout Tail` 可见 `<event topic="reply.human.message" ...>...</event>`。
  - 结论: durability/语义层是 OK 的,重点应放在 display/交互层。

- 下一步排查方向(两条并行假设):
  1) **交互层假设**: 你在 Chat 里显式 `@ralph#1` 发消息时,Output 面板仍停留在其它实例(例如 experiment_runner#1)。
     - 结果就是: 回复确实产生了,但你“看的不是 ralph#1 的输出”,体感像“没显示”。
  2) **展示层假设**: 仍存在某条渲染路径没有应用 `reply.human.message` 的展示层改写(或仍被 termimad 吞掉)。

- 计划(目标是一次闭环,不靠猜):
  - [ ] 阶段1: 复盘 record-session + 复现路径定位(确认到底是选错实例,还是渲染吞输出)
  - [ ] 阶段2: 修复交互层: Chat 提交成功后自动切换到目标实例(并在 status 显示 target)
  - [ ] 阶段3: 补回归测试(锁定: @target 发送后 selected_instance 必须切到 target)
  - [ ] 阶段4: `cargo test` 验证,并把结论写入 WORKLOG/ERRORFIX

- 状态:
  - **目前在阶段1**: 先把“事实与复现路径”钉牢,再动代码.

## 2026-03-05 12:57 +0800 | 完成: Chat 提交后自动切到目标实例,避免“回复在但你没看到”

- 我做了什么:
  - 修复并行 TUI 的一个关键交互坑:
    - 当你在 Chat 输入里显式 `@ralph#1`(或其它实例)发送 `human.message` 时,
      UI 现在会在“发送成功”后 best-effort 自动切换 Output 的 selected instance 到目标实例。
    - 同时 status 会显示目标实例,减少误解成本。

- 为什么这能解决你描述的现象:
  - Output 面板是按 "selected instance" 展示的.
  - 旧行为下,你可能给 `@ralph#1` 发了消息,但 Output 仍停在别的实例,
    回复确实产生了,却展示在你没看的地方,体感就是“不显示回复消息”。

- 阶段完成情况(对照本节计划):
  - [x] 阶段1: 复盘 record-session + 复现路径定位(确认 durability OK,优先怀疑交互层)
  - [x] 阶段2: 修复交互层: Chat 提交成功后自动切换到目标实例(并在 status 显示 target)
  - [x] 阶段3: 补回归测试(锁定: @target 发送后 selected_instance 必须切到 target)
  - [x] 阶段4: `cargo test` 验证,并把结论写入 WORKLOG/ERRORFIX

- 验证(硬门槛):
  - `cargo fmt` ✅
  - `cargo test -p ralph-tui` ✅
  - `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo test` ✅

- 当前状态:
  - **本问题已完成交互层修复与验证**。下一步是把结果同步到 WORKLOG/ERRORFIX,并给出后续复测建议。

## 2026-03-05 13:40 +0800 | 加固: reply.human.message 到达时,底部 status 显示预览(兜底可见性)

- 我正在做什么:
  - 在并行 TUI 里,对 `reply.human.message` 再加一层“兜底可见性”.
  - 目标是: 就算 Output 面板当前没选中正确实例,用户也能在底部 status 看到一条短预览,避免继续误判。

- 为什么要这样做:
  - 你反馈的核心痛点不是“事件没产生”,而是“人类没看到”.
  - 仅靠自动切换 selected instance 仍可能在某些边界条件下失效(例如 reply 异步到达,或用户此时正在看别的实例)。
  - 因此把 `reply.human.message` 做成 status 提示,可以把“看不到”降级成“至少看到一条可审计预览”。

- 我做了什么:
  - `ParallelTuiState.apply_event()` 现在会处理 `reply.human.message`:
    - 从 `source_instance` 推导来源(例如 `ralph#1`),
    - 对 payload 做短截断预览,
    - 写入 `chat_status` 作为可见提示.

- 回归测试:
  - 新增单测锁定 status 预览行为:
    - `state::parallel::tests::apply_event_reply_human_message_sets_chat_status_preview`

- 验证:
  - `cargo fmt` ✅
  - `cargo test -p ralph-tui` ✅
  - `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo test` ✅
  - `cargo build --release` ✅

- 当前状态:
  - **兜底可见性已落地并验证**。接下来只需同步 WORKLOG/ERRORFIX 追加记录即可。

## 2026-03-05 14:25 +0800 | 继续修复: Ctrl+C 过早退出 TUI,可能导致“reply 已到但 UI 没显示”

- 我正在做什么:
  - 针对你“并行 TUI 仍看不到回复”的新反馈,把排查重点扩到“退出/中断路径”。

- 为什么要这样做:
  - 当前 TUI 在 raw mode 下捕获 Ctrl+C 后会立刻退出 UI loop。
  - 如果 reply 在中断/收尾阶段才到达(或 UI 来不及渲染最后几帧),就会出现“record-session 有 reply,但 TUI 看不到”的错觉。

- 本轮改动(已实现,待跑测试验证):
  - 第一次 Ctrl+C: 只 request_interrupt,UI 继续运行并等待 terminated 信号。
  - 第二次 Ctrl+C: 强制退出 UI(兜底)。
