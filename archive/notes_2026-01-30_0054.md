# 笔记：OpenSpec FF - mdfrier 渲染器 + GPL3

## 已知事实（来自仓库与调研）
- 目标 change：`openspec/changes/tui-markdown-renderer-mdfried/`
- schema：`spec-driven`
- `applyRequires=["tasks"]`
- `mdfrier`（crates.io）信息：
  - 版本示例：`0.2.0`
  - license：`GPL-3.0-or-later`
  - rust-version：`1.86.0`
  - 提供 `ratatui` feature（含 `ratatui::Theme` 与 markdown→ratatui 行的转换）

## 相关现状（本项目）
- 当前仓库许可证：根目录 `LICENSE` 为 MIT；`Cargo.toml` 的 `[workspace.package].license = "MIT"`。
- 当前 Markdown 渲染（已存在实现）：
  - `crates/ralph-adapters/src/stream_handler.rs::render_text_to_lines(...)`：无 ANSI 时用 `termimad` 把 Markdown 转为 ANSI，再用 `ansi_to_tui` 转为 ratatui 行。
  - 存在 `--plain` 等渲染模式开关（之前 change 已实现）。

## 待在 artifacts 里明确的点
- 是否同时替换 non-TUI pretty 输出的 Markdown 渲染器（建议与 TUI 统一，减少行为分叉）。
- ANSI 与 Markdown 混合策略（默认沿用：检测到 ANSI 就跳过 Markdown 渲染，优先保留 ANSI 样式）。
- 许可证切换的工程动作清单（更新 LICENSE / Cargo.toml / README 等）。

## 过程记录
### 2026-01-30 00:08 +0800
- 已确认 `mdfrier` 的 license 为 `GPL-3.0-or-later`，要直接依赖它，需要把本项目许可证调整为 GPLv3 兼容（例如 `GPL-3.0-or-later`）。

### 2026-01-30 00:21 +0800
- `git` 历史存在多位贡献者（`git --no-pager shortlog -sne HEAD` 可见），且仓库没有 `.gitmodules`（无 submodule）。
- 仓库内目前只发现根目录 `LICENSE` 与 `docs/license.md` 两处显式许可证文档。
- 由于现状为 MIT（包含 `sublicense` 授权），切换到 GPLv3 发行在许可证兼容性上是可行的；但实现时仍需注意保留必要的版权/许可声明（避免把历史 MIT notice “消失”）。
