# 笔记（滚动归档）

- 历史归档：`notes_2026-01-29_2205.md`
- 当前关注：并行 TUI 的输入体验与可读性（Shift+Enter、灰色主题）

## 2026-01-29 22:10 +0800
- 已完成：TUI 启用 kitty keyboard protocol（best-effort），以支持 `Shift+Enter`。
- 已完成：引入 `MUTED_FG = Color::Indexed(245)` 作为 `DarkGray` 与 `Gray` 的折中灰。

## 2026-01-29 22:26 +0800
- tui-markdown-rendering：`mdfried` 为 GPL-3.0-or-later，而本项目为 MIT，因此不直接引入/复用其实现。
- 实现选择：复用项目既有 `termimad` + `ansi_to_tui` 渲染链路，默认渲染 Markdown；`--plain` 关闭渲染回退纯文本。
