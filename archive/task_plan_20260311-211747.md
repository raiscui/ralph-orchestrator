# 任务计划: example 覆盖型 E2E 收敛

## 目标

让 example 目录相关的 E2E/自动化测试稳定跑完。
至少要确认 `parallel-trigger-routing-example` 与 `parallel-experimental-dev-engine-example` 的真实状态。
如果修复完成,还要补齐 smoke 与仓库级测试验证。

## 阶段

- [x] 阶段1: 复盘旧档证据与已落地修复
- [ ] 阶段2: 等待并核对单场景 `parallel-experimental-dev-engine-example` 的最终结果
- [ ] 阶段3: 根据单场景结果继续跑 full example,或定位新的失败点
- [ ] 阶段4: 完成 `smoke_runner`、`cargo test` 与收尾记录

## 关键问题

1. 这次把 `commit` 改成结构化顶层字段后,是否真的让 example 场景稳定收敛?
2. 如果仍失败,失败点还是 `commit` 漂移,还是已经转成别的控制面/集成阶段问题?

## 做出的决定

- 先不继续猜 runtime 根因,优先等待正在运行的单场景 E2E 给出动态证据。
- 因 `task_plan.md`、`notes.md`、`WORKLOG.md`、`ERRORFIX.md` 均超过 1000 行,本轮已先续档,避免继续污染旧档。

## 当前状态

- **目前在阶段2**: 已完成续档与持续学习摘要,正在等待会话 `86054` 的单场景 E2E 最终结果。

## 2026-03-09 11:54:58 +0800 | 续档说明

- 旧文件已重命名:
  - `task_plan_20260309-115458.md`
  - `notes_20260309-115458.md`
  - `WORKLOG_20260309-115458.md`
  - `ERRORFIX_20260309-115458.md`
- 续档原因:
  - `task_plan.md`: 1517 行
  - `notes.md`: 1539 行
  - `WORKLOG.md`: 1004 行
  - `ERRORFIX.md`: 1478 行

## 2026-03-09 11:54:58 +0800 | 旧档复盘摘要

- 已确认的现象:
  - full example E2E 曾出现 `parallel-trigger-routing-example` 通过,`parallel-experimental-dev-engine-example` 失败。
  - 失败形态一度表现为 `integration.task` 缺失或 `commit` 证据丢失。
- 已落地修复:
  - example 配置与 E2E 预填计划都改成强制结构化顶层 `commit`。
  - `experiment.reviewed` 与 `integration.task` 都要求携带明确 `commit`。
  - README 与单测已同步更新。
- 当前最关键的动态证据:
  - 运行中的单场景会话 `86054` 已经走到 `integration.task`。
  - 这说明问题不再像之前那样停在“根本起不来 integration”。

## 2026-03-09 11:54:58 +0800 | 本轮计划

- [ ] 轮询 `86054`,直到单场景 E2E 结束
- [ ] 读取 `.e2e-tests/report.md` / `.e2e-tests/report.json`
- [ ] 若单场景通过,立即重跑 `cargo run -p ralph-e2e -- codex --filter example --keep-workspace --verbose`
- [ ] 若 full example 通过,继续执行:
  - `cargo test -p ralph-core smoke_runner`
  - `cargo test`
- [ ] 根据最终结果更新 `WORKLOG.md` / `ERRORFIX.md`,并判断是否追加 `EPIPHANY_LOG.md` 或清理 `LATER_PLANS.md`

## 2026-03-09 12:55:01 +0800 | 阶段推进: 单场景 `parallel-experimental-dev-engine-example` 已通过

- [x] 阶段2: 等待并核对单场景 `parallel-experimental-dev-engine-example` 的最终结果
- [ ] 阶段3: 根据单场景结果继续跑 full example,或定位新的失败点
- [ ] 阶段4: 完成 `smoke_runner`、`cargo test` 与收尾记录

- 已确认的动态证据:
  - 会话 `86054` 结果为 `✅ parallel-experimental-dev-engine-example (570.9s)`。
  - `events.jsonl` 已出现完整链路:
    - `integration.task`
    - `integration.applied`
    - `experiment.complete`
  - 正式报告显示 `1/1 passed`。

- 当前判断:
  - 结构化顶层 `commit` 契约已经把该单场景最主要的 flaky 点收住了。
  - 下一步应立即复跑 full example,确认两条 example 一起跑时也能稳定通过。
- 运行前基线补充:
  - 主仓库 `git log -n 1` 仍为旧污染提交 `2c4dd37 exp-002: e2e marker file`。
  - 这轮单场景未继续推进主仓库 `HEAD`。
  - 为避免误伤用户现场,本轮不做任何 reset/cleanup,直接在现有基线上复跑 full example。

- 当前状态:
  - **目前在阶段3**: 准备重跑 full example。

## 2026-03-09 13:15:00 +0800 | 静态排查: `parallel-trigger-routing-example` 收尾后 supervisor 发送失败

- 当前任务边界:
  - 只做代码阅读与结论整理
  - 不改代码
  - 目标是回答 `HatInstance actor exited with error instance=ralph#1 error=Failed to send JobCompleted to supervisor` 的最可能触发链路
- 本轮排查步骤:
  - [ ] 读取 example 场景与 E2E 断言,确认 `parallel-trigger-routing-example` 的完成判定与 timeout/130 判定点
  - [ ] 搜索 `Failed to send JobCompleted to supervisor` 的定义与调用链
  - [ ] 回读 parallel runtime 的 shutdown、completion、channel 关闭顺序
  - [ ] 输出 "现象 -> 假设 -> 静态证据 -> 结论"
- 当前状态:
  - **目前在静态排查阶段**: 已回读六文件中的历史记录,下一步开始钉具体源码路径。

## 2026-03-09 13:42:00 +0800 | 静态排查完成

- [x] 读取 example 场景与 E2E 断言,确认 `parallel-trigger-routing-example` 的完成判定与 timeout/130 判定点
- [x] 搜索 `Failed to send JobCompleted to supervisor` 的定义与调用链
- [x] 回读 parallel runtime 的 shutdown、completion、channel 关闭顺序
- [x] 输出 "现象 -> 假设 -> 静态证据 -> 结论"

- 结论摘要:
  - 直接 warning 最可能来自 `HatInstanceActor::on_job_completed()` 正常完成分支
  - completion 后仍报错,更像 Supervisor receiver 生命周期与 late job completion 的竞态
  - `exit 130` 由 E2E timeout -> SIGTERM 触发,不代表业务 completion 没发生
- 当前状态:
  - **本轮排查已完成**: 准备向用户输出精简结论与可疑文件/函数。

## 2026-03-09 12:14:25 +0800 | 继续执行: 从静态结论进入最小可证伪验证

- [x] 阶段1: 复盘旧档证据与已落地修复
- [x] 阶段2: 等待并核对单场景 `parallel-experimental-dev-engine-example` 的最终结果
- [ ] 阶段3: 验证 `parallel-trigger-routing-example` 的真实阻塞点并落修
- [ ] 阶段4: 重新跑 example 全量、smoke 与仓库级测试

- 本轮主假设:
  - `parallel-trigger-routing-example` 的业务事件其实已经闭环。
  - 当前失败更像 completion 之后的收尾竞态,并且现有 stdout-based 断言可能已经脱离真实证据源。
- 本轮备选解释:
  - 不是 shutdown 卡住,而是 example 场景对子实例输出可见性的假设已经过时,导致 job count/LOOP_COMPLETE 断言误报。
- 本轮最小验证计划:
  - [ ] 读取 `parallel_runner`、`codex_app_server_session`、`supervisor`、`instance` 的相关代码,确认 `shutdown_all()` 是否可能长时间等待
  - [ ] 回读 `parallel-trigger-routing-example` 与 `job_run_counts` 的断言来源,确认 stdout 口径是否已经不可靠
  - [ ] 若证据成立,优先做“最正确修复”而不是继续堆叠场景特判

- 当前状态:
  - **目前在阶段3**: 正在执行最小可证伪验证,先钉住 runtime shutdown 与 example 断言口径。

## 2026-03-09 12:28:00 +0800 | 阶段3推进: 准备落修并行 stdout flush 与 cleanup timeout

- [x] 读取 `parallel_runner`、`codex_app_server_session`、`supervisor`、`instance` 的相关代码,确认 `shutdown_all()` 存在无界等待面
- [x] 回读 `parallel-trigger-routing-example` 与 `job_run_counts` 的断言来源,确认 stdout 口径当前会被丢尾部输出放大
- [ ] 修改 `parallel_runner`:
  - 日志模式 stdout 写入后显式 flush
  - runtime cleanup 改为有界等待并保留 warning
- [ ] 补充回归测试
- [ ] 重跑 `parallel-trigger-routing-example`
- [ ] 重跑 `example`、`smoke_runner`、`cargo test`

- 当前状态:
  - **目前在阶段3**: 主假设已被新的动态证据支撑,开始实施修复。

## 2026-03-09 12:40:00 +0800 | 动态验证通过: `parallel-trigger-routing-example` 已恢复

- [x] 修改 `parallel_runner`:
  - 日志模式 stdout 写入后显式 flush
  - runtime cleanup 改为有界等待并保留 warning
- [x] 补充回归测试
- [x] 重跑 `parallel-trigger-routing-example`
- [ ] 重跑 `example`
- [ ] 重跑 `smoke_runner`
- [ ] 重跑 `cargo test`

- 本轮关键结果:
  - `cargo run -p ralph-e2e -- codex --filter parallel-trigger-routing-example --keep-workspace --verbose`
  - 结果: `PASS`
  - 用时: `86.2s`
- 当前状态:
  - **目前在阶段3**: 准备重跑 full example,确认两个 example 场景一起运行时也稳定。

## 2026-03-09 12:49:00 +0800 | full example 验证通过

- [x] 重跑 `example`
- [ ] 重跑 `smoke_runner`
- [ ] 重跑 `cargo test`

- 本轮关键结果:
  - `cargo run -p ralph-e2e -- codex --filter example --keep-workspace --verbose`
  - 结果: `PASS`
  - 明细:
    - `parallel-trigger-routing-example` `79.7s`
    - `parallel-experimental-dev-engine-example` `382.1s`
  - 总耗时: `463.7s`
- 当前状态:
  - **目前在阶段4**: 开始执行仓库级 smoke 与全量测试。

## 2026-03-09 12:34:05 +0800 | 阶段4完成: 仓库级验证全部通过

- [x] 重跑 `smoke_runner`
- [x] 重跑 `cargo test`

- 验证结果:
  - `cargo test -p ralph-core smoke_runner` ✅
  - `cargo test` ✅

- 当前状态:
  - **全部阶段已完成**: 准备做最终总结与交付。

## 2026-03-09 15:51:05 +0800 | 新任务: 扩充更多真实并行范例与对应 E2E

- 任务目标:
  - 为并行模式新增一批更贴近真实工作流的 example。
  - 同时补齐对应的 `ralph-e2e` scenario,让这些范例不只是文档示意,而是能自动回归验证。
- 本轮候选方向:
  - 方案A: 一次铺 5-6 个以上 example,覆盖更多花样。
    - 优点: 看起来全面。
    - 风险: 真实后端 E2E 成本高,一轮内很容易把稳定性和收尾都摊薄。
  - 方案B: 先做 3 个高价值、强区分度的真实场景。
    - 优点: 更容易把 spec、example、README、scenario、验证链路一次做扎实。
    - 风险: 首批覆盖面不如一次性铺很多。
- 当前决定:
  - 先按方案B推进。
  - 第一批优先考虑:
    - `parallel-pr-review`
    - `parallel-release-checklist`
    - `parallel-human-approval-gate`

## 2026-03-09 15:51:05 +0800 | 本轮阶段规划

- [ ] 阶段1: 追加任务上下文并沉淀 spec
## 2026-03-09 17:05:00 +0800 | 继续执行: 第二批真实并行 example 收尾验证

- 当前任务边界:
  - 延续上一轮已完成的第二批 example 落地
  - 本轮优先做验证闭环与日志收尾
- 本轮主计划:
  - [ ] 轮询现有 `cargo test` 会话 `67428`,确认最终退出状态
  - [ ] 若仅剩 warning,清理未使用 import 并重新验证
  - [ ] 跑第二批 3 个 live E2E:
    - `parallel-incident-response-war-room-example`
    - `parallel-migration-rehearsal-example`
    - `parallel-proposal-assembly-example`
  - [ ] 完成 `notes.md` / `WORKLOG.md` / `LATER_PLANS.md` / `EPIPHANY_LOG.md` 收尾
- 当前状态:
  - **目前在验证收尾阶段**: 先收 `cargo test` 最终证据,再继续 live E2E。
- [ ] 阶段2: 实现 3 个真实并行 example
- [ ] 阶段3: 注册对应 E2E scenario 并补断言
- [ ] 阶段4: 跑新增场景验证,再跑仓库级测试

- 本轮主假设:
  - 更真实的并行范例,不一定需要引入新的复杂 runtime 机制。
  - 复用现有 `event_loop.ralph_prompt` 让 `ralph#1` 做 fanout / fanin / gate 收敛,可以先得到更稳的第一批场景。
- 本轮备选解释:
  - 如果 coordinator 语义写得不够硬,真实后端可能仍会把这些场景跑散,导致 example 漂亮但 E2E 不稳。
- 本轮最小验证计划:
  - [ ] 先补 `specs/parallel-real-world-examples.spec.md`,把 3 个场景的 topic 链路和完成条件写清楚
  - [ ] 再按现有 example 模式实现 `examples/*` 与 `crates/ralph-e2e/src/scenarios/*`
  - [ ] 先过滤运行新增 example 场景,确认基本闭环后再跑更大范围测试

- 当前状态:
  - **目前在阶段1**: 已回读上下文文件、现有 example 与 E2E 接线点,正在整理 spec 与最终落地方向。

## 2026-03-09 16:07:07 +0800 | 阶段推进: spec、example 与 scenario 已落地

- [x] 阶段1: 追加任务上下文并沉淀 spec
- [x] 阶段2: 实现 3 个真实并行 example
- [x] 阶段3: 注册对应 E2E scenario 并补断言
- [ ] 阶段4: 跑新增场景验证,再跑仓库级测试

- 已完成内容:
  - 新增 `specs/parallel-real-world-examples.spec.md`
  - 新增 3 个 example 目录:
    - `examples/parallel-pr-review`
    - `examples/parallel-release-checklist`
    - `examples/parallel-human-approval-gate`
  - 新增 3 个 E2E scenario:
    - `parallel-pr-review-example`
    - `parallel-release-checklist-example`
    - `parallel-human-approval-gate-example`
  - 已补注册点:
    - `crates/ralph-e2e/src/scenarios/mod.rs`
    - `crates/ralph-e2e/src/lib.rs`
    - `crates/ralph-e2e/src/main.rs`
  - 已同步可发现性文档与自包含性测试:
    - `README.md`
    - `crates/ralph-e2e/README.md`
    - `crates/ralph-cli/tests/integration_examples.rs`

- 当前状态:
  - **目前在阶段4**: 开始跑格式化、编译与新增场景验证。

## 2026-03-09 16:28:00 +0800 | 阶段4收尾: 重新做最终验证与落盘

- 当前原因:
  - 本轮新增了 3 个 example、3 个 live E2E scenario,还同步改了 README、自包含性测试和共享 helper。
  - 在最终交付前,需要补一轮新鲜验证证据,并把结果正式追加到 `WORKLOG.md` / `EPIPHANY_LOG.md`。
- 本轮动作:
  - [ ] 运行与本轮改动最相关的验证命令
  - [ ] 依据验证结果更新 `task_plan.md`
  - [ ] 追加 `WORKLOG.md`
  - [ ] 判断并更新 `LATER_PLANS.md` / `EPIPHANY_LOG.md`
- 当前状态:
  - **目前仍在阶段4**: 正在开始最终验证,随后完成日志收尾与交付。

## 2026-03-09 16:35:00 +0800 | 阶段4完成: 新增真实并行 example 已验证并完成收尾

- [x] 阶段1: 追加任务上下文并沉淀 spec
- [x] 阶段2: 实现 3 个真实并行 example
- [x] 阶段3: 注册对应 E2E scenario 并补断言
- [x] 阶段4: 跑新增场景验证,再跑仓库级测试

- [x] 运行与本轮改动最相关的验证命令
- [x] 依据验证结果更新 `task_plan.md`
- [x] 追加 `WORKLOG.md`
- [x] 判断并更新 `LATER_PLANS.md` / `EPIPHANY_LOG.md`

- 新鲜验证证据:
  - `cargo test` ✅
    - `integration_examples` 4 条用例全部通过
    - `ralph-e2e` doctest 全部通过
    - 仓库级测试整体通过,未出现新的 error
- 本轮已完成的 live 场景证据:
  - `parallel-pr-review-example` ✅ `179.7s`
  - `parallel-release-checklist-example` ✅ `127.5s`
  - `parallel-human-approval-gate-example` ✅ `201.9s`

- 当前状态:
  - **全部阶段已完成**: 可以对用户正式交付本轮新增范例、验证结果与后续建议。

## 2026-03-09 16:42:00 +0800 | 新任务: 继续扩充第二批真实并行 example 与 E2E

- 任务目标:
  - 基于第一批已验证通过的工作流模板,继续补第二批更贴近真实协作的并行 example。
  - 这次优先落地:
    - `parallel-incident-response-war-room`
    - `parallel-migration-rehearsal`
    - `parallel-proposal-assembly`
- 本轮阶段规划:
  - [ ] 阶段1: 回读现有 example / scenario 模板并沉淀第二批 spec 变更
  - [ ] 阶段2: 实现 3 个新 example
  - [ ] 阶段3: 补齐对应 direct example E2E scenario、注册点、文档与自包含性测试
  - [ ] 阶段4: 跑验证并完成收尾记录
- 本轮主假设:
  - 第一批已经验证过的设计套路可以继续复用。
  - 只要 terminal topic owner 和 external gate 预算设计得当,第二批场景也能较稳地落地。
- 本轮备选解释:
  - 某些场景如果需要更强的人类交互或更长链路,可能不适合直接照抄第一批 coordinator fanout/fanin 模式。
- 本轮最小验证计划:
  - [ ] 先回读现有 example 的 `ralph.yml` / `PROMPT.md` / `README.md`
  - [ ] 再回读 3 个 direct example scenario 的通用接线方式
  - [ ] 先把第二批 spec 和 example 落下,再补 scenario 与验证
- 当前状态:
  - **目前在阶段1**: 正在回读模板并整理第二批场景的落地方向。

## 2026-03-09 17:08:00 +0800 | 阶段推进: 第二批 example、spec 与 scenario 接线已落地

- [x] 阶段1: 回读现有 example / scenario 模板并沉淀第二批 spec 变更
- [x] 阶段2: 实现 3 个新 example
- [x] 阶段3: 补齐对应 direct example E2E scenario、注册点、文档与自包含性测试
- [ ] 阶段4: 跑验证并完成收尾记录

- 已完成内容:
  - 新增第二批 spec:
    - `specs/parallel-real-world-examples-batch-2.spec.md`
  - 新增 3 个 example 目录:
    - `examples/parallel-incident-response-war-room`
    - `examples/parallel-migration-rehearsal`
    - `examples/parallel-proposal-assembly`
  - 新增 3 个 direct example scenario:
    - `parallel-incident-response-war-room-example`
    - `parallel-migration-rehearsal-example`
    - `parallel-proposal-assembly-example`
  - 已补注册点:
    - `crates/ralph-e2e/src/scenarios/mod.rs`
    - `crates/ralph-e2e/src/lib.rs`
    - `crates/ralph-e2e/src/main.rs`
  - 已同步文档与自包含性测试:
    - `README.md`
    - `crates/ralph-e2e/README.md`
    - `crates/ralph-cli/tests/integration_examples.rs`
  - 已顺手改良:
    - `crates/ralph-e2e/src/scenarios/parallel/mod.rs` 新增 direct example setup helper
    - 第一批 3 个 direct example scenario 已改用该 helper,减少重复 setup 代码

- 当前状态:
  - **目前在阶段4**: 开始跑 mermaid 校验、格式化与测试验证。
## 2026-03-09 23:15:00 +0800 | 阶段4异常: `parallel-migration-rehearsal-example` 首轮 live E2E 失败

- 现象:
  - live E2E `parallel-migration-rehearsal-example` ❌ `123.3s`
  - 断言失败:
    - 缺 `migration.ready`
    - 缺最终 payload
    - 未观察到 `LOOP_COMPLETE`
- 当前主假设:
  - 不是 `migration_conductor` 逻辑本身先天坏掉。
  - 而是 `ralph#1` 在中途 continuation 的普通文本里提到了 `LOOP_COMPLETE`。
  - `EventParser::contains_promise()` 会把事件标签外的该字符串视为 completion promise。
  - Supervisor 因此过早进入 completion drain,后续 `migration.go_no_go.request` 只被记录,但不再真正路由给 `migration_conductor`。
- 已验证证据:
  - 动态:
    - `.e2e/.../stdout.txt` 中 `ralph#1:out:job=2` 的普通文本明确出现 `LOOP_COMPLETE`
    - 随后 `ralph#1:out:job=3` 虽发出 `migration.go_no_go.request`,但 `migration_conductor#1` 没有任何 `:err:job=` / `:out:job=` 记录,直接从 `idle` 到 `done`
  - 静态:
    - `crates/ralph-core/src/event_parser.rs` 中 `contains_promise()` 对事件标签外文本做 `contains(promise)`
    - `crates/ralph-core/src/parallel/supervisor.rs` 中一旦命中 completion promise,后续 `JobCompleted` / `Published` 事件将停止派生新 job
    - `crates/ralph-core/src/parallel/instance.rs` 中 shutdown 会清空 pending,实例可在未启动 job 时直接进入 `Done`
- 本轮最小修复:
  - [ ] 强化 `examples/parallel-migration-rehearsal/ralph.yml` 中 coordinator 规则
    - 未收齐 4 条 ready 前必须保持静默
    - 禁止输出任何分析文本
    - 禁止在最终收尾前提及 `LOOP_COMPLETE`
  - [ ] 补一条回归测试,锁住该 guard 文案
  - [ ] 重跑 `parallel-migration-rehearsal-example`
  - [ ] 若通过,继续跑 `parallel-proposal-assembly-example`
- 当前状态:
  - **目前仍在阶段4**: 已完成根因调查,开始实施最小修复。

## 2026-03-09 23:19:50 +0800 | 阶段4完成: 第二批真实并行 example 已全部验证并收尾

- [x] 阶段1: 回读现有 example / scenario 模板并沉淀第二批 spec 变更
- [x] 阶段2: 实现 3 个新 example
- [x] 阶段3: 补齐对应 direct example E2E scenario、注册点、文档与自包含性测试
- [x] 阶段4: 跑验证并完成收尾记录

- 本轮最终验证:
  - live E2E:
    - `parallel-incident-response-war-room-example` ✅ `104.7s`
    - `parallel-migration-rehearsal-example` ✅ `88.5s`
    - `parallel-proposal-assembly-example` ✅ `151.1s`
  - 仓库级验证:
    - `cargo test` ✅

- 本轮新增修复:
  - 已修 `parallel-migration-rehearsal-example` 的 premature completion 问题
  - 修法是强化 coordinator 静默等待规则,避免在中途普通文本里提及 `LOOP_COMPLETE`

- 当前状态:
  - **全部阶段已完成**: 可以正式向用户交付第二批场景、失败根因与最终验证证据。

## 2026-03-10 00:06:00 +0800 | 新任务: 继续扩第三批真实并行 example,并收敛 completion promise 长期风险

- 任务目标:
  - 新增第三批更贴近真实协作的并行 example。
  - 同时把 `completion promise` 在普通 prose 中被误触发的问题,从 prompt 级规避继续推进到 runtime 级护栏。

- 这轮候选方向:
  - 方案A: 只继续堆第三批 example,暂不动 runtime。
    - 优点: 交付面更快扩张。
    - 风险: 迁移场景刚暴露的 completion token 风险仍可能在新场景复发。
  - 方案B: 第三批 example + 一个小而硬的 runtime 护栏一起做。
    - 优点: 新场景一边扩,一边降低已知 flaky 根因。
    - 风险: 需要补更多验证,节奏更重。

- 当前决定:
  - 既然用户已经同意继续,本轮按方案B执行。
  - 第三批优先考虑:
    - `parallel-launch-readiness-command`
    - `parallel-vendor-security-procurement`
    - `parallel-postmortem-action-board`

- 本轮阶段规划:
  - [ ] 阶段1: 回读第二批经验、completion promise 相关代码与第三批落地方向
  - [ ] 阶段2: 为第三批 example 与 runtime 护栏补 spec
  - [ ] 阶段3: 实现 runtime completion promise 护栏与回归测试
  - [ ] 阶段4: 实现第三批 example + direct example E2E + 文档/自包含性测试
  - [ ] 阶段5: 跑 live E2E、仓库级测试并完成收尾记录

- 本轮主假设:
  - 如果把 completion promise 检测收紧到“独立单行语义”,就能显著降低普通 prose 误触发的风险,同时不破坏现有场景。

- 本轮备选解释:
  - 如果现有测试或真实场景里依赖“子串命中就算 completion”,那 runtime 护栏可能会暴露旧用法不一致的问题。

- 本轮最小验证计划:
  - [ ] 先回读 `crates/ralph-core/src/event_parser.rs` 与相关测试
  - [ ] 再回读已通过 example 的 completion 输出形态,确认“独立单行”是否兼容
  - [ ] 先补 runtime 护栏与回归测试,再落第三批 example

- 当前状态:
  - **目前在阶段1**: 正在回读 completion promise 语义与第三批场景模板。

## 2026-03-10 00:17:00 +0800 | 阶段1推进: completion promise 语义已完成静态收敛判断

- [x] 阶段1: 回读第二批经验、completion promise 相关代码与第三批落地方向
- [ ] 阶段2: 为第三批 example 与 runtime 护栏补 spec
- [ ] 阶段3: 实现 runtime completion promise 护栏与回归测试
- [ ] 阶段4: 实现第三批 example + direct example E2E + 文档/自包含性测试
- [ ] 阶段5: 跑 live E2E、仓库级测试并完成收尾记录

- 阶段1结论:
  - `completion promise` 的长期风险已经不是候选方向,而是有静态与动态证据支撑的真实口径问题。
  - 本轮最小正确修复方向确定为:
    - event 外
    - 独立单行
    - runtime 与 E2E termination detection 一起收敛

- 当前状态:
  - **目前在阶段2**: 开始补 batch-3 与 completion promise guardrail spec。

## 2026-03-10 00:27:00 +0800 | 阶段2完成,进入阶段3验证

- [x] 阶段1: 回读第二批经验、completion promise 相关代码与第三批落地方向
- [x] 阶段2: 为第三批 example 与 runtime 护栏补 spec
- [ ] 阶段3: 实现 runtime completion promise 护栏与回归测试
- [ ] 阶段4: 实现第三批 example + direct example E2E + 文档/自包含性测试
- [ ] 阶段5: 跑 live E2E、仓库级测试并完成收尾记录

- 阶段2产物:
  - `specs/parallel-real-world-examples-batch-3.spec.md`
  - `specs/completion-promise-guardrail.spec.md`
- spec 校验:
  - 已使用 `beautiful-mermaid-rs --ascii` 校验 4 个 mermaid 代码块,全部通过。
- 阶段3当前动作:
  - 已修改 `event_parser` / `event_loop` 测试 / `ralph-e2e executor`。
  - 正准备跑格式化与定向测试,确认“独立单行 completion”语义成立。

- 当前状态:
  - **目前在阶段3**: 验证 runtime 与 E2E completion promise 收敛实现。

## 2026-03-10 00:32:00 +0800 | 阶段3完成,进入阶段4

- [x] 阶段1: 回读第二批经验、completion promise 相关代码与第三批落地方向
- [x] 阶段2: 为第三批 example 与 runtime 护栏补 spec
- [x] 阶段3: 实现 runtime completion promise 护栏与回归测试
- [ ] 阶段4: 实现第三批 example + direct example E2E + 文档/自包含性测试
- [ ] 阶段5: 跑 live E2E、仓库级测试并完成收尾记录

- 阶段3已验证:
  - `cargo test -p ralph-core event_parser` ✅
  - `cargo test -p ralph-core event_loop` ✅
  - `cargo test -p ralph-e2e executor` ✅
- 当前结论:
  - runtime 与 E2E 的 `completion promise` 语义已经统一到“事件外 + 独立单行”。
- 当前动作:
  - 开始实现第三批 3 个 direct example 及其注册点。

- 当前状态:
  - **目前在阶段4**: 新增 batch-3 examples 与对应 E2E scenario。

## 2026-03-10 19:08:00 +0800 | 子任务: 实现第三批 launch readiness command example

- 任务背景:
  - 用户明确把写入责任范围限制在 4 个文件内:
    - `examples/parallel-launch-readiness-command/ralph.yml`
    - `examples/parallel-launch-readiness-command/PROMPT.md`
    - `examples/parallel-launch-readiness-command/README.md`
    - `crates/ralph-e2e/src/scenarios/parallel_launch_readiness_command_example.rs`
  - 不允许修改其它文件,也不能回滚他人改动。
  - 需要参考第二批 direct prompt-file example 的风格完成第三批 launch 场景。

- 本次目标:
  - 实现 launch 场景协议:
    - `launch.qa.readiness`
    - `launch.observability.review`
    - `launch.rollback.review`
    - `launch.comms.review`
    - `launch.command.request`
    - `launch.command.ready`
  - 锁住 coordinator / worker 输出约束:
    - coordinator 在未收齐 4 条 ready 前必须静默
    - coordinator 不得输出普通 prose
    - `LOOP_COMPLETE` 只能最后单独一行
    - worker 不能输出 `LOOP_COMPLETE`
  - 固定最终 payload 便于断言:
    - `decision: GO`
    - `command: PROCEED_LAUNCH`
    - `launch_window: 2026-05-01T09:00Z`
  - scenario 需要补齐:
    - topic / payload / agents snapshot
    - no unexpected gate
    - no new jobs after LOOP_COMPLETE
    - 自包含性小测试(如 config 不嵌 raw `<event>`)

- 方案对比:
  - 方案A: 纯复制第二批现有 scenario 结构,最小替换 topic 与文案。
    - 优点: 风险低,更快复用已验证的 direct example 模板。
    - 风险: 容易把与 launch 语义不完全匹配的文案或断言残留带进来。
  - 方案B: 以第二批模板为骨架,重新收敛 launch 的角色职责与最终 payload 结构。
    - 优点: 语义更干净,后续更容易维护。
    - 风险: 需要更仔细核对 prompt 与测试的一致性。

- 当前决定:
  - 采用方案B。
  - 先严格复用第二批成功模式,但 topic、角色职责、README 说明与断言全部按 launch 语义重写。

- 当前阶段:
  - [ ] 阶段1: 对照第二批 prompt-file example 与现有 parallel scenario 模板
  - [ ] 阶段2: 在 3 个 example 文件中实现 launch 场景协议与文档
  - [ ] 阶段3: 在单个 scenario 文件中补齐 direct example E2E 与自包含性测试
  - [ ] 阶段4: 跑定向验证并完成工作记录

- 当前状态:
  - **目前在阶段1**: 开始回读第二批 prompt-file example 模板与现有 parallel scenario helper。

## 2026-03-10 19:14:00 +0800 | 阶段1完成: launch 模板与边界已收敛

- [x] 阶段1: 对照第二批 prompt-file example 与现有 parallel scenario 模板
- [ ] 阶段2: 在 3 个 example 文件中实现 launch 场景协议与文档
- [ ] 阶段3: 在单个 scenario 文件中补齐 direct example E2E 与自包含性测试
- [ ] 阶段4: 跑定向验证并完成工作记录

- 阶段1结论:
  - launch 场景可直接复用第二批 direct prompt-file example 的结构骨架。
  - `setup_prompt_file_example_workspace(...)` 已足够支撑新 scenario,无需额外共享 helper 改动。
  - 本轮最重要的 prompt 护栏是:
    - 未收齐 4 条 ready 前静默
    - coordinator 不得输出普通 prose
    - `LOOP_COMPLETE` 只能最后单独一行
    - worker 禁止输出 `LOOP_COMPLETE`
  - scenario 注册点当前尚未补,但因超出写入责任范围,本轮只做实现并在交付时提醒主线程补齐。

- 当前状态:
  - **目前在阶段2**: 开始落盘 launch example 的 `ralph.yml`、`PROMPT.md`、`README.md`。

## 2026-03-10 19:30:00 +0800 | 阶段2-3完成,进入阶段4验证收尾

- [x] 阶段1: 对照第二批 prompt-file example 与现有 parallel scenario 模板
- [x] 阶段2: 在 3 个 example 文件中实现 launch 场景协议与文档
- [x] 阶段3: 在单个 scenario 文件中补齐 direct example E2E 与自包含性测试
- [ ] 阶段4: 跑定向验证并完成工作记录

- 阶段2产物:
  - `examples/parallel-launch-readiness-command/ralph.yml`
  - `examples/parallel-launch-readiness-command/PROMPT.md`
  - `examples/parallel-launch-readiness-command/README.md`
- 阶段3产物:
  - `crates/ralph-e2e/src/scenarios/parallel_launch_readiness_command_example.rs`
- 当前验证状态:
  - YAML 解析通过
  - scenario 文件 `rustfmt --edition 2024 --check` 通过
  - 文本契约脚本检查通过
  - 由于注册点不在写入范围内, scenario 还无法接入 crate 编译与定向 `cargo test`

- 当前状态:
  - **目前在阶段4**: 整理最终 diff,确认需要主线程补的共享注册点.

## 2026-03-10 20:05:00 +0800 | 继续执行: 定位 `parallel-postmortem-action-board-example` 未发出 `pm.board.request`

- 当前现象:
  - 第三批 direct example 中, `parallel-launch-readiness-command-example` 与 `parallel-vendor-security-procurement-example` 的 live E2E 已通过。
  - `parallel-postmortem-action-board-example` 的 live E2E 仍在 300s timeout。
  - 失败报告缺少:
    - `root_cause.ready`
    - `pm.board.request`
    - `postmortem.board.ready`
  - 但已有历史动态证据显示 stdout 里至少出现过:
    - `timeline.ready`
    - `customer.recap.ready`
    - `actions.ready`
    - `root_cause.ready`

- 当前主假设:
  - runtime 的 completion promise 误判已经不是主问题。
  - 更可能是 postmortem 场景的 coordinator / worker prompt 仍不够机械,导致 `ralph#1` 在收到 4 条 ready 后没有稳定 fan-in 到 `pm.board.request`。

- 最强备选解释:
  - `root_cause.ready` 虽然出现在 stdout,但没有被事件解析器稳定写入 `.ralph/events.jsonl`,从而让 coordinator 视角里永远少一条 ready。

- 最小验证计划:
  - [ ] 复跑 `parallel-postmortem-action-board-example` live E2E,保留 workspace 与 artifacts。
  - [ ] 对照 `.ralph/events.jsonl`、`artifacts/stdout.txt`、scenario 报告,确认 4 条 ready 哪些真正被 parser / runtime 接收。
  - [ ] 若 4 条 ready 都已入库但未 fan-in,优先收紧 example prompt,要求 worker 只发单条 event 且禁止 event 前后 prose。
  - [ ] 修复后先跑定向单测与 scenario,再回到仓库级验证。

- 当前状态:
  - **目前在阶段4/5之间**: 开始做 postmortem 场景的最小可证伪验证,确认卡点在 parser 还是 prompt/fan-in 逻辑。

## 2026-03-10 20:27:00 +0800 | 最小验证完成: 阻塞点已从 runtime 猜测收敛到 action lane 漂移

- [x] 复跑 `parallel-postmortem-action-board-example` live E2E,保留 workspace 与 artifacts
- [x] 对照 `.ralph/events.jsonl`、`artifacts/stdout.txt`、scenario 报告,确认缺失 topic 的真实来源
- [ ] 收紧 `examples/parallel-postmortem-action-board/ralph.yml` 与相关自包含性测试,锁死 event-only worker 输出
- [ ] 重新跑 postmortem 定向验证与 live E2E,确认 `pm.board.request` / `postmortem.board.ready` 回归
- [ ] 之后回到仓库级验证与收尾记录

- 已验证结论:
  - `root_cause.ready`、`timeline.ready`、`customer.recap.ready` 都已真实发布并被 `ralph#1` continuation 接收。
  - 当前 run 缺失的是 `actions.ready`。
  - `action_owner_mapper` 漂移成了输出转义示例 `&lt;event ...&gt;`,而不是输出真实 `<event ...>`。
  - 因此 `ralph#1` 在 job=3 合理地继续等待 `actions.ready`,没有发布 `pm.board.request`。

- 当前状态:
  - **目前在阶段4/5之间**: 开始收紧 postmortem example 的 action lane 与共享 worker 机械约束,优先修 prompt 而不是 runtime。

## 2026-03-10 20:41:00 +0800 | 阶段推进: postmortem 业务链路已通,当前 blocker 切换为 reporter UTF-8 panic

- [x] 收紧 `examples/parallel-postmortem-action-board/ralph.yml` 与相关自包含性测试,锁死 event-only worker 输出
- [ ] 修复 `crates/ralph-e2e/src/reporter.rs` 的 UTF-8 截断 panic 并补测试
- [ ] 修复后重跑 postmortem live E2E
- [ ] 再补 `integration_examples` / 仓库级验证与收尾记录

- 新证据:
  - `.ralph/events.jsonl` 已出现:
    - `actions.ready`
    - `pm.board.request`
    - `postmortem.board.ready`
  - 当前 live run 没有停在业务 topic 缺失,而是 reporter 在生成报告时 panic:
    - `byte index 200 is not a char boundary`
    - 文件: `crates/ralph-e2e/src/reporter.rs:395`

- 当前状态:
  - **目前在新增 blocker 修复阶段**: 开始修 reporter 的 Unicode 安全截断,随后立即回到 postmortem live E2E 复验。

## 2026-03-10 20:55:00 +0800 | 本轮完成

- [x] 复跑 `parallel-postmortem-action-board-example` live E2E,保留 workspace 与 artifacts
- [x] 对照 `.ralph/events.jsonl`、`artifacts/stdout.txt`、scenario 报告,确认缺失 topic 的真实来源
- [x] 收紧 `examples/parallel-postmortem-action-board/ralph.yml` 与相关自包含性测试,锁死 event-only worker 输出
- [x] 修复 `crates/ralph-e2e/src/reporter.rs` 的 UTF-8 截断 panic 并补测试
- [x] 修复 scenario 对最终 payload 仅接受单一文本形态的断言偏差
- [x] 重跑 postmortem live E2E
- [x] 跑 `cargo test -p ralph-cli --test integration_examples`
- [x] 跑仓库级 `cargo test`

- 最终验证结果:
  - `cargo test -p ralph-e2e reporter` ✅
  - `cargo test -p ralph-e2e parallel_postmortem_action_board_example` ✅
  - `cargo test -p ralph-cli --test integration_examples` ✅
  - `cargo run -p ralph-e2e -- codex --filter parallel-postmortem-action-board-example --skip-analysis --keep-workspace --verbose` ✅ `121.1s`
  - `cargo test` ✅

- 当前状态:
  - **全部完成**: postmortem example 的业务链路、reporter、scenario 断言与仓库级测试均已收敛。

## 2026-03-10 21:05:00 +0800 | 启动第四批真实并行 example 设计

- 当前目标:
  - 在前三批基础上继续补 3 个更贴近真实协作的 parallel examples。
  - 这轮优先补“治理 / 客户经营 / 合规”方向,避免继续堆叠发布与事故类场景。

- 方向对比:
  - 方向A: 继续偏工程治理
    - 候选: change advisory board / security exception review / audit evidence pack
    - 优点: 与当前仓库的并行 runtime 调性最贴近,live E2E 风险低。
    - 风险: 与 launch readiness / release checklist 体感相近,新鲜度有限。
  - 方向B: 混合治理 + 商业运营
    - 候选: security exception review / customer renewal desk / audit evidence pack
    - 优点: 题材面更宽,能证明 Ralph 的并行 examples 不只适用于工程发布。
    - 风险: renewal 场景需要更仔细设计固定 payload,避免过于空泛。

- 当前决定:
  - 采用方向B。
  - 第四批暂定 3 个场景:
    1. `parallel-security-exception-review`
    2. `parallel-customer-renewal-desk`
    3. `parallel-audit-evidence-pack`

- 设计约束:
  - 继续沿用 direct prompt-file example 模式。
  - 继续要求 coordinator 在未收齐所有 ready 前保持静默。
  - terminal topic 由 finalizer hat 明确发布。
  - worker 禁止输出 `LOOP_COMPLETE`。
  - 场景断言优先锁 topic 链和固定 payload 字段。

- 当前状态:
  - **目前在阶段1**: 开始把 batch-4 候选收敛成正式 spec 与 topic 协议。

## 2026-03-10 21:18:00 +0800 | 开始实现 batch-4 三个真实并行 example

- [x] 阶段1: 回读 batch-4 spec、现有成功模板与相关上下文,确认可复用骨架和注册点
- [ ] 阶段2: 实现 3 个 batch-4 example 的 `ralph.yml`、`PROMPT.md`、`README.md`
- [ ] 阶段3: 实现 3 个 direct example scenario 与注册点、自包含测试
- [ ] 阶段4: 同步 README 与 `crates/ralph-e2e/README.md` 的 example 列表
- [ ] 阶段5: 运行定向测试、必要 live E2E 与全仓验证,再完成六文件收尾

- 阶段1结论:
  - batch-4 可以直接复用 `parallel-vendor-security-procurement` 与 `parallel-postmortem-action-board` 的稳定骨架。
  - scenario 接线仍走顶层 `src/scenarios/*.rs` + `main.rs` 注册 + `integration_examples.rs` 自包含性测试。
  - 为避免重复踩坑,这 3 个新 example 都继续锁死:
    - worker 只输出真实 event
    - coordinator 未收齐 4 条 ready 前静默
    - finalizer 收到 fan-in request 后立刻只发 1 条 final topic
    - `LOOP_COMPLETE` 只能在 coordinator 最后一行单独输出

- 当前状态:
  - **目前在阶段2**: 开始落盘 3 个 batch-4 example 目录与 prompt 协议。

## 2026-03-10 21:42:00 +0800 | 阶段2-4已落地, 开始进入 batch-4 定向验证

- [x] 阶段1: 回读 batch-4 spec、现有成功模板与相关上下文,确认可复用骨架和注册点
- [x] 阶段2: 实现 3 个 batch-4 example 的 `ralph.yml`、`PROMPT.md`、`README.md`
- [x] 阶段3: 实现 3 个 direct example scenario 与注册点、自包含测试
- [x] 阶段4: 同步 README 与 `crates/ralph-e2e/README.md` 的 example 列表
- [ ] 阶段5: 运行定向测试、必要 live E2E 与全仓验证,再完成六文件收尾

- 新增产物:
  - `examples/parallel-security-exception-review/*`
  - `examples/parallel-customer-renewal-desk/*`
  - `examples/parallel-audit-evidence-pack/*`
  - `crates/ralph-e2e/src/scenarios/parallel_security_exception_review_example.rs`
  - `crates/ralph-e2e/src/scenarios/parallel_customer_renewal_desk_example.rs`
  - `crates/ralph-e2e/src/scenarios/parallel_audit_evidence_pack_example.rs`
- 已补注册点与文档入口:
  - `crates/ralph-e2e/src/scenarios/mod.rs`
  - `crates/ralph-e2e/src/lib.rs`
  - `crates/ralph-e2e/src/main.rs`
  - `crates/ralph-cli/tests/integration_examples.rs`
  - `README.md`
  - `crates/ralph-e2e/README.md`

- 当前状态:
  - **目前在阶段5**: 先做 rustfmt 与定向测试, 再按成本挑选 live E2E 验证顺序。

## 2026-03-10 21:55:00 +0800 | batch-4 live 验证中途插入修复: renewal 场景卡在 self-closing event

- [x] `parallel-security-exception-review-example` live E2E
- [ ] 修复 `parallel-customer-renewal-desk-example` 的 live E2E timeout
- [ ] 跑 `parallel-audit-evidence-pack-example` live E2E
- [ ] 最后跑仓库级 `cargo test`

- 现象:
  - renewal live E2E 在 300s timeout。
  - `events.jsonl` 里已有:
    - `adoption.ready`
    - `support.ready`
  - 缺失:
    - `commercial.ready`
    - `sponsor.ready`
    - `renewal.plan.request`
    - `renewal.plan.ready`

- 已验证结论:
  - `stdout.txt` 里 `commercial_owner#1` 与 `sponsor_mapper#1` 实际输出了事件形态文本。
  - 但它们输出的是自闭合/属性内塞字段的形态,例如 `&lt;event ... /&gt;`。
  - `crates/ralph-core/src/event_parser.rs` 当前只稳定解析成对的 `<event ...>payload</event>`。
  - 因此这轮真正的问题不是 runtime 丢事件,而是 renewal example 对 event 形态约束还不够强。

- 当前修法:
  - 收紧 renewal 与 audit 两个 example 的 worker/finalizer 指令:
    - 禁止自闭合 `&lt;event .../&gt;`
    - 禁止把业务字段塞进 opening tag 属性
    - 必须输出开始标签 + payload 正文 + 结束标签
  - 同步补自包含测试锁住这条护栏。

- 当前状态:
  - **目前在阶段5**: 先重跑 renewal 定向测试与 live E2E,再继续 audit live E2E。

## 2026-03-10 22:08:00 +0800 | 本轮完成

- [x] 阶段1: 回读 batch-4 spec、现有成功模板与相关上下文,确认可复用骨架和注册点
- [x] 阶段2: 实现 3 个 batch-4 example 的 `ralph.yml`、`PROMPT.md`、`README.md`
- [x] 阶段3: 实现 3 个 direct example scenario 与注册点、自包含测试
- [x] 阶段4: 同步 README 与 `crates/ralph-e2e/README.md` 的 example 列表
- [x] 阶段5: 运行定向测试、必要 live E2E 与全仓验证,再完成六文件收尾

- 最终验证结果:
  - `cargo fmt --all --check` ✅
  - `cargo test -p ralph-e2e parallel_security_exception_review_example` ✅
  - `cargo test -p ralph-e2e parallel_customer_renewal_desk_example` ✅
  - `cargo test -p ralph-e2e parallel_audit_evidence_pack_example` ✅
  - `cargo test -p ralph-cli --test integration_examples` ✅
  - `parallel-security-exception-review-example` live E2E ✅ `130.5s`
  - `parallel-customer-renewal-desk-example` live E2E ✅ `131.1s`
  - `parallel-audit-evidence-pack-example` live E2E ✅ `113.2s`
  - `cargo test` ✅

- 当前状态:
  - **全部完成**: batch-4 的 3 个真实并行 example、direct scenario、文档入口、guard 测试与 live E2E 都已收敛。

## 2026-03-11 09:18:00 +0800 | 启动第五批真实并行 example 设计

- 当前目标:
  - 在 batch-4 基础上继续补 3 个不重复的真实并行场景。
  - 这轮优先拉开到“财务运营 / 人才招聘 / 客户落地”三个面,继续证明并行编排不只服务于工程和治理。

- 方向对比:
  - 方向A: 继续偏治理与企业内控
    - 候选: finance close control room / access recertification sweep / change advisory board
    - 优点: 与当前协议骨架最贴近,live E2E 风险低。
    - 风险: 和 audit / security exception / launch readiness 的观感仍然偏近。
  - 方向B: 混合财务运营 + 招聘 + 客户落地
    - 候选: finance close control room / hiring debrief panel / customer onboarding activation
    - 优点: 题材跨度更大,能把矩阵扩到 finance、people、post-sales operations。
    - 风险: onboarding 场景要刻意避开 launch readiness 的既视感。

- 当前决定:
  - 采用方向B。
  - 第五批暂定 3 个场景:
    1. `parallel-finance-close-control-room`
    2. `parallel-hiring-debrief-panel`
    3. `parallel-customer-onboarding-activation`

- 设计约束:
  - 继续沿用 direct prompt-file example 模式。
  - 继续要求 coordinator 在未收齐所有 ready 前保持静默。
  - terminal topic 由 finalizer hat 明确发布。
  - worker 继续禁止输出 `LOOP_COMPLETE`。
  - 默认继续写死 event 三段式,禁止 self-closing event。

- 当前状态:
  - **目前在阶段1**: 开始把 batch-5 候选收敛成正式 spec 与 topic 协议。

## 2026-03-11 18:01:31 +0800 | 继续 batch-5: 从 spec 落盘推进到 scenario 与验证

- [x] 阶段1: 回读六文件、batch-5 spec 与已验证模板,确认继续沿用的骨架和护栏
- [x] 阶段2: 落盘 3 个 batch-5 example 的 `ralph.yml`、`PROMPT.md`、`README.md`
- [ ] 阶段3: 校验 `specs/parallel-real-world-examples-batch-5.spec.md` 的 mermaid,并记录结果
- [ ] 阶段4: 实现 3 个 direct example scenario 与注册点
- [ ] 阶段5: 同步 README / `crates/ralph-e2e/README.md` / integration example 入口
- [ ] 阶段6: 运行定向测试、live E2E 与仓库级验证,再完成六文件收尾

- 当前已确认的事实:
  - batch-5 三个 example 目录已经落盘:
    - `parallel-finance-close-control-room`
    - `parallel-hiring-debrief-panel`
    - `parallel-customer-onboarding-activation`
  - 这轮从 prompt 设计阶段就内建了 batch-4 学到的 event 护栏:
    - 禁止 self-closing `<event .../>`
    - 禁止把业务字段塞进 opening tag 属性
    - 强制 `<event ...>payload</event>`
  - Rust scenario / 注册点 / 文档入口 / 测试链路仍未开始

- 本轮执行顺序:
  1. 先校验 batch-5 spec 的 mermaid,避免图表语法错误继续扩散到实现和文档
  2. 再按 batch-4 成功模板补 3 个 scenario 与注册点
  3. 最后统一做定向测试、3 个 live E2E 与 `cargo test`

- 当前状态:
  - **目前在阶段3**: 先做 batch-5 spec 的 mermaid 校验,然后进入 scenario 实现。

## 2026-03-11 18:01:31 +0800 | batch-5 阶段推进: mermaid 与 Rust 接线已完成

- [x] 阶段3: 校验 `specs/parallel-real-world-examples-batch-5.spec.md` 的 mermaid,并记录结果
- [x] 阶段4: 实现 3 个 direct example scenario 与注册点
- [x] 阶段5: 同步 README / `crates/ralph-e2e/README.md` / integration example 入口
- [ ] 阶段6: 运行定向测试、live E2E 与仓库级验证,再完成六文件收尾

- 已完成的关键动作:
  - `beautiful-mermaid-rs --ascii` 已通过 batch-5 spec 的两个 mermaid 代码块校验
  - 已新增 scenario:
    - `parallel_finance_close_control_room_example.rs`
    - `parallel_hiring_debrief_panel_example.rs`
    - `parallel_customer_onboarding_activation_example.rs`
  - 已同步注册点:
    - `crates/ralph-e2e/src/scenarios/mod.rs`
    - `crates/ralph-e2e/src/lib.rs`
    - `crates/ralph-e2e/src/main.rs`
    - `crates/ralph-cli/tests/integration_examples.rs`
  - 已同步文档入口:
    - `README.md`
    - `crates/ralph-e2e/README.md`

- 当前状态:
  - **目前在阶段6**: 开始做 rustfmt、定向测试,随后进入 3 个 live E2E 与仓库级验证。

## 2026-03-11 18:12:53 +0800 | batch-5 全部完成

- [x] 阶段1: 回读六文件、batch-5 spec 与已验证模板,确认继续沿用的骨架和护栏
- [x] 阶段2: 落盘 3 个 batch-5 example 的 `ralph.yml`、`PROMPT.md`、`README.md`
- [x] 阶段3: 校验 `specs/parallel-real-world-examples-batch-5.spec.md` 的 mermaid,并记录结果
- [x] 阶段4: 实现 3 个 direct example scenario 与注册点
- [x] 阶段5: 同步 README / `crates/ralph-e2e/README.md` / integration example 入口
- [x] 阶段6: 运行定向测试、live E2E 与仓库级验证,再完成六文件收尾

- 最终验证结果:
  - `cargo fmt --all --check` ✅
  - `cargo test -p ralph-e2e parallel_finance_close_control_room_example` ✅
  - `cargo test -p ralph-e2e parallel_hiring_debrief_panel_example` ✅
  - `cargo test -p ralph-e2e parallel_customer_onboarding_activation_example` ✅
  - `cargo test -p ralph-cli --test integration_examples` ✅
  - `cargo run -p ralph-e2e -- codex --filter parallel-finance-close-control-room-example --skip-analysis --keep-workspace --verbose` ✅ `89.1s`
  - `cargo run -p ralph-e2e -- codex --filter parallel-hiring-debrief-panel-example --skip-analysis --keep-workspace --verbose` ✅ `81.4s`
  - `cargo run -p ralph-e2e -- codex --filter parallel-customer-onboarding-activation-example --skip-analysis --keep-workspace --verbose` ✅ `87.1s`
  - `cargo test` ✅

- 当前状态:
  - **全部完成**: batch-5 的 3 个真实并行 example、direct scenario、文档入口、guard 测试与 live E2E 都已收敛。

## 2026-03-11 21:04:14 +0800 | 启动 batch-6: 继续扩到 support / partner / field enablement

- 当前目标:
  - 在 batch-5 基础上继续补 3 个不重复的真实并行场景。
  - 这轮优先拉开到 support ops / partner ops / field enablement 三个面,继续证明并行编排不只服务于工程、治理和客户 onboarding。

- 方向对比:
  - 方向A: 继续偏审批 / 采购 / 内控
    - 候选: procurement intake triage / access recertification sweep / change advisory board
    - 优点: 与已有治理骨架最接近,实现风险低。
    - 风险: 和 `parallel-vendor-security-procurement`、`parallel-security-exception-review`、`parallel-human-approval-gate` 的观感太近。
  - 方向B: support ops + partner ops + field enablement
    - 候选: support escalation desk / partner launch coordination / field enablement rollout
    - 优点: 题材跨度更大,能把矩阵扩到 support、ecosystem、internal enablement。
    - 风险: 需要刻意避免和 incident war-room、launch readiness、customer onboarding 的既视感。

- 当前决定:
  - 采用方向B。
  - 第六批暂定 3 个场景:
    1. `parallel-support-escalation-desk`
    2. `parallel-partner-launch-coordination`
    3. `parallel-field-enablement-rollout`

- 设计约束:
  - 继续沿用 direct prompt-file example 模式。
  - 继续要求 coordinator 在未收齐所有 ready 前保持静默。
  - terminal topic 由 finalizer hat 明确发布。
  - worker 继续禁止输出 `LOOP_COMPLETE`。
  - 默认继续写死 event 三段式,禁止 self-closing event 与 attribute-only payload。

- 阶段规划:
  - [ ] 阶段1: 落 batch-6 spec,写清楚 topic 协议、终态字段和去重边界
  - [ ] 阶段2: 实现 3 个 batch-6 example 的 `ralph.yml`、`PROMPT.md`、`README.md`
  - [ ] 阶段3: 校验 batch-6 spec 的 mermaid
  - [ ] 阶段4: 实现 3 个 direct example scenario 与注册点
  - [ ] 阶段5: 同步 README / `crates/ralph-e2e/README.md` / integration example 入口
  - [ ] 阶段6: 运行定向测试、live E2E 与仓库级验证,再做六文件收尾

- 当前状态:
  - **目前在阶段1**: 开始收敛 batch-6 的正式题材、topic 协议与固定终态字段。
