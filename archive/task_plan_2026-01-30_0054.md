# 任务计划: OpenSpec FF - 更换 Markdown 渲染器（mdfrier）+ 许可证改为 GPL3

## 目标
使用 OpenSpec 的实验性 workflow，把 `tui-markdown-renderer-mdfried` 这个 change 的 artifacts 一次性补齐到可实施状态：

- 生成并写入 `proposal.md` / `design.md` / `specs/**/*.md` / `tasks.md`
- `openspec status --change "tui-markdown-renderer-mdfried"` 显示 `applyRequires=["tasks"]` 已满足
- 不做任何实现代码修改（只产出 artifacts）

## 方案（至少二选一）

### 方案 A：不惜代价，最佳方案（贴近用户目标）
- 将仓库许可证从 MIT 切换到 `GPL-3.0-or-later`（与你提到的“GPL3”一致）
- 直接引入并使用 `mdfrier`（GPL-3.0-or-later）作为 Markdown 渲染器（TUI/pretty 输出按设计决定范围）
- 以 specs 把“用户可见行为”锁死（渲染/降级/`--plain` 等），以 tasks 把工程步骤拆到可验证

### 方案 B：先能用，后面再优雅（风险更低但偏离你当前指令）
- 保持项目 MIT License
- 不直接依赖 `mdfrier`（避免 GPL 传染），仅“参考思路”并继续使用 `termimad` 或改用宽松许可证库
- 这会与你明确的“可以改为 GPL3 并采用 mdfrier”不一致，所以除非你反悔，否则不选

## 阶段
- [ ] 阶段1: 收集 OpenSpec 状态与顺序
- [ ] 阶段2: 创建 proposal
- [ ] 阶段3: 创建 design
- [ ] 阶段4: 创建 specs
- [ ] 阶段5: 创建 tasks（达到 apply-ready）
- [ ] 阶段6: 复核 status 并交付

## 关键问题（在 artifacts 中回答）
1. 许可证：最终是 `GPL-3.0-only` 还是 `GPL-3.0-or-later`？（默认倾向后者，以兼容 `mdfrier` 的 `GPL-3.0-or-later`）
2. 渲染范围：只替换 Supervisor TUI 的 Markdown 渲染，还是同时替换 non-TUI pretty 输出？
3. ANSI 与 Markdown 混合：遇到 ANSI 转义序列时是否仍跳过 Markdown 渲染（保留现有策略）？

## 做出的决定
- [决定] 继续使用已存在的 change：`tui-markdown-renderer-mdfried`（不新建另一个 change）。
  [理由] 你刚刚明确要求 `/opsx:ff` 快进到“可开始实现”的状态，沿用现有 change 最直接。

## 遇到错误
- 暂无（开始阶段）

## 状态
**目前在阶段1**：我将读取 `openspec status --json` 的依赖顺序，然后按 `proposal → design → specs → tasks` 逐个生成文件。

## 日志
### 2026-01-30 00:08 +0800
- [启动] 进入 `/opsx:ff`：为 `tui-markdown-renderer-mdfried` 自动生成全部 artifacts，直到满足 `applyRequires=["tasks"]`。

### 2026-01-30 00:09 +0800
- [阶段1] 已读取 `openspec status --change "tui-markdown-renderer-mdfried" --json`，确认顺序为：`proposal → design + specs → tasks`，且 `applyRequires=["tasks"]`。
- [下一步] 开始生成 `proposal.md`（先建立“为什么要换渲染器/为什么要改 GPL3”的共识边界）。

### 2026-01-30 00:11 +0800
- [阶段2] 已生成 `openspec/changes/tui-markdown-renderer-mdfried/proposal.md`。
- [推进] 当前 `design` / `specs` 已变为 ready，下一步生成 `design.md`（落地技术决策与风险控制）。

### 2026-01-30 00:14 +0800
- [阶段3] 已生成 `openspec/changes/tui-markdown-renderer-mdfried/design.md`（涵盖许可证策略、依赖策略、渲染管线、风险与迁移计划）。
- [下一步] 开始生成 specs（对 `parallel-supervisor-tui` 增补 Markdown 渲染要求，形成可验证的“行为契约”）。

### 2026-01-30 00:17 +0800
- [阶段4] 已生成 delta spec：`openspec/changes/tui-markdown-renderer-mdfried/specs/parallel-supervisor-tui/spec.md`（新增 Markdown 渲染/`--plain`/安全降级/ANSI 优先级要求）。
- [推进] `tasks` 已变为 ready，下一步生成 `tasks.md`（把许可证切换与渲染器替换拆成可验证的工程步骤）。

### 2026-01-30 00:20 +0800
- [阶段5] 已生成 `openspec/changes/tui-markdown-renderer-mdfried/tasks.md`。
- [完成] `openspec status --change "tui-markdown-renderer-mdfried"` 显示 `4/4 artifacts complete`，已满足 `applyRequires=["tasks"]`，进入 apply-ready 状态。
