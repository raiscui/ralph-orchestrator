# 任务计划: 并行 TUI 视觉间距 + 灰色提亮（可读性）

## 目标
1) Chat 输入区的文本/光标不要紧贴下方 `Targets:` 行，视觉上“提高一点”，留出呼吸感。

2) TUI 内大量灰色文本（提示/标签/空态）在部分终端主题下偏暗，整体提亮以改善可读性。

## 阶段
- [x] 阶段1: 更新 spec/notes/task_plan（明确验收标准与原因）
- [x] 阶段2: 实现输入框底部留白（同步 hit-test 保持一致）
- [x] 阶段3: 提亮灰色样式（`Color::DarkGray` -> `Color::Gray`）
- [x] 阶段4: `cargo fmt` + `cargo test` 全量验证
- [x] 阶段5: 追加记录到 `WORKLOG.md` / `ERRORFIX.md`

## 关键问题
1. “太挤”是布局高度不足，还是输入框内的垂直对齐过度贴底？（本次判断：主要是贴底导致与 `Targets` 贴近）
2. 灰色提亮是否会影响快照测试？（本次判断：主要影响 style，不影响文本，但仍以 `cargo test` 做背压验证）

## 做出的决定
- [决定] 先不改底部面板整体布局结构，只在 chat 输入框内部增加 1 行底部留白，并统一把灰色提亮到 `Color::Gray`。
  - [理由] 改动小、可快速验证；先消除“看不清/太挤”的硬体验问题，后续再考虑抽主题系统。

## 遇到错误
- 暂无（进行中如遇到会追加）

## 状态
**已完成**：已通过 `cargo fmt` / `cargo test` 验证，并完成记录追加。

## 参考
- 历史 `task_plan.md` 已因超过 1000 行归档：`task_plan_2026-01-29_2130.md`

## 日志
### 2026-01-29 21:35 +0800
- [完成] spec 已补充“输入框底部留白（不贴 Targets）”验收标准；notes 已记录根因与策略。
- [完成] 实现输入框 bottom breathing room，并同步 hit-test 逻辑，补回归测试。
- [完成] 提亮灰色：并行 TUI 内 `DarkGray` 全量替换为 `Gray`。

### 2026-01-29 21:45 +0800
- [验证] `cargo fmt` ✅
- [验证] `cargo test` ✅（包含 replay smoke tests）
- [记录] 已追加到 `WORKLOG.md` / `ERRORFIX.md`。

---

# 任务计划: 并行 TUI 灰色“折中” + 强制支持 Shift+Enter

## 目标
1) 灰色样式：`Color::Gray` 在你的终端主题下看起来接近白色，太亮；`Color::DarkGray` 又太暗。
   - 需要一个介于两者之间的“提示灰”。

2) Chat 输入：`Shift+Enter` 你这里仍然无法换行，但 `Ctrl+J` 可以。
   - 你希望像 Codex 那样必须支持 `Shift+Enter`。

## 阶段
- [ ] 阶段1: systematic-debugging 调查（定位 Shift+Enter 失效的根因）
- [ ] 阶段2: 实现：启用 kitty keyboard protocol（解开 Enter 修饰键歧义）
- [ ] 阶段3: 实现：引入“折中灰”并替换当前 `Gray` 用法
- [ ] 阶段4: `cargo fmt` + `cargo test` 全量验证
- [ ] 阶段5: 记录到 `notes.md` / `WORKLOG.md` / `ERRORFIX.md`

## 关键问题
1. 为什么 Codex 能区分 `Shift+Enter`，而我们这里不行？
2. 最小的“折中灰”方案是什么，才能跨主题尽量稳定？

## 状态
**目前在阶段1**：先做根因调查，再动代码。

## 日志
### 2026-01-29 22:05 +0800
- [现象] `Ctrl+J` 可用，说明 modifier 事件本身能到应用；但 `Shift+Enter` 仍表现为普通 Enter。
- [计划] 优先确认是否需要启用 crossterm 的 kitty keyboard protocol（`PushKeyboardEnhancementFlags`），来获取 Enter 的修饰键信息。
